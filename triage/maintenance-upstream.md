# Upstream & maintenance (5)

> Part of the [open-issue triage](./README.md) of vuejs/vitepress — snapshot 2026-07-04, generated by Claude Code. Issues rooted in dependencies (Vite/Vue/markdown-it/Shiki/…) or repo chores.

#### [#5073](https://github.com/vuejs/vitepress/issues/5073) Please update esbuild in v1 branch.

`P1` `actionable` `effort:S` · 👍 6 · 💬 2 · opened 6mo ago, active 6d ago · labels: `stale`

VitePress 1.6.4 depends on esbuild ^0.24.2 but esbuild 0.24.2 has security vulnerability (GHSA-67mh-4wv8-2f99); needs update to 0.25.0+.

**Assessment:** Valid security issue affecting stable v1 branch. Current constraint ^0.24.2 prevents upgrades to patched versions. Workaround exists (npm overrides) but core fix is needed.

**Action:** Update esbuild constraint in v1 package.json from ^0.24.2 to ^0.25.0 to include patched versions; release patch version.

#### [#5226](https://github.com/vuejs/vitepress/issues/5226) @vueuse/core contains an annotation that Rolldown cannot interpret

`P2` `possibly-fixed` `effort:S` · 👍 2 · 💬 3 · opened 11d ago, active 5d ago · labels: `upstream`

@vueuse/core v2.0.0-alpha.17 contains misplaced `/* #__PURE__ */` annotations that Rolldown cannot parse, causing build failures.

**Assessment:** Upstream issue in @vueuse/core (not VitePress bug). Root cause already fixed upstream (vueuse PR #5388 merged). Workaround exists: add `rolldownOptions: { checks: { invalidAnnotation: false } }` to vite.config.ts. Waiting for @vueuse/core release with fix.

**Action:** Close: upstream fixed, awaiting vueuse release; document workaround in issue or docs.

#### [#4740](https://github.com/vuejs/vitepress/issues/4740) equation numbering in math with markdown-it-mathjax3 triggers duplicate label errors

`P2` `upstream-blocked` `effort:M` · 👍 1 · 💬 2 · opened 1.2y ago, active 6d ago · labels: `upstream` `stale`

Math equation numbering increases on HMR updates and duplicate label errors occur when using markdown-it-mathjax3 with equation labels.

**Assessment:** Maintainer brc-dd confirmed root cause is in markdown-it-mathjax3 plugin state management; provided workaround code. Secondary report indicates memory leak issues with the workaround. Needs upstream plugin fix.

**Action:** Close with upstream reference to markdown-it-mathjax3 issue #55; document workaround in FAQ but note memory concerns.

#### [#4618](https://github.com/vuejs/vitepress/issues/4618) 部分图片写法无法解析

`P2` `upstream-blocked` `effort:M` · 👍 0 · 💬 1 · opened 1.3y ago, active 6d ago · labels: `upstream` `stale`

HTML img tags without `./` prefix in src attribute (e.g., `<img src="img/file.png" />`) fail to parse correctly, but `./img/file.png` and markdown `![](img/file.png)` syntax work.

**Assessment:** Upstream issue. brc-dd identified root cause as vite-plugin-vue bug, not VitePress. User provided reproduction screenshots.

**Action:** Close: upstream — report to vite-plugin-vue repo; VitePress markdown handling is correct.

#### [#5126](https://github.com/vuejs/vitepress/issues/5126) Algolia search results truncated on mobile — breadcrumb and inline context not shown despite sufficient space

`P2` `upstream-blocked` `effort:M` · 👍 0 · 💬 2 · opened 4mo ago, active 6d ago · labels: `upstream` `stale`

Algolia search results on mobile viewports truncate breadcrumb paths and inline context despite sufficient space, unlike desktop view which displays full details.

**Assessment:** Valid bug with screenshot reproduction. brc-dd identified the root cause as Algolia API response (2026-02-23) and said they would create an upstream issue. Reporter provided a workaround using character limit adjustment on mobile. Blocked on Algolia fix.

**Action:** Verify upstream issue filed with Algolia; track their fix or apply character-limit workaround internally if Algolia does not respond.

