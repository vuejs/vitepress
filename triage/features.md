# Feature requests & enhancements (195)

> Part of the [open-issue triage](./README.md) of vuejs/vitepress — snapshot 2026-07-04, generated by Claude Code. Grouped by area, sorted by community demand (👍).

## Markdown / content pipeline (26)

#### [#3093](https://github.com/vuejs/vitepress/issues/3093) Feature Request: Inline Syntax Highlight

`P1` `actionable` `effort:M` · 👍 16 · 💬 3 · opened 2.7y ago, active 2d ago · labels: `theme` `stale`

Request for inline syntax highlighting for backtick code; currently inline code has no highlighting unlike code blocks.

**Assessment:** Strong demand: 16 thumbs_up, multiple comments, working solutions provided (Shiki Rehype, custom plugin). Feature closure was controversial. Recent comment (Zezombye, Apr 2025) provides working implementation. This is a legitimate feature gap.

**Action:** Implement or link plugin — high demand and feasible; consider Shiki's built-in inline highlight support or adopt community solution.

#### [#3746](https://github.com/vuejs/vitepress/issues/3746) How to custom the title of code block like docusaurus?

`P1` `actionable` `effort:M` · 👍 7 · 💬 0 · opened 2.2y ago, active 6d ago · labels: `stale`

Request for built-in code block title syntax (e.g. yaml [prom.yml] or yaml title="prom.yml") similar to Docusaurus, to label code blocks without custom components.

**Assessment:** Popular feature request (7 thumbs_up, no comments). Improves DX for documentation sites. Currently requires custom CodeBlock component workaround. Feature is well-defined and has precedent in other tools.

**Action:** Implement code block title syntax support; consider both bracket and attribute syntaxes for flexibility.

#### [#2410](https://github.com/vuejs/vitepress/issues/2410) Expose the built-in markdown parser to render markdown-valued variables with

`P2` `actionable` `effort:M` · 👍 7 · 💬 6 · opened 3.1y ago, active 2d ago · labels: `stale`

Feature request to expose the built-in markdown-it parser so users can render markdown-valued variables from frontmatter. Multiple use cases: CMS content rendering, API documentation generation with dynamic markdown snippets.

**Assessment:** Valid feature with demonstrated demand (7 thumbs up). Community found workaround (createMarkdownRenderer undocumented function). Recent activity shows ongoing interest. Maintainer has not directly commented on this request.

**Action:** Evaluate exposing createMarkdownRenderer or a simpler API; document current workaround prominently or provide official solution for 2.0.

#### [#3349](https://github.com/vuejs/vitepress/issues/3349) @include markdown and code files from an arbitrary public URL

`P2` `needs-info` `effort:L` · 👍 7 · 💬 5 · opened 2.5y ago, active 2d ago · labels: `stale`

Request to include markdown/code files from arbitrary public URLs using @include directive. Currently only supports local files.

**Assessment:** Valid use case for multi-repo documentation. 7 thumbs up. Maintainer (brc-dd) suggested git submodules + rewrites as workaround but no native support indicated. Multiple users express need (May-Jun 2024, Feb 2026).

**Action:** Decide on feasibility: remote URL includes introduce security/caching complexity. If proceeding, design cache strategy and URL validation. Otherwise, recommend submodules or alternative CI solutions.

#### [#3591](https://github.com/vuejs/vitepress/issues/3591) feat(containers-plugin): Being able to add custom containers

`P2` `actionable` `effort:M` · 👍 7 · 💬 2 · opened 2.4y ago, active 6d ago · labels: `stale`

Request ability to add custom containers beyond the default set (tip, warning, danger, etc.) via markdown config options.

**Assessment:** Valid feature (7 upvotes, 2 supportive comments) for extensible container system. Currently users must disable and re-register GFM plugin. Request dates to early 2024 with sustained interest.

**Action:** Implement—moderate demand, enables user customization, feasible by extending ContainerOptions interface.

#### [#2432](https://github.com/vuejs/vitepress/issues/2432) feat(demo): built-in markdown plugin provides support for showcasing demo capabilities.

`P2` `actionable` `effort:L` · 👍 3 · 💬 5 · opened 3.1y ago, active 2d ago · labels: `stale`

Feature request for built-in demo component support (similar to dumi) for component library documentation. Author created markdown-it-vitepress-demo plugin demonstrating solution.

**Assessment:** Valid feature for component library use case. Author provided working plugin with deployment and real-world examples (vue3-pixi). Maintainer engaged positively. Limited demand (3 thumbs up) but strong community interest in component documentation. Author actively maintains plugin.

**Action:** Consider recommending markdown-it-vitepress-demo in docs for component library authors, or evaluate integrating similar capability into core.

#### [#3823](https://github.com/vuejs/vitepress/issues/3823) Support custom attributes such as twoslash for snippets

`P2` `stale-candidate` `effort:M` · 👍 3 · 💬 0 · opened 2.2y ago, active 6d ago · labels: `stale`

Request to support custom attributes like twoslash in code snippet imports. User wants to append attributes such as `{ts:twoslash}` to `<<< .file` syntax to enable Shiki twoslash highlighting.

**Assessment:** Valid feature request but marked stale since April 2024. No comments. Low demand (3 thumbs up). Requires extending the snippet plugin to parse and forward custom attributes to the renderer.

**Action:** Verify if this is still relevant for 2.0.0-alpha. If demand increases or community provides PRs, consider implementing.

#### [#2660](https://github.com/vuejs/vitepress/issues/2660) ✨Feature Request: Allow wrapping code block by some special syntax like `:::js:wrap`

`P2` `needs-info` `effort:M` · 👍 1 · 💬 5 · opened 3.0y ago, active 2d ago · labels: `theme` `stale`

Request to add code block wrapping syntax (:::js:wrap, :::js:nowrap) and global configuration to enable CSS overflow wrapping on code blocks.

**Assessment:** Valid feature with clear use case (long query strings). Maintainer (brc-dd) noted incompatibility with line-number implementation and agreed wrap can be added standalone. Last comment (Jan 2025) bumped issue. No implementation yet.

**Action:** Implement wrap feature independently of line-numbers; coordinate with CSS strategy to avoid breaking existing styling.

#### [#3690](https://github.com/vuejs/vitepress/issues/3690) Multi region snippet support

`P2` `likely-answered` `effort:M` · 👍 1 · 💬 1 · opened 2.3y ago, active 6d ago · labels: `has-workaround` `stale`

Request for multi-region snippet support in code blocks, allowing inclusion of multiple disjoint regions from a source file (similar to Asciidoctor tagged regions).

**Assessment:** Valid feature request with a workaround provided. Maintainer brc-dd linked to markdown-it-vuepress-code-snippet-enhanced plugin that already supports this via @[code transcludeWith=#tag] syntax. Request is somewhat stale but has legitimate use case.

**Action:** Close: feature already available via community plugin; update docs to reference the plugin for multi-region support.

#### [#4853](https://github.com/vuejs/vitepress/issues/4853) Add `height` / `width` attribute to images to avoid layout shifts

`P2` `actionable` `effort:M` · 👍 1 · 💬 3 · opened 11mo ago, active 6d ago · labels: `build` `stale`

Feature request to auto-generate width/height attributes on images rendered from markdown to prevent layout shifts. Users can manually work around via markdown-it-image-size plugin.

**Assessment:** Valid enhancement; improves Core Web Vitals (CLS). Maintainer (brc-dd) provided working third-party plugin recommendation (markdown-it-image-size) as proof of concept. Requestor confirmed plugin works. Good candidate for default integration or built-in plugin.

**Action:** Consider integrating markdown-it-image-size into core or default plugins; or document as recommended plugin. Moderate effort, real user value.

#### [#4928](https://github.com/vuejs/vitepress/issues/4928) Titleless custom containers

`P2` `actionable` `effort:M` · 👍 1 · 💬 3 · opened 10mo ago, active 6d ago · labels: `theme` `stale`

Request ability to render custom containers without a title bar, e.g., `::: tip {no-title}`. User proposes syntax to skip the title div and adjust padding.

**Assessment:** Valid feature with clear use case (VitePress docs already have this scenario). brc-dd proposed `{.no-title}` syntax but user encountered Vue errors in v1.6.4. User opened #4932 with `{no-title}` attribute approach. Needs review of proposed implementation.

**Action:** Review and implement #4932 — small feature, medium effort, solves real use case in docs.

#### [#5222](https://github.com/vuejs/vitepress/issues/5222) Official Mermaid support

`P2` `actionable` `effort:L` · 👍 1 · 💬 2 · opened 15d ago, active 5d ago · unlabeled

Feature request for official built-in Mermaid diagram support in markdown fenced code blocks.

**Assessment:** Valid, well-scoped feature request. cschanhniem provided excellent technical analysis: markdown-it plugin approach is cleanest, SSR build-time rendering preferred for perf (client-side causes 20+ diagram re-parse overhead), bundle impact is ~800KB gzipped. Design decisions documented. Ready for prioritization.

**Action:** Accept as enhancement; consult cschanhniem's technical notes on pipeline fit, SSR strategy, and lazy-load approach before implementation.

#### [#4427](https://github.com/vuejs/vitepress/issues/4427) Add `note` to custom containers

`P3` `stale-candidate` `effort:S` · 👍 1 · 💬 3 · opened 1.5y ago, active 6d ago · labels: `stale`

Request to add `note` custom container type to default markdown plugin, similar to GitHub flavored alerts and Docusaurus admonitions.

**Assessment:** Low-value feature request. GitHub already supports `> [!NOTE]` syntax (recommended by VitePress docs). Adding `:::note` is minor. Marked stale. Only 1 thumbs_up, minimal interest.

**Action:** Close as stale—GitHub alerts are the standard; document that as alternative.

#### [#3436](https://github.com/vuejs/vitepress/issues/3436) Partially Copying Code Blocks

`P2` `likely-answered` `effort:M` · 👍 0 · 💬 5 · opened 2.5y ago, active 6d ago · labels: `stale`

Feature request to support partial copying from code blocks (copy only specific lines, excluding output/prompts).

**Assessment:** Valid use case with multiple contributors showing support. Community member found a working solution using shiki transformers with @shikijs/transformers and custom `copy-ignore` notation (as of Jan 2025). Fix is functional but requires manual config.

**Action:** Document the shiki transformer workaround in docs; consider integrating copy-ignore as built-in notation in future releases.

#### [#3842](https://github.com/vuejs/vitepress/issues/3842) 支持 Markdown 内的代码折叠功能

`P2` `stale-candidate` `effort:L` · 👍 0 · 💬 0 · opened 2.2y ago, active 6d ago · labels: `stale`

Request for code folding/collapse in markdown code blocks using `// [!code collapse:<lines>]` syntax. Chinese issue proposing collapsible code sections with default expand/collapse state.

**Assessment:** Valid feature but stale since April 2024. No comments or engagement. Requires custom markdown transformer and theme-level collapsible component implementation.

**Action:** Low priority; wait for more demand or community plugin solutions. Consider if aligns with VitePress 2.0 direction.

#### [#4243](https://github.com/vuejs/vitepress/issues/4243) Import code snippet from lines

`P2` `actionable` `effort:M` · 👍 0 · 💬 4 · opened 1.8y ago, active 6d ago · labels: `build` `stale`

Request to import code snippets by line range (e.g., lines 5-13) rather than only by regions, for files that don't support region markers.

**Assessment:** Valid request. brc-dd confirmed this was supported in older plugins (markdown-it-vuepress-code-snippet-enhanced) and not yet reimplemented in VitePress. Syntax design needed since `{1,2..}` is reserved for highlighting.

**Action:** Implement with clear syntax (e.g., `myfile#1-13{highlight-lines}`); coordinate syntax with existing highlighting to avoid conflicts.

#### [#4622](https://github.com/vuejs/vitepress/issues/4622) [FR] Frontmatter config to disable <> tags parsing

`P2` `likely-answered` `effort:S` · 👍 0 · 💬 9 · opened 1.3y ago, active 6d ago · labels: `build` `has-workaround` `stale`

Feature request to add frontmatter config to disable HTML tag parsing (e.g., `<partition>`) to avoid parsing errors in content like changelogs that use angle brackets.

**Assessment:** Valid use case but has multiple workarounds: (1) global config `markdown.html: false`, (2) manual HTML entity escaping (`&lt;partition&gt;`), (3) custom per-page markdown config via hook. brc-dd provided both a global solution and a per-page config workaround. Issue is answered but user expressed interest in frontmatter config for cleaner API.

**Action:** Consider for 2.0 roadmap: add frontmatter-level html parsing control (lower priority enhancement). For now: documented workarounds exist.

#### [#4838](https://github.com/vuejs/vitepress/issues/4838) export `processIncludes` method

`P2` `actionable` `effort:S` · 👍 0 · 💬 1 · opened 12mo ago, active 6d ago · labels: `stale`

Request to export internal `processIncludes` method from VitePress so users can programmatically process markdown includes (e.g., @include directives) in build plugins. Currently requires copying internal function.

**Assessment:** Valid API surface request. User already solved via manual copy of internal function. Small, non-breaking export. Alternative suggested: add processIncludes flag to createMarkdownRenderer. Both approaches are reasonable.

**Action:** Implement: export processIncludes from core API, or add flag to createMarkdownRenderer; either unblocks user build plugin and reduces copy-paste maintenance.

#### [#4947](https://github.com/vuejs/vitepress/issues/4947) Linking to src for code snippets

`P2` `stale-candidate` `effort:M` · 👍 0 · 💬 0 · opened 10mo ago, active 6d ago · labels: `stale`

Request to add filename/title display for code snippet imports and link them to source repository URLs, similar to 'edit on github' functionality.

**Assessment:** Valid feature request for improving snippet import usability. No maintainer feedback in thread. Marked stale, but the request is reasonable and well-articulated with no blockers.

**Action:** Reopen and prioritize if snippet import linking is planned for 2.0; otherwise keep closed until interest resurfaces.

#### [#3334](https://github.com/vuejs/vitepress/issues/3334) Import code snippets as dynamic path

`P3` `likely-answered` `effort:M` · 👍 0 · 💬 3 · opened 2.6y ago, active 2d ago · labels: `stale`

Request to support dynamic paths in code snippet imports using frontmatter variables. Example: <<< ./{{ $frontmatter.file }}

**Assessment:** Valid feature request. Author found working solution via custom md.renderer.rules.fence config and shared example. Shows achievable via existing VitePress extension hooks without core changes.

**Action:** Close: answered. Document pattern for dynamic code snippet imports in config examples/guides.

#### [#3347](https://github.com/vuejs/vitepress/issues/3347) [markdown-it] as a pluggable markdown parser

`P3` `likely-answered` `effort:L` · 👍 0 · 💬 1 · opened 2.5y ago, active 2d ago · labels: `stale`

Request to make markdown-it pluggable/replaceable. User wants to substitute markdown parser with server API that converts markdown to HTML.

**Assessment:** Valid use case. Maintainer (brc-dd) provided working solution: override md.render() in markdown config. No follow-up needed. Already documented in discussion.

**Action:** Close: answered. Link to maintainer solution in markdown config documentation.

#### [#3616](https://github.com/vuejs/vitepress/issues/3616) Support some shortcut commands such as `pm-install` to generate code-group conveniently！

`P3` `likely-answered` `effort:M` · 👍 0 · 💬 1 · opened 2.3y ago, active 6d ago · labels: `has-workaround` `stale`

Request shortcut markdown syntax (e.g., `:pm-install{name="cli-tool"}`) to auto-generate package manager code-groups instead of manually writing boilerplate.

**Assessment:** Convenience feature inspired by UnDoc's approach. One comment suggests minimal reply. Has workaround via custom components/plugins. Low demand (0 upvotes).

**Action:** Close: answered—can be implemented as user plugin; core doesn't need built-in shortcuts for this pattern.

#### [#4039](https://github.com/vuejs/vitepress/issues/4039) Optionally disable or override preWrapper for fence blocks

`P3` `likely-answered` `effort:S` · 👍 0 · 💬 1 · opened 2.0y ago, active 6d ago · labels: `stale`

Requests option to disable or override preWrapper for code fence blocks to remove button and language span wrapper.

**Assessment:** Answered by brc-dd with simple solution: hide wrapper elements using CSS inside custom components. User appears satisfied. Not requesting core change.

**Action:** Close: answered with workaround. CSS hiding approach is sufficient.

#### [#4214](https://github.com/vuejs/vitepress/issues/4214) [Feature request] Ability to use images in customer containers' custom title

`P3` `likely-answered` `effort:M` · 👍 0 · 💬 1 · opened 1.8y ago, active 6d ago · labels: `stale`

User requests ability to embed images in custom container titles (e.g., ::: tip ![icon](/tip.svg) TIP).

**Assessment:** Niche feature request (emojis typically sufficient). One reply suggests CSS mask workaround (pseudo-element ::before with mask-image). Marked stale. Low demand (0 thumbs up).

**Action:** Close: workaround provided—document CSS mask approach in theme customization guide or close as not-in-scope.

#### [#4386](https://github.com/vuejs/vitepress/issues/4386) 文档复用中的多链接场景需求。

`P3` `needs-info` `effort:L` · 👍 0 · 💬 4 · opened 1.6y ago, active 6d ago · labels: `stale`

Feature request (in Chinese) for multi-link reuse scenario: markdown file inclusion with variable links that differ per referencing document, similar to Vue slots.

**Assessment:** User wants to reuse markdown includes with context-dependent links. Suggested solution: placeholder tags with slot-like variable injection. Comment suggests using Vue components as workaround, which user found complex. Low priority — niche use case.

**Action:** Suggest Vue component approach; if demand emerges, consider as future enhancement.

#### [#4570](https://github.com/vuejs/vitepress/issues/4570) 支持动态加载 markdown文件的导出 <!--@include: ./parts/${name}.md-->

`P3` `likely-answered` `effort:L` · 👍 0 · 💬 1 · opened 1.4y ago, active 6d ago · labels: `stale`

Feature request for dynamic markdown file inclusion (e.g., `<!--@include: ./parts/${name}.md-->` with variables) to loop-load multiple files.

**Assessment:** Feature request for dynamic includes. Responder suggests workaround using dynamic routes feature and MDX-style approach. Not straightforward to implement as markdown processor doesn't have template syntax. Workaround exists.

**Action:** Close with link to dynamic routes and MDX patterns as solutions; document alternative patterns in guides.

## Default theme (81)

#### [#2954](https://github.com/vuejs/vitepress/issues/2954) Sync code group selection

`P1` `actionable` `effort:M` · 👍 14 · 💬 8 · opened 2.8y ago, active 2d ago · labels: `theme` `stale`

Request to add synchronized code group selection across multiple code blocks on a page, with persistence across navigation. Similar to Docusaurus tabs sync feature.

**Assessment:** High-demand feature (14 thumbs up, 8 comments, 9+ reactions). Multiple users confirm this is essential for multi-language documentation. User provided working implementation examples. Feature is well-scoped and maintainer response suggests openness to consideration.

**Action:** Implement code group sync mechanism with localStorage persistence; accept as core feature given strong community demand.

#### [#854](https://github.com/vuejs/vitepress/issues/854) Allow images to be zoomed in on click

`P2` `likely-answered` `effort:M` · 👍 10 · 💬 29 · opened 4.0y ago, active 2d ago · labels: `theme` `stale`

Request for image zoom-on-click feature (similar to medium-zoom in VuePress), especially for screenshots in docs.

**Assessment:** Valid request (10 thumbs_up). brc-dd provided immediate workaround (2022-06) with medium-zoom + stackblitz demo. Many comments show community plugins emerged (vitepress-plugin-image-viewer, vitepress-plugin-lightbox, vitepress-component-medium-zoom). One recent issue (2026-02): medium-zoom can cause Vue animation stuttering. Users happy with plugins; feature unlikely to be core priority.

**Action:** Close: solved by community plugins; update docs with examples and link to vitepress-plugin-lightbox or similar.

#### [#2257](https://github.com/vuejs/vitepress/issues/2257) Highlight active sidebar item when child page is loaded

`P1` `actionable` `effort:M` · 👍 9 · 💬 7 · opened 3.2y ago, active 2d ago · labels: `stale`

Request to highlight parent sidebar item when visiting a child/unlisted page; also auto-expand parent section.

**Assessment:** Valid UX feature with strong demand (11 reactions, 9 thumbs_up). Clear use case: child pages should make parent nav item active. Thread suggests activeMatch config as solution. Recent comments (Jan/Jul 2025) show continued interest.

**Action:** Add optional activeMatch pattern to sidebar config to support parent-active-on-child logic.

#### [#3263](https://github.com/vuejs/vitepress/issues/3263) Link previews

`P2` `needs-info` `effort:M` · 👍 9 · 💬 5 · opened 2.6y ago, active 2d ago · labels: `theme` `stale`

Request for link preview hover popups in VitePress using iframe or open graph protocol. Users want rich preview information when hovering over links.

**Assessment:** Valid feature request with moderate demand (9 thumbs up). Thread shows community interest, but no clear maintainer direction. One comment suggests markdown-it plugin approach as alternative.

**Action:** Clarify implementation strategy: built-in default theme feature vs. markdown-it plugin. Seek maintainer guidance on architecture preference.

#### [#2146](https://github.com/vuejs/vitepress/issues/2146) Auto expand/collapse sections in page outline

`P1` `actionable` `effort:M` · 👍 8 · 💬 2 · opened 3.3y ago, active 2d ago · labels: `theme` `stale`

Request for auto-expand/collapse of nested headings in the 'On this page' outline—expand child headings only when parent is active.

**Assessment:** Valid UX improvement with moderate demand (8 thumbs_up). Clear use case for deeply nested docs. Simple visual mockup provided. Recent +1 comment (Aug 2025) confirms ongoing interest.

**Action:** Implement smart outline expansion: collapse sibling trees when user navigates to a different heading section.

#### [#1838](https://github.com/vuejs/vitepress/issues/1838) blog layout

`P2` `actionable` `effort:L` · 👍 8 · 💬 8 · opened 3.4y ago, active 2d ago · labels: `build` `theme` `stale`

Request blog layout with features like tags, archives, and pagination for the default theme.

**Assessment:** Moderate demand (8 upvotes, 8 comments). Maintainer kiaking said too complex for core toward stable release; better as third-party theme addon. However, recent comment (Sep 2024) notes this is missing feature. Official blog.vuejs.org uses custom theme.

**Action:** Document recommended third-party blog themes; consider this for 2.x if architecture allows. Not priority for 1.x stable.

#### [#1037](https://github.com/vuejs/vitepress/issues/1037) Allow footer compatible with sidebar

`P2` `actionable` `effort:M` · 👍 7 · 💬 5 · opened 4.0y ago, active 2d ago · labels: `theme` `stale`

Allow footer and sidebar to be displayed together on the same page instead of being mutually exclusive.

**Assessment:** Valid design request with 7 upvotes. Maintainer brc-dd noted design concerns (doesn't look nice together) but suggested making it optional via frontmatter or theme config. High demand, ready to implement with proper design consideration.

**Action:** Implement as optional configuration for footer on doc pages; consider design implications for Windows/sidebar layout.

#### [#3534](https://github.com/vuejs/vitepress/issues/3534) breadcrumb

`P2` `likely-answered` `effort:M` · 👍 6 · 💬 9 · opened 2.4y ago, active 6d ago · labels: `theme` `has-workaround` `stale`

Request for built-in breadcrumb navigation component in the default theme. Multiple comments show workarounds using layout slots and transformPageData.

**Assessment:** Valid enhancement (6 upvotes) but maintainer brc-dd offered multiple solutions: layout slots, transformPageData for dynamic breadcrumbs. Other users posted example implementations. Issue is 28+ months old.

**Action:** Close: answered—multiple detailed workarounds provided; breadcrumb is considered theme-level extension rather than core.

#### [#2983](https://github.com/vuejs/vitepress/issues/2983) FR(theme): Add a separate page for search

`P2` `actionable` `effort:L` · 👍 4 · 💬 4 · opened 2.8y ago, active 2d ago · labels: `theme` `stale`

Request to add a dedicated search results page (similar to Docusaurus `/search` page) to complement Algolia modal search.

**Assessment:** Valid feature request with moderate demand (4 thumbs up). Thread shows related discussion about Algolia result limits. Maintainer acknowledged feasibility but no commitment. Would benefit documentation and improve search UX for large doc sites.

**Action:** Scope dedicated search page feature for future enhancement; consider as theme extension point.

#### [#4217](https://github.com/vuejs/vitepress/issues/4217) Adjust the width of the table

`P2` `actionable` `effort:S` · 👍 4 · 💬 1 · opened 1.8y ago, active 6d ago · labels: `theme` `stale`

User requests tables to auto-expand to full container width (like Typora) instead of shrinking to fit content.

**Assessment:** Clear UX feature request with reasonable motivation (Typora-like layout). Has 4 thumbs up and recent comment (2026-04) asking for progress, indicating sustained demand. Marked stale. CSS-level fix likely simple.

**Action:** Implement—add CSS rule to make tables width: 100% by default in theme; consider adding themeConfig option to toggle.

#### [#4521](https://github.com/vuejs/vitepress/issues/4521) Scroll to top button on desktop

`P2` `actionable` `effort:M` · 👍 4 · 💬 3 · opened 1.4y ago, active 6d ago · labels: `stale`

Feature request for a scroll-to-top button on desktop, to quickly navigate long pages back to the top. Has 4 thumbs up and user interest in comments.

**Assessment:** Valid feature request with modest user interest (4 reactions). Standard UX improvement. Would require theme modification. Not critical but reasonable enhancement.

**Action:** Consider as backlog feature for default theme; could be made optional via config.

#### [#4345](https://github.com/vuejs/vitepress/issues/4345) Auto-anchor the sidebar to the openned page

`P1` `actionable` `effort:M` · 👍 3 · 💬 1 · opened 1.7y ago, active 6d ago · labels: `stale`

Sidebar should auto-scroll to reveal the currently active page when navigating. Optional: highlight active item with blink animation and persistent dimmed background (like rustdoc).

**Assessment:** Clear feature request with strong UX reasoning. Links to rustdoc example. User provided working implementation in comments. 3+ thumbs up. Community-provided workaround shows it's implementable. High value: improves discoverability in long sidebars. Duplicate of #4296 in spirit but this is more focused on sidebar scrolling.

**Action:** Implement sidebar auto-scroll to active item; consider highlight animation as follow-up. Can reference user's implementation in comments.

#### [#5013](https://github.com/vuejs/vitepress/issues/5013) PR made: feat: Group name for Code Groups to sync opened tab

`P1` `actionable` `effort:M` · 👍 3 · 💬 2 · opened 8mo ago, active 6d ago · labels: `stale`

Feature request and associated PR (#5012) to add group name synchronization for code groups: when a user selects a tab in one code group with the same name, all code groups with that name automatically switch to the same tab, with preference persisted in localStorage.

**Assessment:** Well-motivated feature (3 thumbs_up) with clear use case (avoid repetitive tab clicking for package managers, languages, etc.). PR already submitted (#5012). Receives positive feedback in thread (cfreear, souljorje). Non-breaking change. Ready for review and merge.

**Action:** Review PR #5012 and merge if tests pass - well-designed feature addressing real UX pain point

#### [#4734](https://github.com/vuejs/vitepress/issues/4734) TOC can scroll as the page scrolls

`P2` `stale-candidate` `effort:M` · 👍 3 · 💬 1 · opened 1.2y ago, active 6d ago · labels: `stale`

Request to make table of contents scroll with page content so all items remain visible. User provides design mockup showing desired behavior.

**Assessment:** Valid feature request with one supportive comment. Marked stale after 13 months with no maintainer engagement. Low demand (3 thumbs up) but straightforward to implement.

**Action:** Review demand; if low priority consider closing as stale, otherwise prioritize for nice-to-have enhancement list.

#### [#3107](https://github.com/vuejs/vitepress/issues/3107) Notes Group

`P2` `likely-answered` `effort:M` · 👍 2 · 💬 4 · opened 2.7y ago, active 2d ago · labels: `build` `stale`

Request for 'notes group' component similar to code-group but for notes/callouts with optional code blocks inside.

**Assessment:** Clear feature request. brc-dd provided multiple workarounds: code-group with custom HTML blocks, suggested simple blocks syntax, and referenced vitepress-plugins tabs. Community has solutions; feature not critical.

**Action:** Close or defer — maintainer suggested syntax and alternative plugins available; users can implement via custom theme.

#### [#3153](https://github.com/vuejs/vitepress/issues/3153) Hiding the excerpt from the content page

`P2` `actionable` `effort:M` · 👍 2 · 💬 7 · opened 2.7y ago, active 2d ago · labels: `build` `stale`

Request to hide excerpts from content pages while keeping them accessible for blog index pages, with a frontmatter option like 'hideExcerpt: true'.

**Assessment:** Valid feature with moderate demand (2 thumbs_up). Thread discusses multiple API options (frontmatter flag, component, theme config). One user found a workaround using `renderExcerpt: false` config though it doesn't work as expected. Recent user solution via contentLoader transform is available but hacky.

**Action:** Implement excerpt hiding via frontmatter or markdown.frontmatter.renderExcerpt; clarify and test the existing renderExcerpt config option to ensure it works.

#### [#3241](https://github.com/vuejs/vitepress/issues/3241) Change Prev / Next Text globally

`P2` `actionable` `effort:M` · 👍 2 · 💬 0 · opened 2.6y ago, active 2d ago · labels: `theme` `stale`

Feature request: allow global configuration of prev/next link text (e.g., '<' / '>') without editing frontmatter on every page, with per-page overrides.

**Assessment:** Valid enhancement. User wants theme config option for docFooter link text patterns. Current workaround is frontmatter on each file. No maintainer feedback yet. Reasonable scope for config.

**Action:** Extend docFooter config to support text templates or patterns; allow frontmatter to override.

#### [#3783](https://github.com/vuejs/vitepress/issues/3783) [default theme] Icons on action buttons

`P2` `actionable` `effort:S` · 👍 2 · 💬 0 · opened 2.2y ago, active 6d ago · labels: `theme` `stale`

Request to add icon support for home page action buttons, allowing SVG or image icons alongside text.

**Assessment:** Valid feature request with 2 thumbs up. No maintainer response but clean, well-specified request. Low effort (UI config addition + theme rendering).

**Action:** Implement icon support for action buttons; add to VPButton props and default theme rendering.

#### [#3816](https://github.com/vuejs/vitepress/issues/3816) Support multi-level navbar

`P2` `actionable` `effort:L` · 👍 2 · 💬 4 · opened 2.2y ago, active 6d ago · labels: `theme` `stale`

Request multi-level nested navbar with collapsible dropdown groups, currently navbar only supports single level.

**Assessment:** Valid feature request with 2 thumbs up. kiaking engaged, noting design concerns about nested dialogs. Suggested alternative: panel-switching inside dialog. No clear decision yet. User asked for status update 2025-01-02.

**Action:** Clarify design direction with maintainers; if proceeding, design and implement nested nav with panel-based UX.

#### [#3928](https://github.com/vuejs/vitepress/issues/3928) Add `important` to custom containers

`P2` `actionable` `effort:S` · 👍 2 · 💬 1 · opened 2.1y ago, active 6d ago · labels: `build` `stale`

Requests adding 'important' custom container type to match GitHub-flavored Alerts' 5-color palette. VitePress currently has only 4 types.

**Assessment:** Valid feature request (2 thumbs_up). rvanhorn provided workaround using markdown-it-container plugin showing demand exists. Simple addition to match GitHub-flavored syntax.

**Action:** Implement: add 'important' container type with appropriate color scheme and styling.

#### [#4296](https://github.com/vuejs/vitepress/issues/4296) Opening or navigating to sidebar links should focus/scroll to the sidebar item

`P2` `actionable` `effort:M` · 👍 2 · 💬 3 · opened 1.7y ago, active 6d ago · labels: `bug: pending triage` `stale`

Sidebar should auto-scroll to highlight the currently active page when navigating. Similar to VSCode's explorer.autoReveal, this helps users locate their position in long sidebars.

**Assessment:** Valid feature request with clear use case and examples. Multiple +1s from users. No maintainer response on merits. Stale label suggests low current priority but the request is legitimate and has community support.

**Action:** Review for feasibility; if aligned with UX goals, implement with option to toggle.

#### [#4424](https://github.com/vuejs/vitepress/issues/4424) Increase the adjustment of the Markdown content width to fill the entire page for better reading on desktop PCs.

`P2` `stale-candidate` `effort:M` · 👍 2 · 💬 0 · opened 1.6y ago, active 6d ago · labels: `stale`

Request to increase Markdown content width on desktop to fill more page space, especially for code blocks and tables. Points to third-party plugin as example of demand.

**Assessment:** Valid feature request with legitimate use case (technical docs benefit from wider code blocks). However, labeled 'stale' and no recent maintainer engagement. May conflict with readability best practices that default theme follows. Low engagement (2 thumbs_up).

**Action:** Close as stale—consider reopening only if demand increases significantly or as part of v2 theme overhaul discussion.

#### [#2843](https://github.com/vuejs/vitepress/issues/2843) Add page transition from VuePress 2

`P3` `stale-candidate` `effort:M` · 👍 2 · 💬 1 · opened 2.9y ago, active 2d ago · labels: `stale`

Request to port page transition animation from VuePress 2 to VitePress. User wants fade/slide effect when navigating between pages.

**Assessment:** Nice-to-have feature request with minimal demand (2 thumbs_up). No detail provided on animation style or performance concerns. Low priority cosmetic enhancement. Would need CSS animation implementation or Vue transition component.

**Action:** Consider for theme enhancements backlog. Low priority; defer unless community demand increases.

#### [#3021](https://github.com/vuejs/vitepress/issues/3021) Make the default theme compatible with 3rd-party CSS frameworks

`P1` `actionable` `effort:L` · 👍 1 · 💬 0 · opened 2.8y ago, active 2d ago · labels: `stale`

Request for default theme compatibility with 3rd-party CSS frameworks via entry points and renamed component classes.

**Assessment:** Valid feature request with strong use case (Spectre CSS docs migration) and concrete proposal. Author committed to submitting PR. Shows working proof-of-concept already. High-value enhancement but requires careful design to avoid breaking changes.

**Action:** Plan for PR review — author committed to submitting changes to make theme namespace-aware and reduce CSS conflicts.

#### [#2881](https://github.com/vuejs/vitepress/issues/2881) an option to auto-collapse sidebar group + scroll sidebar item into view

`P2` `actionable` `effort:M` · 👍 1 · 💬 0 · opened 2.8y ago, active 2d ago · labels: `theme` `stale`

Request for option to auto-collapse sidebar groups and scroll active sidebar item into view. Related to #2879.

**Assessment:** Valid UX feature. Addresses common sidebar usability issues: preventing sidebar groups from growing too large and keeping active item visible. Low engagement (1 thumbs_up) but straightforward to implement.

**Action:** Implement sidebar config option for auto-collapse behavior and scroll-into-view for active item.

#### [#2989](https://github.com/vuejs/vitepress/issues/2989) Allow link on Nav item with children items

`P2` `actionable` `effort:M` · 👍 1 · 💬 5 · opened 2.8y ago, active 2d ago · labels: `theme` `stale`

Request to allow navbar items to have both a link and a dropdown menu with children items, mirroring existing sidebar functionality.

**Assessment:** Valid UX enhancement request. Maintainer (brc-dd) raised valid keyboard navigation concerns (focus handling, arrow keys, esc key) but issue remains open. Recent comment (2025-03-12) reiterates usability need and provides real-world example (SAP CAP docs). Would improve navbar flexibility.

**Action:** Design and implement navbar items with dual link+dropdown capability; resolve keyboard navigation interaction model.

#### [#3848](https://github.com/vuejs/vitepress/issues/3848) Support author zoom in doc layout

`P2` `stale-candidate` `effort:M` · 👍 1 · 💬 0 · opened 2.2y ago, active 6d ago · labels: `stale`

Request to add author metadata field to the doc layout component, allowing per-page author configuration via frontmatter. User mentions it is already custom-implemented.

**Assessment:** Valid enhancement; 1 thumbs up. Stale since April 2024, no maintainer input. User confirms custom workaround exists. Low priority unless strong community demand.

**Action:** Collect usage signals or feature votes before implementing. Consider creating a composable/hook for theme authors.

#### [#3891](https://github.com/vuejs/vitepress/issues/3891) Solid borders for custom containers

`P2` `actionable` `effort:S` · 👍 1 · 💬 0 · opened 2.1y ago, active 6d ago · labels: `theme` `needs more discussion` `stale`

Requests adding solid left border and faint drop shadow to custom containers in default theme to improve visual hierarchy and polish.

**Assessment:** Valid feature request with clear visual mockup showing desired styling. Author offers to contribute CSS changes. Limited uptake (1 thumbs_up). Marked stale but straightforward CSS enhancement.

**Action:** Review design, accept if theme maintainers approve the visual change, have author prepare PR.

#### [#4141](https://github.com/vuejs/vitepress/issues/4141) Allow SVG logo to be inlined

`P2` `actionable` `effort:M` · 👍 1 · 💬 1 · opened 1.9y ago, active 6d ago · labels: `stale`

Allow SVG logos to be inlined in HTML instead of using img tag, to enable CSS-based dark/light mode switching within the SVG.

**Assessment:** Valid feature request with clear use case: user wants SVG with embedded media query styles for theme switching. Currently img tag isolation prevents this. mrlubos confirmed in Aug 2025 that this is not stale.

**Action:** Implement SVG inlining option for logo config, likely as a theme option.

#### [#4413](https://github.com/vuejs/vitepress/issues/4413) Theme switcher should be more clear and obvious

`P2` `actionable` `effort:M` · 👍 1 · 💬 2 · opened 1.6y ago, active 6d ago · labels: `stale`

Theme switcher UI should explicitly show all three options (Light / Auto / Dark) instead of cycling buttons, and clarify behavior for switching back to auto mode.

**Assessment:** Valid UX enhancement. User points out confusing affordance: no obvious way to return to auto after manual selection. References better UX from ESLint and MDN sites. Comments show other users want this (mrlubos calls it critical).

**Action:** Redesign theme switcher to show all 3 options clearly; consider dropdown for space efficiency; add to 2.0 theme improvements.

#### [#2613](https://github.com/vuejs/vitepress/issues/2613) [Default Theme] VPHero.Actions + VPButton support boolean attribute for download link

`P3` `stale-candidate` `effort:S` · 👍 1 · 💬 1 · opened 3.0y ago, active 2d ago · labels: `theme` `stale`

Request to add download attribute support to VPButton and VPHero components in the default theme to enable download link functionality.

**Assessment:** Valid feature request with code samples provided. Created July 2023, marked stale. Only 1 thumbs-up reaction suggests limited demand. Thread has no maintainer response or decision.

**Action:** Verify if still relevant for 2.0.0-alpha; if accepted, implement with minimal effort as straightforward prop addition.

#### [#3130](https://github.com/vuejs/vitepress/issues/3130) Automatic definition tooltips

`P3` `stale-candidate` `effort:M` · 👍 1 · 💬 2 · opened 2.7y ago, active 2d ago · labels: `stale`

Request for automatic definition tooltips using a glossary system similar to Material for MkDocs, allowing terms to be hoverable across the entire project.

**Assessment:** Valid feature request with low demand (1 thumbs_up, 2 comments expressing interest). Marked stale but still represents a reasonable enhancement for documentation sites. No concrete API design yet.

**Action:** Clarify if this is a priority feature request or defer to community plugins. Consider if markdown-it tooltip plugins exist that could address the use case.

#### [#3697](https://github.com/vuejs/vitepress/issues/3697) 关于多侧边栏的配置与使用

`P3` `stale-candidate` `effort:M` · 👍 1 · 💬 0 · opened 2.3y ago, active 6d ago · labels: `stale`

Request for flexible sidebar routing: ability to use frontmatter to assign articles to sidebars instead of relying on directory structure, enabling flexible sidebar management.

**Assessment:** Feature request in Chinese with no responses or maintainer engagement. References related issue #1297. Legitimate use case but unclear priority. Issue is old (March 2024) and marked stale.

**Action:** Investigate if #1297 is duplicate and consolidate; otherwise ask author to clarify current status and priority.

#### [#4820](https://github.com/vuejs/vitepress/issues/4820) Created by and Last modified by

`P3` `needs-info` `effort:M` · 👍 1 · 💬 0 · opened 1.0y ago, active 6d ago · labels: `stale`

Feature request to display author/committer name alongside last-modified timestamp, parsed from git history.

**Assessment:** Straightforward feature with one thumb. No maintainer engagement or technical discussion. Marked stale. Useful but low priority for theme; git metadata availability during build is known issue.

**Action:** Close or defer: determine if git metadata is reliably available during build; clarify scope (show all co-authors, most recent author, etc.).

#### [#2071](https://github.com/vuejs/vitepress/issues/2071) Add support of Global Notification

`P1` `actionable` `effort:M` · 👍 0 · 💬 5 · opened 3.3y ago, active 2d ago · labels: `docs` `stale`

Request for built-in global notification/announcement banner at top of page with dismissal capability.

**Assessment:** Valid and recurring demand (5 comments, recent +1 from Feb 2025). Maintainer documented workaround using layout-top slot, but users report it requires non-trivial SSR boilerplate, dynamic content is hard, and layout shift issues. Strong community pushback indicates real need for built-in support.

**Action:** Implement built-in announcement banner component with configurable content, dismiss state persistence, and SSR-safe layout handling.

#### [#1054](https://github.com/vuejs/vitepress/issues/1054) Could it be better to adjust width of some html element ?

`P2` `actionable` `effort:M` · 👍 0 · 💬 8 · opened 3.9y ago, active 2d ago · labels: `theme` `stale`

Layout shift caused by scrollbar appearing/disappearing when navigating between pages with different content heights on Windows systems.

**Assessment:** Valid UX issue. Maintainer kiaking agreed to fix (especially for search modal), citing that other sites and libraries handle this well. PR #1844 was mentioned with working fix. Needs implementation decision on CSS approach.

**Action:** Implement scrollbar-gutter or overflow-y: overlay solution; verify fix works for modal opening without affecting overlay appearance.

#### [#2334](https://github.com/vuejs/vitepress/issues/2334) Using slot doc-top messes up active heading determination in aside

`P2` `actionable` `effort:M` · 👍 0 · 💬 0 · opened 3.2y ago, active 2d ago · labels: `theme` `stale`

Using doc-top slot breaks active heading detection in outline because hard-coded __PAGE_OFFSET__ doesn't account for slot content height.

**Assessment:** Valid but specific: using doc-top slot adds height that throws off heading offset calculation. Requesting either dynamic offset detection or config option to override hard-coded value. No maintainer response yet.

**Action:** Use IntersectionObserver to detect active heading dynamically instead of hard-coded offset; or expose offset as config option.

#### [#2831](https://github.com/vuejs/vitepress/issues/2831) Whether can add a slot in VPnavbar

`P2` `actionable` `effort:S` · 👍 0 · 💬 1 · opened 2.9y ago, active 2d ago · labels: `theme` `contribution welcome` `stale`

Request to add a navbar slot after the search component. User wants to insert custom elements between search and other navbar items without affecting search display.

**Assessment:** Valid request with weak but real demand (0 thumbs_up in original, +1 from follow-up comment). Low complexity to implement. Request for 'search-after' slot is reasonable extensibility gap in navbar customization.

**Action:** Add search-after slot to VPNavBar component. Low-effort improvement for navbar customization.

#### [#3245](https://github.com/vuejs/vitepress/issues/3245) use https://vitepress-plugins.sapphi.red/tabs/

`P2` `actionable` `effort:M` · 👍 0 · 💬 5 · opened 2.6y ago, active 2d ago · labels: `stale`

Feature request: extend code-group tabs to support synchronized switching across multiple tabs on a page (like Android Developer docs).

**Assessment:** User wants code-group to support global state (select one language, all tabs update). Thread shows user explored plugin alternatives but wants core support. Maintainer (brc-dd) referenced external plugin. Clear demand but may be better solved as a plugin or config option.

**Action:** Either implement tab-sync feature in code-group or document official plugin approach and style guidance for consistency.

#### [#3433](https://github.com/vuejs/vitepress/issues/3433) Use `color-mix` instead of multiple color CSS vars

`P2` `stale-candidate` `effort:M` · 👍 0 · 💬 1 · opened 2.5y ago, active 6d ago · labels: `stale`

Feature request to use CSS color-mix() function instead of multiple color variables for simpler theme customization.

**Assessment:** Valid technical request with good motivation. Maintainer blocked it due to browser support constraints (Safari <14 not supported by color-mix). Issue correctly stays stale until minimum browser targets are raised.

**Action:** Keep stale pending browser support targets update; revisit when VitePress raises Safari minimum to >=15.

#### [#3441](https://github.com/vuejs/vitepress/issues/3441) How to make multiple sidebars expand only one at a time

`P2` `stale-candidate` `effort:M` · 👍 0 · 💬 1 · opened 2.5y ago, active 6d ago · labels: `theme` `stale`

Feature request to make multiple sidebars expand mutually exclusive (only one open at a time).

**Assessment:** Valid feature request. Maintainer comment from Aug 2024 correctly notes it should not have been staled. Reasonable use case for accordion-style sidebar behavior.

**Action:** Unstale and add to backlog; reasonable UX improvement for multi-sidebar setups.

#### [#3704](https://github.com/vuejs/vitepress/issues/3704) Multiple Sidebars auto produce/关于侧边栏的自动生成建议

`P2` `stale-candidate` `effort:L` · 👍 0 · 💬 3 · opened 2.3y ago, active 6d ago · labels: `stale`

Request for auto-generated sidebar from SUMMARY.md file (like gitbook/mdbook) instead of requiring full sidebar config in themeConfig. Author seeks reduced config complexity and per-book sidebar control.

**Assessment:** Feature request in Chinese with moderate interest (3 comments including +1). Issue references #1297, suggesting potential duplicate. Topic is desirable but architectural decision against auto-generation from files. Marked stale.

**Action:** Check #1297 for duplicate; if distinct, clarify why manual config approach is preferred over file-based generation.

#### [#3769](https://github.com/vuejs/vitepress/issues/3769) Support centering members in VPTeamPage

`P2` `likely-answered` `effort:M` · 👍 0 · 💬 4 · opened 2.2y ago, active 6d ago · labels: `theme` `stale`

Request to center VPTeamMembers component when displaying a single team member.

**Assessment:** User misunderstood the theme behavior as a breaking change. brc-dd clarified it was never centered and requires flex rewrite (currently uses grid). Not a bug, valid feature request with low demand (no additional thumbs up).

**Action:** Tag as feature request; consider for future theme improvements if centering becomes common user request.

#### [#3828](https://github.com/vuejs/vitepress/issues/3828) add an API to support SVG's dynamic logo

`P2` `stale-candidate` `effort:M` · 👍 0 · 💬 1 · opened 2.2y ago, active 6d ago · labels: `theme` `stale`

Request to support dynamic SVG logos in the theme instead of static img elements. User wants logos to preserve SVG interactivity and animation.

**Assessment:** Valid theme enhancement; one comment agreeing with the need. No maintainer feedback. Requires modifying the logo rendering logic to accept Vue components or SVG content.

**Action:** Check if 2.0.0-alpha has a better component-based logo system. Otherwise, collect more votes to prioritize.

#### [#4048](https://github.com/vuejs/vitepress/issues/4048) Add `#sidebar-nav-active-link-after` slot + expose `VPDocAside`

`P2` `actionable` `effort:M` · 👍 0 · 💬 3 · opened 2.0y ago, active 6d ago · labels: `stale`

Requests new slot `#sidebar-nav-active-link-after` and public export of `VPDocAside` component to allow placing table-of-contents inside sidebar.

**Assessment:** Clear feature request with design mockups and working custom solution provided by author. Author willing to contribute PR. ghostrider-05 suggested `aside: 'left'` but that is separate layout, not integrated as shown.

**Action:** Review design, accept PR from author that adds slot and exports component.

#### [#4061](https://github.com/vuejs/vitepress/issues/4061) Page based navbars (MultiNavbar)

`P2` `actionable` `effort:M` · 👍 0 · 💬 0 · opened 2.0y ago, active 6d ago · labels: `theme` `stale`

Request for page-based multiple navbars (MultiNavbar) in the default theme, analogous to the existing MultiSidebar feature.

**Assessment:** Valid feature request for theme flexibility. The concept is clear and follows existing MultiSidebar pattern. No maintainer comments yet. Reasonable scope for enhancement.

**Action:** Implement MultiNavbar support in default theme following MultiSidebar precedent.

#### [#4105](https://github.com/vuejs/vitepress/issues/4105) Need to customize 404 page title

`P2` `likely-answered` `effort:S` · 👍 0 · 💬 3 · opened 1.9y ago, active 6d ago · labels: `client` `stale`

Request to customize 404 page title format beyond the default "404 \| siteTitle".

**Assessment:** Feature request with partial solution provided. Maintainer brc-dd suggested using transformPageData to modify pageData.title. User later found workaround by modifying page data in Vue script. Solution exists but could be more discoverable.

**Action:** Close: answered; document transformPageData approach for 404 title customization in theme docs.

#### [#4114](https://github.com/vuejs/vitepress/issues/4114) Sidebar's base need extends parent's base

`P2` `needs-info` `effort:M` · 👍 0 · 💬 3 · opened 1.9y ago, active 6d ago · labels: `stale`

Sidebar child items should inherit parent base path when specifying their own base, to avoid path repetition in configuration.

**Assessment:** Valid feature request but brc-dd raised valid concern: allowing base inheritance would remove ability to exclude parent base in some items. Needs clarification on how this would work without breaking existing use cases. User found workaround using symlinks.

**Action:** Request clarification on how to handle items that need to break from parent base inheritance pattern.

#### [#4211](https://github.com/vuejs/vitepress/issues/4211) [Feature Proposal] Expand only active sidebar group

`P2` `actionable` `effort:M` · 👍 0 · 💬 0 · opened 1.8y ago, active 6d ago · labels: `stale`

Feature proposal to automatically expand only the active sidebar group and collapse others, with optional per-group toggle behavior to bypass auto-collapse.

**Assessment:** Clear, well-scoped feature request with real-world motivation (e.g., Spectre docs, iles framework). Author offers to contribute. No maintainer feedback. Marked stale but proposal is coherent and has implementation path (sidebar composable).

**Action:** Review for feasibility—sketch API design, clarify interaction model (toggle vs. navigation), estimate effort, consider accepting a PR.

#### [#4232](https://github.com/vuejs/vitepress/issues/4232) Make Nav URL customisable for home page and subpages

`P2` `possibly-fixed` `effort:S` · 👍 0 · 💬 13 · opened 1.8y ago, active 6d ago · labels: `docs` `stale`

Request for customizable nav URLs that differ between home page and subpages to enable context-aware navigation.

**Assessment:** Valid request with repro video. brc-dd provided `logoLink` workaround initially. Later comments (Sep-Oct 2025) suggest the issue was resolved by #4963, allowing dynamic nav functions. its-miroma confirmed implementation with link functions accepting relativePath.

**Action:** Verify against current main; likely close as fixed by #4963 which enables link as function.

#### [#4330](https://github.com/vuejs/vitepress/issues/4330) Open sidebar with touch navigation (swipe) [default-theme] (bug in useSidebar?)

`P2` `needs-info` `effort:M` · 👍 0 · 💬 2 · opened 1.7y ago, active 6d ago · labels: `stale`

Request to add touch swipe gesture to open/close sidebar on mobile, similar to VuePress. Also requests clarification on useSidebar() API which appears to create separate sidebar state.

**Assessment:** Valid feature request (VuePress has this). User provided detailed attempt showing useSidebar() appears to be isolated from Layout's sidebar state. Another user found DOM-based workaround (clicking button/backdrop). Needs design decision: should useSidebar be connected to Layout state or should Layout expose sidebar controls?

**Action:** Design touch gesture feature and clarify useSidebar API exposure; consider adding sidebar control methods to theme API.

#### [#4637](https://github.com/vuejs/vitepress/issues/4637) Sidenav Components / Skip title update

`P2` `needs-info` `effort:L` · 👍 0 · 💬 0 · opened 1.3y ago, active 6d ago · labels: `stale`

Feature request to allow custom components in sidenav/title rendering and a flag to skip title update logic, enabling custom Vue template compilation in navigation and metadata.

**Assessment:** Feature request linked to PR #4630. User wants to customize how page titles (with Vue templates) are rendered in sidenav and search paths. Related to issue #4640. No comments; depends on PR review.

**Action:** Await PR review of #4630 — assess scope and decide on design before committing.

#### [#4751](https://github.com/vuejs/vitepress/issues/4751) [Suggestion] Skip deleted lines from line numbering in code blocks

`P2` `possibly-fixed` `effort:S` · 👍 0 · 💬 3 · opened 1.1y ago, active 6d ago · labels: `stale`

Request to skip deleted code lines (marked with `// [!code --]`) from line numbering, or provide option to do so.

**Assessment:** Valid UX improvement. Maintainer brc-dd responded with GitHub comparison and later commented 'fixed the copying issue', suggesting related fix may address this.

**Action:** Verify fix status; if incomplete, implement line numbering skip for deleted lines per GitHub behavior.

#### [#4867](https://github.com/vuejs/vitepress/issues/4867) Auto‑insert `<a.header‑anchor>` for headings rendered by Vue components

`P2` `actionable` `effort:M` · 👍 0 · 💬 4 · opened 11mo ago, active 6d ago · labels: `client` `stale`

Headings rendered by Vue components get IDs and appear in outline but lack the invisible header-anchor <a> element, making them inconsistent with markdown headings and preventing easy permalink copying.

**Assessment:** Valid UX/consistency issue with clear repro. Maintainer (brc-dd) rejected client-side injection due to hydration concerns. Suggested creating a reusable heading component instead. User then proposed v-header-anchor directive, but maintainer noted directive can't access slot text for aria-label. Component solution is best path forward.

**Action:** Create and export a built-in VitePress heading component (e.g., <VPHeading>) that auto-injects header-anchor; or document component pattern in guide.

#### [#4920](https://github.com/vuejs/vitepress/issues/4920) Use CSS Modules in default VitePress theme for more flexible class scoping

`P2` `needs-info` `effort:L` · 👍 0 · 💬 5 · opened 10mo ago, active 6d ago · labels: `needs more discussion` `stale`

Propose migrating default theme from Vue scoped styles to CSS Modules to enable class name obfuscation, defending against aggressive adblock filters.

**Assessment:** Valid concern about predictable `.VPDocAside` selectors being targeted by adblockers. brc-dd noted maintainability tradeoffs: CSS Modules don't change selector names across deploys unless component names change. Suggested filing upstream issues with adblock maintainers. Needs broader discussion on benefit vs. refactoring cost.

**Action:** Needs discussion — breaking change with unclear benefit. Consider addressing root cause with adblocker maintainers first (brc-dd filed easylist#22226).

#### [#4953](https://github.com/vuejs/vitepress/issues/4953) Improve portrait screen support in default theme

`P2` `actionable` `effort:M` · 👍 0 · 💬 0 · opened 9mo ago, active 6d ago · labels: `stale`

Feature request to improve portrait screen support by reducing horizontal margins/padding so the 'On this page' sidebar can be shown on 1200px+ portrait screens instead of requiring 1280px+.

**Assessment:** Valid UX improvement with concrete suggestions (reduce padding by 80px). User provided before/after screenshots and specific CSS changes. No maintainer feedback. Stale label but issue is well-reasoned.

**Action:** Reopen and implement—reasonable change with measurable impact on responsive design; user provided detailed CSS guidance.

#### [#5069](https://github.com/vuejs/vitepress/issues/5069) 请求将屏幕最小宽度的判断从 1280px 改成 1200 px

`P2` `actionable` `effort:S` · 👍 0 · 💬 2 · opened 6mo ago, active 6d ago · labels: `theme` `has-workaround` `stale`

Default theme requires 1280px minimum width; many portable screens are 1200px wide, causing article summaries to be hidden. Requests 1200px threshold or config option.

**Assessment:** Valid issue with clear repro. Maintainer provided Vite plugin workaround. Could be made configurable in theme config or adjusted with minor breakpoint change.

**Action:** Consider making responsive breakpoint configurable in theme config or adjust 1280px default to 1200px; document plugin workaround in interim.

#### [#5210](https://github.com/vuejs/vitepress/issues/5210) Centered layout option for Home Hero section (Desktop)

`P2` `actionable` `effort:M` · 👍 0 · 💬 0 · opened 27d ago, active 17d ago · unlabeled

Requests centered layout option for home hero section via new frontmatter property (e.g., hero.align: 'center').

**Assessment:** Well-scoped feature request with clear motivation (classic centered landing pages), detailed PoC CSS, and before/after screenshots. Currently requires custom CSS override. Small, focused scope. Aligns with theme customization goals.

**Action:** Implement align property in hero config; add centered layout variant to VPHomeHero component.

#### [#5221](https://github.com/vuejs/vitepress/issues/5221) Navigate to next / previous page with arrow keys

`P2` `likely-answered` `effort:M` · 👍 0 · 💬 3 · opened 15d ago, active 7d ago · unlabeled

Feature request for arrow key navigation (left arrow = previous page, right arrow = next page) similar to mdbook for efficient documentation skimming.

**Assessment:** Valid feature request. Community member (its-miroma) provided working code solution that hooks keyboard events and clicks existing prev/next links. Feature is implementable without core changes via theme customization, reducing urgency for built-in support.

**Action:** Close or mark as future enhancement; solution available in default theme via custom hook, reducing need for core implementation.

#### [#1138](https://github.com/vuejs/vitepress/issues/1138) Add a progressbar on top when performing navigation, like VuePress.

`P3` `actionable` `effort:M` · 👍 0 · 💬 1 · opened 3.9y ago, active 2d ago · labels: `theme` `stale`

Request to add a progress bar indicator during page navigation, similar to VuePress.

**Assessment:** Feature request with minimal engagement (0 upvotes, 1 comment). One respondent questioned the value, suggesting it would only be useful on slow networks. Low demand compared to other features.

**Action:** Consider low priority; gather more demand signals or evaluate against planned 2.0 features.

#### [#2453](https://github.com/vuejs/vitepress/issues/2453) Allow newlines in the text within features

`P3` `likely-answered` `effort:S` · 👍 0 · 💬 2 · opened 3.1y ago, active 2d ago · labels: `theme` `has-workaround` `stale`

Feature request to allow newlines in home page feature box text. YAML supports multiline text (>, \|) but newlines are stripped when rendering.

**Assessment:** Low-priority cosmetic enhancement. Community provided two workarounds: use <br> tags or add CSS white-space property. Labeled with has-workaround. Minimal demand (0 thumbs up). Issue author not engaged with solutions.

**Action:** Close: answered — maintainer guidance documents CSS solution, users can use <br> or custom CSS for existing releases.

#### [#2706](https://github.com/vuejs/vitepress/issues/2706) 默认主题增加nav-bar-after的slot

`P3` `needs-info` `effort:S` · 👍 0 · 💬 2 · opened 2.9y ago, active 2d ago · labels: `theme` `stale`

Request to add nav-bar-after slot to default theme layout for full-width content (e.g., cover images) below the navbar.

**Assessment:** Valid feature request in Chinese. User demonstrated workaround using DOM manipulation. Maintainer asked for visual clarification; user provided screenshot. Need to assess if this is common use case or edge case.

**Action:** Review screenshot and decide if nav-bar-after slot is general-purpose enough; if yes, implement alongside existing nav slots.

#### [#2788](https://github.com/vuejs/vitepress/issues/2788) Ability to add title to features section

`P3` `actionable` `effort:S` · 👍 0 · 💬 4 · opened 2.9y ago, active 2d ago · labels: `theme` `has-workaround` `stale`

Request to add features-title property to home layout frontmatter to display a title above the features section.

**Assessment:** Valid small feature with workaround (CSS ::before). Zero thumbs-up but user showed clear use case and willingness to work on it. Another user extended request to multiple feature sections with tags.

**Action:** Implement features-title support; consider multiple feature sections if scope allows, but focus on single title first.

#### [#3071](https://github.com/vuejs/vitepress/issues/3071) Do you have a feature similar to Notion's "Lock sidebar open" and "Close sidebar"?

`P3` `likely-answered` `effort:S` · 👍 0 · 💬 4 · opened 2.7y ago, active 2d ago · labels: `theme` `stale`

Request to lock/unlock sidebar on large viewports to allow manual collapse for wider reading space.

**Assessment:** brc-dd explained sidebar auto-collapses on small screens and can be toggled on medium viewports. User wants this behavior on large viewports too. This is a theme UX enhancement but lower priority.

**Action:** Close or defer — current behavior is intentional; feature is a nice-to-have that could be implemented as theme override.

#### [#3270](https://github.com/vuejs/vitepress/issues/3270) Feature request: Vue component source code demo

`P3` `likely-answered` `effort:M` · 👍 0 · 💬 5 · opened 2.6y ago, active 2d ago · labels: `stale`

Request for built-in Vue component demo/preview component similar to vitepress-demo-preview plugin. Users want side-by-side code and live component display.

**Assessment:** Feature request is valid but likely addressed via custom components per thread comments. Community confirmed Vue code preview is already supported; feature request seeks convenience component. No maintainer block identified.

**Action:** Close: answered. Document workaround using custom Vue components or link to community plugins that provide this functionality.

#### [#3351](https://github.com/vuejs/vitepress/issues/3351) Keep aside marker visible

`P3` `stale-candidate` `effort:S` · 👍 0 · 💬 0 · opened 2.5y ago, active 2d ago · labels: `theme` `a11y` `stale`

Request to keep the sidebar navigation marker visible when scrolling in a tall aside element. Currently the marker scrolls out of view.

**Assessment:** Valid feature request with minimal detail. No maintainer engagement. Marked stale but is a reasonable accessibility/UX enhancement for the default theme.

**Action:** Verify still relevant, assess if feasible fix for keeping outline marker sticky/fixed during scroll.

#### [#3407](https://github.com/vuejs/vitepress/issues/3407) Add separator/delimiter between nav links

`P3` `actionable` `effort:S` · 👍 0 · 💬 0 · opened 2.5y ago, active 6d ago · labels: `theme` `stale`

Request for separator/delimiter type in nav config to visually group navigation links (e.g., { type: 'separator' }).

**Assessment:** Valid feature request for improved nav UX. No maintainer engagement or thumbs up. Low priority enhancement with clear, implementable syntax suggestion.

**Action:** Consider implementing as simple nav item type; low complexity, good UX improvement.

#### [#3420](https://github.com/vuejs/vitepress/issues/3420) SVG theming on ThemeableImage

`P3` `actionable` `effort:M` · 👍 0 · 💬 0 · opened 2.5y ago, active 6d ago · labels: `theme` `stale`

Request to support SVG theming (light/dark fill colors) in ThemeableImage, extending beyond current img tag support.

**Assessment:** Valid feature request to extend ThemeableImage to support SVG with theme-aware fill colors. No maintainer feedback or demand signals. Requires thoughtful API design for SVG-specific attributes.

**Action:** Design API for SVG theming in ThemeableImage; consider if SVG files should be inlined or referenced with CSS overrides.

#### [#3426](https://github.com/vuejs/vitepress/issues/3426) feat: cannot automatically top the current page

`P3` `actionable` `effort:S` · 👍 0 · 💬 0 · opened 2.5y ago, active 6d ago · labels: `theme` `stale`

Request for auto-scroll/auto-reveal of current page in sidebar to improve UX with long sidebars.

**Assessment:** Valid UX improvement request. User provides screenshots showing current page title is out of view in sidebar after navigation. Author willing to contribute. No maintainer feedback yet.

**Action:** Implement smooth scroll-to-active-item in sidebar on page navigation.

#### [#3855](https://github.com/vuejs/vitepress/issues/3855) Support for lightbox image galleries

`P3` `stale-candidate` `effort:L` · 👍 0 · 💬 1 · opened 2.2y ago, active 6d ago · labels: `stale`

Request for built-in lightbox/image gallery support. User describes detailed workaround using markdown-it-div, fslightbox library, and custom theme setup.

**Assessment:** Valid request but demonstrates heavy custom setup required. Community plugin linked in September 2024 comment (vitepress-plugin-lightbox). No maintainer engagement. Out of core scope; library approach better.

**Action:** Recommend the linked plugin or similar community solutions. Document plugin architecture for community contributions.

#### [#4112](https://github.com/vuejs/vitepress/issues/4112) Distinguish style isolation, router isolation, and both

`P3` `likely-answered` `effort:M` · 👍 0 · 💬 3 · opened 1.9y ago, active 6d ago · labels: `stale`

Request to distinguish between style isolation and router isolation, as `vp-raw` currently applies both. User wants style isolation without router isolation.

**Assessment:** Enhancement request with working solution. User wants finer-grained control. Maintainer brc-dd suggested workaround: use custom class with postcssIsolateStyles prefix. User confirmed solution and asked for enhancement ticket if reasonable. Good feature suggestion but solved via config.

**Action:** Close: answered; document using custom class names with postcssIsolateStyles for selective isolation instead of vp-raw.

#### [#4396](https://github.com/vuejs/vitepress/issues/4396) 提供/暴露 一些 文档中常用的组件,在vue中使用，这样展示内容就可以是响应式的了,内容以插槽的方式传入

`P3` `needs-info` `effort:M` · 👍 0 · 💬 0 · opened 1.6y ago, active 6d ago · labels: `stale`

Request to expose common markdown components (e.g., `<press-code>`) as responsive Vue components with slot support, improving readability over raw template syntax.

**Assessment:** Feature request for better authoring DX. User shows poor readability with template hacks. Notes template indentation not working (possible bug). No maintainer response. Lower priority — workaround exists via custom components.

**Action:** Clarify if indentation bug is real; suggest custom component approach; consider for 2.0 theme redesign.

#### [#4669](https://github.com/vuejs/vitepress/issues/4669) sidebar accounts for tooo much

`P3` `likely-answered` `effort:M` · 👍 0 · 💬 1 · opened 1.3y ago, active 6d ago · labels: `stale`

Request to adjust sidebar width to leave more space for main content; sidebars consume too much space.

**Assessment:** Valid feature request for theme customization. Community member Teraskull suggested workaround using enhanced readability plugin. No maintainer response. Marked stale.

**Action:** Close or mark as documentation—refer to custom CSS override options or third-party plugins for sidebar adjustments.

#### [#4926](https://github.com/vuejs/vitepress/issues/4926) Make (it possible to set) color of Caution yellow

`P3` `likely-answered` `effort:S` · 👍 0 · 💬 6 · opened 10mo ago, active 6d ago · labels: `docs` `theme` `has-workaround` `needs more discussion` `stale`

Request changing Caution alert color from red to yellow to match safety standards where Caution is less serious than Warning, differing from GitHub's choice.

**Assessment:** User makes valid point about safety conventions (yellow = caution, red = danger), but VitePress follows GitHub's schema. brc-dd confirmed feature is already customizable via CSS variables (--vp-c-caution-*) and Danger type is supported. Suggested documenting custom alert title syntax and Danger type in docs.

**Action:** Close: answered — feature already customizable. Consider docs addition for custom titles and Danger type.

#### [#5057](https://github.com/vuejs/vitepress/issues/5057) Feature Request: Add Built-in Comment Section Support for Documentation Pages

`P3` `likely-answered` `effort:L` · 👍 0 · 💬 1 · opened 7mo ago, active 6d ago · labels: `stale`

Request for built-in comment section support with optional providers (Giscus, Twikoo) and automatic theme adaptation.

**Assessment:** Valid feature request but workarounds exist. Commenter already suggested Giscus as proven workaround. Likely out of scope for core; better handled by custom integration or community plugin.

**Action:** Close with link to Giscus workaround; document custom comment integration pattern in guides.

#### [#5062](https://github.com/vuejs/vitepress/issues/5062) Proposal: Provide optional mobile phone preview for component library documents

`P3` `actionable` `effort:L` · 👍 0 · 💬 0 · opened 6mo ago, active 6d ago · labels: `stale`

Proposal for optional mobile phone preview feature in VPDoc component for mobile-first component library documentation.

**Assessment:** Well-documented feature proposal with mockup. Specific use case for component library docs. No maintainer feedback yet. Significant implementation effort.

**Action:** Consider as optional theme feature; document as community contribution opportunity or custom layout guide.

#### [#5090](https://github.com/vuejs/vitepress/issues/5090) Theme suggestion: mobile TOC active highlight

`P3` `actionable` `effort:S` · 👍 0 · 💬 0 · opened 6mo ago, active 6d ago · labels: `stale`

Mobile table of contents lacks visual active highlight, unlike desktop version.

**Assessment:** Valid UX enhancement request. Simple CSS styling change. No blockers or maintainer feedback.

**Action:** Add CSS styling to highlight active TOC item on mobile to match desktop behavior.

#### [#5094](https://github.com/vuejs/vitepress/issues/5094) Edit link option in home page

`P3` `stale-candidate` `effort:S` · 👍 0 · 💬 0 · opened 6mo ago, active 6d ago · labels: `stale`

Request to add an `index` option to the `editLink` config to enable edit links on the home page (index.md), which is currently not supported.

**Assessment:** Valid feature request with clear use case. Has been marked stale with no response; no maintainer engagement. Low demand (0 thumbs_up), likely low priority.

**Action:** Close: stale with no activity; reopen if community demand increases.

#### [#5146](https://github.com/vuejs/vitepress/issues/5146) Add Auto Theme Mode (Match System Theme)

`P3` `actionable` `effort:M` · 👍 0 · 💬 0 · opened 4mo ago, active 6d ago · labels: `stale`

Request to add a 'system' theme option that automatically matches the OS-level theme preference using `prefers-color-scheme` media query, syncing with system theme switches.

**Assessment:** Valid feature request with clear use case. No maintainer response; stale. Many modern frameworks support this pattern. Moderate implementation effort (add theme mode + media listener). No high demand (0 thumbs_up).

**Action:** Implement: add 'system' theme mode to default theme that observes prefers-color-scheme and auto-switches between light/dark.

#### [#5178](https://github.com/vuejs/vitepress/issues/5178) Feature Request: Cookie Consent & Privacy Acceptance Banner

`P3` `stale-candidate` `effort:M` · 👍 0 · 💬 0 · opened 3mo ago, active 6d ago · labels: `stale`

Requests built-in cookie consent and privacy acceptance banner component or plugin for VitePress sites.

**Assessment:** Valid feature request but marked stale; out-of-scope for core VitePress (better suited as community plugin). No maintainer response and zero community interest.

**Action:** Close as out-of-scope; suggest building a community plugin.

#### [#5184](https://github.com/vuejs/vitepress/issues/5184) 能不能在页面这里加个插槽

`P3` `needs-info` `effort:M` · 👍 0 · 💬 0 · opened 3mo ago, active 6d ago · labels: `stale`

Feature request (in Chinese) asking for a slot to add custom content (character profile module) on the left side of a page.

**Assessment:** Vague feature request in non-English; includes screenshot but no context on desired behavior or current workarounds. May be answerable via existing theme slots or custom CSS. Marked stale.

**Action:** Request clarification: which page layout, what existing slots were tried, current theme version. Provide slot documentation link.

## Local search (12)

#### [#3254](https://github.com/vuejs/vitepress/issues/3254) Searchable tags

`P1` `actionable` `effort:L` · 👍 20 · 💬 9 · opened 2.6y ago, active 2d ago · labels: `stale`

Feature request: tagging system with frontmatter tags, searchable via built-in search or sidebar tag list, to organize related content.

**Assessment:** High-demand feature (20 thumbs_up). User building legal resource site needs taxonomy. Thread shows multiple +1s and one detailed implementation suggestion (include rendered frontmatter in IndexObject, weighting support). Parallel need to support search across MiniSearch-based local search. Significant implementation effort but strong community support.

**Action:** Implement searchable tags: add frontmatter tag field, include in local search index, support tag filtering in search query syntax (e.g., 'tag:security').

#### [#2454](https://github.com/vuejs/vitepress/issues/2454) Feature request: Add hook for transforming minisearch index data

`P2` `likely-answered` `effort:S` · 👍 4 · 💬 1 · opened 3.1y ago, active 2d ago · labels: `stale`

Feature request for a hook (transformPageIndex) to customize minisearch index fields, allowing addition of custom fields like category.

**Assessment:** Valid feature request with practical use case. Maintainer provided working solution using miniSearch options in config (extractField, custom fields/storeFields). 4 thumbs up. Issue is answered but users may not discover the solution.

**Action:** Close: answered — maintainer solution is valid; update docs or docs example to show miniSearch customization pattern.

#### [#3230](https://github.com/vuejs/vitepress/issues/3230) local search: show partial hint or full link path in search result

`P1` `actionable` `effort:M` · 👍 0 · 💬 2 · opened 2.6y ago, active 2d ago · labels: `stale`

Feature request: display partial breadcrumb hint (e.g., '[fruit]') or full URL path in local search results to show where results come from.

**Assessment:** Valid feature request with clear use case. User notes many pages share similar names. Author later created a duplicate issue #3437 with more detail. Second commenter offered to work on PR. High demand—improves search UX significantly. Should track under issue #3437.

**Action:** Verify against #3437 (duplicate mentioned in thread); prioritize #3437 if active, or consolidate effort there.

#### [#4621](https://github.com/vuejs/vitepress/issues/4621) Search bar should show something/anything to indicate it's working

`P1` `actionable` `effort:M` · 👍 0 · 💬 0 · opened 1.3y ago, active 6d ago · labels: `stale`

Local search UI shows no loading indicator while the ~20 MB search chunk downloads on first use, giving the impression the UI is frozen or broken.

**Assessment:** Valid UX issue with clear impact. Feature request for loading state (spinner/message) in search bar. No blockers. High value: improves perceived performance and clarity for users on slow connections. Recent commit 7e2273a3 mentions 'show local search loading state', likely addressing this.

**Action:** Verify against latest main — likely already implemented; if so, close as fixed.

#### [#2731](https://github.com/vuejs/vitepress/issues/2731) Exact match local search

`P2` `actionable` `effort:M` · 👍 0 · 💬 8 · opened 2.9y ago, active 2d ago · labels: `stale`

Request to add exact phrase matching to local search (e.g., quoted strings like 'create table' should match only results with both words together).

**Assessment:** Valid feature with high demand (large doc sets benefit from phrase search). MiniSearch author confirmed exact matching is complex but possible via combineWith:AND. Maintainer indicated readiness to implement. Clear path forward via minisearch options.

**Action:** Implement exact phrase matching in local search; use MiniSearch combineWith option or phrase-aware filtering logic.

#### [#4049](https://github.com/vuejs/vitepress/issues/4049) Better default minisearch tokenizer for Chinese documents

`P2` `actionable` `effort:M` · 👍 0 · 💬 6 · opened 2.0y ago, active 6d ago · labels: `help wanted` `stale`

Chinese text search in minisearch does not work without manual tokenizer configuration. Users need default support for Chinese language segmentation.

**Assessment:** Valid issue acknowledged by maintainer brc-dd, who stated "There should be some defaults here instead of needing Chinese users to manually configure it." The issue is about improving default tokenizer behavior for CJK languages. Some workarounds exist via minisearch segmenter plugins.

**Action:** Implement better default minisearch tokenizer for Chinese/CJK documents; reference minisearch#201 and existing community solutions.

#### [#5009](https://github.com/vuejs/vitepress/issues/5009) Search Box Slow

`P2` `actionable` `effort:L` · 👍 0 · 💬 0 · opened 8mo ago, active 6d ago · labels: `stale`

Performance issue with local search: large index files cause slow load times and require reload on each search box open. User suggests using SQLite3 instead.

**Assessment:** Valid performance concern for large documentation sites. No maintainer feedback yet. Proposes SQLite3 which is not feasible for client-side web search (would require backend). May benefit from index chunking, lazy loading, or compression strategies instead.

**Action:** Investigate local search performance optimization: index chunking, lazy loading, or compression; SQLite3 not viable for client-side

#### [#5077](https://github.com/vuejs/vitepress/issues/5077) feat(search): Scalable Local Search Architecture (Worker offload + Binary Indexing) for large documentation sites

`P2` `actionable` `effort:L` · 👍 0 · 💬 1 · opened 6mo ago, active 6d ago · labels: `stale`

Proposal for optimized local search using Web Workers, binary indexing, and pre-built indexes to handle large sites (16k+ pages) with 70% memory reduction and 0ms search latency.

**Assessment:** Well-researched feature request with detailed architecture and PoC. Author claims 1.7GB→510MB memory reduction. Author willing to contribute PR. Addresses real scaling limitation.

**Action:** Engage author on technical approach; consider for 2.0 roadmap if feasible; request draft PR for architecture review.

#### [#3192](https://github.com/vuejs/vitepress/issues/3192) Display directory heading in search results

`P3` `likely-answered` `effort:S` · 👍 0 · 💬 2 · opened 2.7y ago, active 2d ago · labels: `theme` `stale`

Request to display sidebar directory breadcrumb in search results to disambiguate pages with similar titles across different sections.

**Assessment:** Valid UX request with clear use case (navigation organization). brc-dd showed that UnoCSS docs already displays full path in results, suggesting current implementation may already show this. User clarified they want directory-level breadcrumb in results like VuePress. Feature gap vs VuePress but may be implementation-specific.

**Action:** Verify if current search displays full breadcrumb. If not, implement breadcrumb display in search results to match UnoCSS example brc-dd provided.

#### [#3429](https://github.com/vuejs/vitepress/issues/3429) Add lunr.js as one of the available plugins for Search

`P3` `likely-answered` `effort:M` · 👍 0 · 💬 1 · opened 2.5y ago, active 6d ago · labels: `has-workaround` `stale`

Feature request to add lunr.js as an alternative search plugin for better search quality compared to default local search.

**Assessment:** Valid request but maintainer already provided workarounds (vitepress-plugin-search, Orama plugin). User shows concrete complaint about search ranking. Marked stale; has viable alternatives.

**Action:** Close: answered with alternative solutions (Orama VitePress plugin, vitepress-plugin-search).

#### [#4117](https://github.com/vuejs/vitepress/issues/4117) Only index pages for searching under the same directory

`P3` `needs-info` `effort:L` · 👍 0 · 💬 0 · opened 1.9y ago, active 6d ago · labels: `stale`

Request to limit local search results to pages in the current directory instead of indexing all pages.

**Assessment:** Valid idea but marked stale. Not actionable without clarification—directories in VitePress are separate and search is global by design. Would need to understand if this is a fundamental design change or custom plugin request.

**Action:** Close or clarify: is user seeking custom plugin guidance or a core feature? Directory-scoped search would be a significant design change.

#### [#4280](https://github.com/vuejs/vitepress/issues/4280) CSS Custom Highlight API

`P3` `actionable` `effort:M` · 👍 0 · 💬 3 · opened 1.7y ago, active 6d ago · labels: `theme` `stale`

Proposal to use native CSS Custom Highlight API instead of string-replacement-based highlighting for search results.

**Assessment:** Good feature request with detailed API example. brc-dd noted browser support not yet sufficient (Chrome/Edge >= 88, Firefox >= 78, Safari >= 14; some browsers don't meet this). Flagged as future improvement when support matures. roydukkey shared a Vue directive.

**Action:** Track as future enhancement when browser support criteria are met; consider CSS.highlights as mark.js replacement in future major version.

## Algolia search (1)

#### [#5124](https://github.com/vuejs/vitepress/issues/5124) Can we support keyword-only search modal alongside Ask AI side panel?

`P2` `actionable` `effort:M` · 👍 0 · 💬 3 · opened 4mo ago, active 6d ago · labels: `theme` `stale`

Request to support new search modes that combine keyword-only search modal with Ask AI side panel (Combination A) and local search with Ask AI side panel (Combination B), currently impossible with existing mode options.

**Assessment:** Valid feature request with detailed analysis of current limitations. brc-dd confirmed Combination A is feasible (2026-03-05), Combination B is more complex. Addresses both UX preference and cost concerns. No thumbs_up but maintainer engagement suggests implementation is planned.

**Action:** Implement Combination A (Algolia keyword-only modal + Ask AI side panel); defer Combination B or scope separately.

## Build (18)

#### [#3461](https://github.com/vuejs/vitepress/issues/3461) support for single file output

`P1` `stale-candidate` `effort:L` · 👍 12 · 💬 1 · opened 2.5y ago, active 6d ago · labels: `build` `stale`

Feature request to support single-file bundle output (all CSS/JS/assets inlined) for offline sharing, games, and email content.

**Assessment:** Valid request with 12 thumbs up. User provides detailed rationale and code examples. Mentions past related issues (1889, 2829). Community member asked for update as of Sep 2025. Real use cases for static/embedded docs.

**Action:** Unstale and evaluate for 2.0.0-alpha; would require hash-based routing and build-time asset inlining, feasible with Vite 8 architecture.

#### [#2826](https://github.com/vuejs/vitepress/issues/2826) Generate dynamic routes after performing build-time data loading

`P1` `upstream-blocked` `effort:L` · 👍 9 · 💬 7 · opened 2.9y ago, active 2d ago · labels: `stale`

Request to generate dynamic routes after build-time data loading. User wants to extract tags from blog posts and generate tag pages using data from a content loader in the paths definition.

**Assessment:** Valid and frequently requested feature (9 thumbs_up). The core blocker is architectural: route resolution happens too early in VitePress build lifecycle, before data loaders are processed. xen0n identified the root cause in resolvePages() call. Multiple users have reported this is a common blog use case.

**Action:** Track as medium-term feature for 2.0. Requires refactoring build pipeline to defer route resolution until after data loaders run. Consider API for parameterized .paths.ts files as alternative.

#### [#3183](https://github.com/vuejs/vitepress/issues/3183) Concurrent build

`P1` `actionable` `effort:L` · 👍 4 · 💬 2 · opened 2.7y ago, active 2d ago · labels: `build` `stale`

Request to use concurrent builds with worker threads to speed up builds for sites with ~1000+ complex pages with many code snippets.

**Assessment:** Valid feature request (4 thumbs_up) with significant demand. zhangyx1998 provided concrete suggestion to move renderPage() into worker threads. pMap already in use for pooling. Implementation path clear and feasible. Alternative to #1991.

**Action:** Implement: Refactor renderPage() to use worker threads via pMap pooling. Moderate-to-small effort with high impact for large documentation sites.

#### [#2692](https://github.com/vuejs/vitepress/issues/2692) VitePress should mimic Vite's --mode flag when running "vitepress build"

`P2` `actionable` `effort:M` · 👍 4 · 💬 1 · opened 2.9y ago, active 2d ago · labels: `build` `has-workaround` `stale`

VitePress should support --mode flag in build command to load .env.<mode> files, matching Vite's behavior.

**Assessment:** Valid feature request with clear explanation and use case (UAT/PROD env separation). 4 thumbs-up reactions. Maintainer (brc-dd) acknowledged difficulty but did not reject. No workaround is satisfactory for CI/CD pipelines.

**Action:** Implement --mode flag support in vitepress build; align with Vite's env loading mechanism.

#### [#3367](https://github.com/vuejs/vitepress/issues/3367) Provide chunk splitting for shared objects like sidebar and nav

`P2` `actionable` `effort:M` · 👍 3 · 💬 8 · opened 2.5y ago, active 2d ago · labels: `perf` `stale`

Request for chunk splitting and client-side rendering of shared sidebar/navbar to reduce bundle size. User reports 1.7GB output with large sidebar (414KB).

**Assessment:** Valid feature with strong repro and measurable impact. Maintainer suggested metaChunk config; user verified it reduces size but nav/sidebar still duplicated. Author of issue (zhangyx1998) has created a PR to make nav/sidebar client-renderable via ClientOnly wrapper. Recent activity (March-April 2025) shows strong demand and progress toward implementation.

**Action:** Review and merge pending PR for configurable client-side rendering of nav/sidebar components.

#### [#646](https://github.com/vuejs/vitepress/issues/646) dead link checks should ideally also take the hash into account

`P3` `actionable` `effort:M` · 👍 3 · 💬 1 · opened 4.1y ago, active 2d ago · labels: `build` `stale`

Dead link checking should also validate URL fragments (anchor links) by parsing and caching page headers.

**Assessment:** Valid enhancement (3 thumbs_up). Authored by maintainer kiaking. A user posted a workaround (2023-09) parsing output HTML. Useful but not critical—dead link checkers currently catch broken page links.

**Action:** Design API for header caching; implement optional fragment validation in link checker; document limitations and performance implications.

#### [#3380](https://github.com/vuejs/vitepress/issues/3380) Dynamic route content loading as a function

`P2` `actionable` `effort:M` · 👍 2 · 💬 9 · opened 2.5y ago, active 2d ago · labels: `build` `perf` `stale`

Request to support lazy/on-demand content loading in dynamic routes via getter functions, to reduce memory overhead during builds with many CMS-powered pages.

**Assessment:** Valid use case: large CMS-driven site with 450+ pages hitting 1GB memory during build. Maintainer brc-dd proposed async getters returning Promises; discussion suggests this is feasible and low-impact. Maintainer noted memory savings would be modest (10-20MB) but worth doing for completeness.

**Action:** Implement support for async getter functions in dynamic route content to enable lazy loading.

#### [#4923](https://github.com/vuejs/vitepress/issues/4923) 【建议】放宽浏览器兼容性以更好地支持企业用户

`P3` `likely-answered` `effort:S` · 👍 2 · 💬 5 · opened 10mo ago, active 6d ago · labels: `client` `stale`

Request relaxed browser compatibility defaults to support older browsers in enterprise environments (targeting Chromium 86+/ES2020).

**Assessment:** Valid enterprise use case. brc-dd provided clear workaround: set `vite.build.target` to `'es2015'`, add polyfills, and use postcss-preset-env. User then reported specific blocker: `:where()` CSS selector unsupported in older browsers. brc-dd removed that selector. No further action needed for general request.

**Action:** Close: answered with workaround — CSS compatibility issue fixed (removed :where() usage). Users can configure Vite target and polyfills per guide.

#### [#1991](https://github.com/vuejs/vitepress/issues/1991) Progressive build

`P2` `actionable` `effort:L` · 👍 1 · 💬 3 · opened 3.4y ago, active 2d ago · labels: `stale`

Request for progressive/incremental build support to speed up builds for large websites by only re-rendering changed markdown files.

**Assessment:** Valid feature request with maintainer (Evan You) acknowledgement that it's technically challenging and would require significant architectural changes. Low engagement (1 thumbs_up) suggests modest demand.

**Action:** Track for 2.0 or future major refactor; document as known limitation. Progressive builds require build system redesign.

#### [#2645](https://github.com/vuejs/vitepress/issues/2645) Use nodegit for last updated detection

`P2` `possibly-fixed` `effort:L` · 👍 0 · 💬 2 · opened 3.0y ago, active 2d ago · labels: `stale`

Request to use nodegit or isomorphic-git instead of spawning git process for every file during last-updated detection, which adds 30 seconds to builds.

**Assessment:** Valid performance issue. Commenter suggested isomorphic-git as better alternative to nodegit. No maintainer response or decision. Likely still relevant but may be addressed by other build optimizations.

**Action:** Investigate if build performance improved in 2.0.0-alpha or Vite 8 migration; if still slow, consider lighter git strategy.

#### [#3384](https://github.com/vuejs/vitepress/issues/3384) [build] add progress indication for `task()`

`P2` `possibly-fixed` `effort:S` · 👍 0 · 💬 2 · opened 2.5y ago, active 2d ago · labels: `stale`

Request for progress indication during build task execution, with format like [Completed/Total].

**Assessment:** Feature request by zhangyx1998 who has already implemented it. Two follow-up comments (Dec 2023, Dec 2023) show working implementation with progress bar and handle exposed for subtask reporting. Thread shows feature working and satisfying the original request.

**Action:** Verify if this feature landed in a release; close if merged or ask for PR link.

#### [#4009](https://github.com/vuejs/vitepress/issues/4009) Enabling External Link Validity Checking

`P2` `likely-answered` `effort:M` · 👍 0 · 💬 2 · opened 2.0y ago, active 6d ago · labels: `has-workaround` `stale`

Requests external link validity checking during build. Currently only localhost links are validated.

**Assessment:** Answered by brc-dd: recommended using dedicated CLI tools (lychee). Explained why ignoreDeadLinks cannot cover external links (many not in markdown AST). User accepted explanation and offered doc PR.

**Action:** Close: answered. Add documentation recommending lychee or similar external link checkers in CI pipeline.

#### [#4428](https://github.com/vuejs/vitepress/issues/4428) Build error or warning when using non-supported custom container

`P2` `actionable` `effort:M` · 👍 0 · 💬 2 · opened 1.5y ago, active 6d ago · labels: `build` `stale`

Request to error or warn during build when user writes unsupported custom container syntax (e.g., `::: NOTE` instead of `::: warning`), which silently renders as plain text.

**Assessment:** Valid feature request. Maintainer brc-dd provided workaround using markdown-it-container with validation, but acknowledged it lacks file/line info. Would improve developer experience by catching mistakes. Related to #4427.

**Action:** Implement warning/error in markdown build phase when unknown containers detected; show file + line number for better DX.

#### [#4992](https://github.com/vuejs/vitepress/issues/4992) [Feature Request] 报错信息中原封不动输出url

`P2` `actionable` `effort:M` · 👍 0 · 💬 0 · opened 9mo ago, active 6d ago · labels: `stale`

Feature request to improve readability of error messages containing URL-encoded characters (e.g., Chinese characters) in build logs, specifically for dead link warnings. Requests URL decoding and optional log localization.

**Assessment:** Valid enhancement for improving developer experience with non-ASCII characters in build output. Chinese community impact noted. No responses from maintainers. No blockers identified.

**Action:** Implement - improve dead-link error messages by decoding URLs for better readability

#### [#1891](https://github.com/vuejs/vitepress/issues/1891) "created" time

`P3` `actionable` `effort:M` · 👍 0 · 💬 3 · opened 3.4y ago, active 2d ago · labels: `build` `theme` `stale`

Request to display file creation time in addition to last updated time.

**Assessment:** Valid feature with technical discussion. Maintainer questioned feasibility of retrieving creation time. Community member suggested using git log --diff-filter=A to find creation commit. Requires git integration, applicable only to git-based projects.

**Action:** Consider as enhancement; add git integration to extract creation date from first commit. Requires config option to enable and handle non-git projects gracefully.

#### [#2681](https://github.com/vuejs/vitepress/issues/2681) New "failsafe" configuration option

`P3` `likely-answered` `effort:M` · 👍 0 · 💬 2 · opened 2.9y ago, active 2d ago · labels: `build` `has-workaround` `stale`

Request for failsafe/lenient build mode that logs errors and skips broken pages instead of crashing the entire build.

**Assessment:** Valid use case for migrating Jekyll sites with malformed HTML. Maintainer offered workaround (srcExclude config). User noted workaround doesn't help with dynamically updated submodule pages. Feature has workaround but incomplete solution.

**Action:** Document srcExclude workaround; clarify if dynamic page filtering is a real blocker or can be solved via pre-build hooks.

#### [#3509](https://github.com/vuejs/vitepress/issues/3509) DOM is oversized

`P3` `needs-info` `effort:L` · 👍 0 · 💬 0 · opened 2.4y ago, active 6d ago · labels: `perf` `stale`

Performance/feature request to lazy-load DOM for very large markdown files (3MB+) to improve initial render time.

**Assessment:** Valid concern for edge case (very large docs). No maintainer response. Marked stale. Asks if DOM can be split and lazy-loaded. No clear repro steps or performance metrics provided.

**Action:** Request minimal reproduction with metrics; evaluate if Vite 8 chunking + on-demand imports can address large content loading.

#### [#3599](https://github.com/vuejs/vitepress/issues/3599) Should add more config about `glob` to data loading , such as `dot: true`

`P3` `actionable` `effort:S` · 👍 0 · 💬 2 · opened 2.4y ago, active 6d ago · labels: `build` `stale`

Request to expose `dot: true` option from fast-glob to staticDataPlugin for matching dotfiles in data loading.

**Assessment:** Valid edge case (dotfiles not matched by default glob). User notes workaround of disabling watch mode exists. Affects data loading with hidden files.

**Action:** Implement—small effort, unblocks users wanting to watch dotfiles in data loading.

## Dev server / HMR (1)

#### [#4221](https://github.com/vuejs/vitepress/issues/4221) Support hot reload by watching files not under docs

`P2` `needs-info` `effort:S` · 👍 0 · 💬 1 · opened 1.8y ago, active 6d ago · labels: `need more info` `stale`

User requests configuration option to watch files outside docs folder to trigger hot reload in monorepo dev setups where docs depend on code in sibling packages.

**Assessment:** Valid monorepo use case. Maintainer brc-dd replied asking for project structure, suggesting it may already be configurable via Vite options. No further details provided. Marked stale.

**Action:** Ask for project structure—likely solvable via vite.server.watch config or symlinks; if not, expose as VitePress config option.

## SSR / hydration (1)

#### [#2492](https://github.com/vuejs/vitepress/issues/2492) [ssr]You can ignore a file for SSR detection/or disable it for SSR rendering

`P3` `actionable` `effort:M` · 👍 1 · 💬 0 · opened 3.1y ago, active 2d ago · labels: `stale`

Feature request to allow marking markdown files as client-only to avoid SSR errors when markdown contains browser-specific APIs (e.g., alert). User loads markdown content via CDN editor.

**Assessment:** Niche use case (storing .md files in /public with browser scripts). Low engagement (1 thumbs up, no comments). Valid problem but unusual pattern. Recommending ClientOnly wrapper for components is standard; similar config for .md files would be new capability.

**Action:** Request clarification: why is .md file in /public instead of docs? Consider if this is the right pattern or if wrapping in ClientOnly component would solve.

## Router / navigation (3)

#### [#2892](https://github.com/vuejs/vitepress/issues/2892) Runtime dynamic routes

`P2` `actionable` `effort:L` · 👍 6 · 💬 3 · opened 2.8y ago, active 2d ago · labels: `stale`

Request for runtime dynamic routes to support client-side routing in partially-static sites. User wants [page].md or similar to behave like Vue Router dynamic routes for user-driven navigation (e.g., user profile pages).

**Assessment:** Valid feature request for hybrid static+SPA use case (6 thumbs_up). edualb provided working workarounds using router hooks and markRaw components. The workarounds are documented but feel hacky. Would benefit from first-class support.

**Action:** Consider API for designating routes as client-side only. Document existing workarounds prominently. Could leverage enhanceApp router customization.

#### [#5004](https://github.com/vuejs/vitepress/issues/5004) Consider to use Vue Router

`P2` `actionable` `effort:L` · 👍 4 · 💬 1 · opened 8mo ago, active 6d ago · labels: `stale`

Feature request to replace VitePress's custom router with the official Vue Router to better support integration with Vue ecosystem packages like Nuxt UI and enable more complex use cases (data fetching via API).

**Assessment:** Architectural feature request with 4 thumbs_up. Valid motivation (Vue ecosystem integration) but represents a major breaking change to VitePress's router layer. Maintainers may have deliberate reasons for custom router (simplicity, static generation focus). Needs discussion on feasibility and design.

**Action:** Discuss with maintainers - significant architectural change, clarify use cases and trade-offs

#### [#3326](https://github.com/vuejs/vitepress/issues/3326) Use the hash of the article title to provide the article url

`P2` `actionable` `effort:M` · 👍 0 · 💬 16 · opened 2.6y ago, active 2d ago · labels: `build` `needs more discussion` `stale`

Request to use hash of article title for URLs instead of encoded non-Latin characters. Reduces URL length/complexity for non-English content.

**Assessment:** Valid feature request. Thread shows discussion pivoted to "copy decoded URL button" as more practical solution. Maintainer (brc-dd) asked for UI ideas. Recent suggestion (May 2025) about anchor consistency in multilingual docs adds related concern.

**Action:** Clarify scope: implement URL slug hashing option, or prioritize decoded URL copy button feature. Separate multilingual anchor consistency into its own issue if needed.

## i18n (6)

#### [#3047](https://github.com/vuejs/vitepress/issues/3047) CSS i18n in one page

`P2` `likely-answered` `effort:M` · 👍 5 · 💬 2 · opened 2.7y ago, active 2d ago · labels: `stale`

Request for CSS-based i18n approach allowing mixed-language content in single markdown file using container syntax like `::: zh-CN`.

**Assessment:** Well-reasoned feature request with working implementation (CSS-I18N, valaxy demo). Community feedback suggests it's good for small projects but multi-site approach is preferred for larger docs. Author acknowledges tradeoffs. Offers to contribute PR but no maintainer response recorded.

**Action:** Close or defer — maintainers prefer multi-site i18n for major projects; this solves a niche use case that users can implement via plugins.

#### [#3861](https://github.com/vuejs/vitepress/issues/3861) i18n with markdown containers

`P2` `needs-info` `effort:M` · 👍 4 · 💬 1 · opened 2.2y ago, active 6d ago · labels: `build` `stale`

Request for i18n support with markdown containers. User wants container titles (tip, warning, info, etc.) to be translatable per locale.

**Assessment:** Valid feature with stackblitz reproduction. Authored by brc-dd (maintainer). Comment from its-miroma (June 2024) provides detailed context: current config-based approach doesn't allow per-locale titles. No decision yet.

**Action:** Clarify design with maintainers: should locale titles be in config.ts locales, separate i18n file, or markdown metadata?

#### [#3275](https://github.com/vuejs/vitepress/issues/3275) Internationalisation: Fall back to specified language if translation is missing

`P2` `possibly-fixed` `effort:L` · 👍 2 · 💬 8 · opened 2.6y ago, active 2d ago · labels: `stale`

Request for i18n fallback mechanism: when a translated page is missing, fall back to default language instead of 404. Want to avoid building all language variants.

**Assessment:** Valid feature request (2 thumbs up). PR #3312 was mentioned (Dec 2023) with better implementation allowing per-language custom fallbacks. Issue author found this promising but did not test. No evidence of merge or closure.

**Action:** Check if PR #3312 was merged and released. If yes, close with reference. If no, clarify status and whether feature landed in 2.0.0-alpha.

#### [#4431](https://github.com/vuejs/vitepress/issues/4431) Localize Code Block copy feature

`P2` `actionable` `effort:S` · 👍 2 · 💬 7 · opened 1.5y ago, active 6d ago · labels: `build` `stale`

Request to make code block copy button tooltip and 'Copied' feedback text localizable per locale in i18n config, not just globally.

**Assessment:** Valid feature request with clear use case. User demonstrated working workaround via fence renderer override in markdown config. brc-dd confirmed it's related to #3861 (i18n for code blocks). Currently no built-in i18n for these strings.

**Action:** Add 'copy' and 'copied' strings to LocaleConfig so users can define per-locale translations without markdown config hacks.

#### [#2958](https://github.com/vuejs/vitepress/issues/2958) Can the hash value be included when the translation link jumps?

`P2` `needs-info` `effort:M` · 👍 0 · 💬 4 · opened 2.8y ago, active 2d ago · labels: `stale`

Request to preserve URL hash when clicking i18n language switcher links, so users land on equivalent section in translated page.

**Assessment:** Valid feature request for i18n UX improvement. Maintainer (brc-dd) noted technical challenge: system cannot automatically determine corresponding hash in different language without explicit mapping. Vue.js docs solved this by manually keeping anchors consistent across locales. No clear path without user configuration.

**Action:** Request clarification on implementation preference: auto-mapping vs. config-driven anchor mappings; document current workaround.

#### [#4765](https://github.com/vuejs/vitepress/issues/4765) Optional consistency validation of custom header anchors across localized documents

`P2` `actionable` `effort:L` · 👍 0 · 💬 0 · opened 1.1y ago, active 6d ago · labels: `stale`

Request for optional build-time validation of custom header anchors across localized documents to prevent broken links when users switch languages.

**Assessment:** Well-designed feature proposal with real multilingual documentation use case. User references own experience at Toss. Clear scope and implementation options provided. Solves translator workflow problem.

**Action:** Consider for i18n enhancement; could be core feature or official plugin. Document in roadmap if deprioritized.

## Config (22)

#### [#109](https://github.com/vuejs/vitepress/issues/109) Versioning

`P1` `actionable` `effort:L` · 👍 36 · 💬 17 · opened 5.7y ago, active 2d ago · labels: `stale`

Request for first-class documentation versioning support, similar to Docusaurus and VuePress. Currently requires workarounds like subdomains or the @IMB11 plugin.

**Assessment:** Valid, high demand (36 thumbs_up). brc-dd in 2025-04 commented that v2-alpha supports themeConfig per directory and outlined a roadmap: version switcher component, redirects, search faceting, and unversioned pages. This is actionable for v2, though requires significant implementation.

**Action:** Target for v2.0: implement version switcher component with URL preservation and search faceting; document versioning patterns in guides.

#### [#1297](https://github.com/vuejs/vitepress/issues/1297) auto sidebar mode

`P1` `actionable` `effort:L` · 👍 17 · 💬 19 · opened 3.8y ago, active 2d ago · labels: `theme` `has-workaround` `stale`

Request automatic sidebar generation from markdown file structure and headings instead of requiring manual configuration.

**Assessment:** High-demand feature (17 upvotes, 19 comments). Multiple third-party plugins exist (vitepress-sidebar, vite-plugin-vitepress-auto-sidebar). Recent comment from alvinometric (Feb 2025) suggests Starlight's approach with autogenerate property is elegant. Users repeatedly ask for this.

**Action:** Implement sidebar autogenerate feature similar to Starlight; allows users to opt-in by directory with explicit control while reducing manual maintenance burden.

#### [#3966](https://github.com/vuejs/vitepress/issues/3966) Unified API for VitePress Plugins

`P1` `actionable` `effort:L` · 👍 17 · 💬 2 · opened 2.0y ago, active 6d ago · labels: `help wanted` `stale`

Requests unified plugin API for VitePress. Currently plugins require inconsistent approaches: markdown config, Vite integration, or wrapper functions.

**Assessment:** Strong community request (17 thumbs_up). Valid problem: plugin setup fragmentation causes confusion and wrapper-nesting anti-patterns. Maintainer comment suggests awaiting Vite's unified API (Vite 6 environment API). Significant design work needed.

**Action:** Design plugin system that leverages Vite 6's environment API. Coordinate with Vite team on unified approach. Plan for 2.0.

#### [#285](https://github.com/vuejs/vitepress/issues/285) Add permalink feature

`P2` `actionable` `effort:M` · 👍 14 · 💬 5 · opened 5.2y ago, active 2d ago · labels: `build` `has-workaround` `stale`

Request for permalink feature in frontmatter to support custom URL structures (e.g., date-based blog permalinks).

**Assessment:** Valid request (14 thumbs_up). brc-dd provided a working workaround using route rewrites (2023-09), though it requires manual server restart on file changes. Labeled "has-workaround". Users cite this as migration blocker from VuePress.

**Action:** Document the rewrites workaround prominently; consider improving DX by making rewrites watch for frontmatter changes without restart.

#### [#2909](https://github.com/vuejs/vitepress/issues/2909) Allow using config folder other than .vitepress

`P2` `actionable` `effort:M` · 👍 5 · 💬 2 · opened 2.8y ago, active 2d ago · labels: `has-workaround` `stale`

Request to allow custom config folder path instead of hardcoded `.vitepress` directory. User wants to place config in `src/vitepress` instead of `.vitepress` for better project organization and TypeScript coverage.

**Assessment:** Valid feature request with legitimate use case. Maintainer (brc-dd) indicated a workaround exists but acknowledged the core issue: VitePress is hardcoded to look for `.vitepress`. The feature would require updating config resolution logic to accept custom paths via CLI option.

**Action:** Implement custom config path via CLI option or config setting; document workaround in the meantime.

#### [#4484](https://github.com/vuejs/vitepress/issues/4484) Add a way to disable included Markdown plugins

`P2` `actionable` `effort:M` · 👍 4 · 💬 0 · opened 1.5y ago, active 6d ago · labels: `build` `stale`

Request for configuration option to disable built-in Markdown plugins (e.g., preWrapperPlugin) that introduce extra elements theme creators may not want.

**Assessment:** Valid request from theme writers who want minimal markdown output. preWrapperPlugin adds copy buttons and code block titles—useful for default theme but intrusive for custom themes. Currently requires CSS hiding workaround. No maintainer response. 4 thumbs_up.

**Action:** Add markdown config option to disable specific plugins; at minimum preWrapper, possibly others.

#### [#736](https://github.com/vuejs/vitepress/issues/736) serve static with CDN

`P3` `likely-answered` `effort:S` · 👍 3 · 💬 5 · opened 4.1y ago, active 2d ago · labels: `stale`

Request to serve static assets (JS/CSS) from a CDN domain while keeping HTML served from origin—useful for performance optimization.

**Assessment:** Valid request (3 thumbs_up). kiaking asked for clarification (2022-06); user wants to set CDN prefix for generated assets only, not HTML. kiaking noted complexity with manifest JSON. User clarified they rely on CDN pull-through caching. No clear outcome; kiaking suggested PR welcome.

**Action:** Evaluate feasibility of asset-only CDN prefix (separate from base). If complex, document best practices (CDN pull-through, manual asset rewrite).

#### [#3180](https://github.com/vuejs/vitepress/issues/3180) Streamline extension API

`P2` `possibly-fixed` `effort:M` · 👍 2 · 💬 4 · opened 2.7y ago, active 2d ago · labels: `stale`

Request to expose 'enhanceApp' hook in site config to streamline plugin authoring, allowing package-based plugins to inject both config and app-level enhancements.

**Assessment:** Valid feature request (2 thumbs_up). User working on plugins needing both config + app context. jcbhmr demonstrated a partial workaround using 'extends' + 'additionalThemeFiles'. elringus noted related issue #3314. Current workaround uses multiple setup points but is functional.

**Action:** Consider if 'additionalThemeFiles' approach is sufficient or if explicit 'enhanceApp' in config would improve DX. Check status of #3314.

#### [#4151](https://github.com/vuejs/vitepress/issues/4151) Consider changing config.js to vitepress.config.js

`P3` `likely-answered` `effort:S` · 👍 2 · 💬 0 · opened 1.9y ago, active 6d ago · labels: `stale`

Suggest renaming config.js to vitepress.config.js following industry convention (next.config.js, vite.config.js, etc).

**Assessment:** Reasonable UX suggestion but marked stale. This would be a breaking change for a major version only. User's concern about recognizability is minor—docs clearly explain config location. Low priority.

**Action:** Consider for major version release; keep as minor enhancement candidate.

#### [#3136](https://github.com/vuejs/vitepress/issues/3136) allow site config .extends to be an array

`P2` `actionable` `effort:M` · 👍 1 · 💬 2 · opened 2.7y ago, active 2d ago · labels: `stale`

Request to allow site config 'extends' property to accept an array of preset configs instead of just a single config, enabling composition of multiple plugins like blog + jsdoc.

**Assessment:** Valid feature request (1 thumbs_up). Author provided code suggestion for implementation using Promise.all and mergeConfig. Note: mergeConfig is not currently exported, blocking the workaround. Implementation feasible.

**Action:** Implement: Allow 'extends' to be string \| object \| array, flatten with Promise.all on config resolution. Moderate effort for significant flexibility gain.

#### [#4556](https://github.com/vuejs/vitepress/issues/4556) Make all Markdown plugins optional

`P2` `actionable` `effort:L` · 👍 1 · 💬 1 · opened 1.4y ago, active 6d ago · labels: `stale`

Feature request to make all Markdown plugins optional for high customizability in theme building. Suitable for v2 release as breaking change.

**Assessment:** Valid feature request for customization. Maintainer confirmed it's planned, likely similar to VuePress v2 approach. Aligns with v2.0.0-alpha goals. Non-trivial work but strategic.

**Action:** Plan for v2.0.0 release; design plugin opt-in/opt-out configuration system similar to VuePress v2.

#### [#3520](https://github.com/vuejs/vitepress/issues/3520) Add `updatedCommitHash` to the `page` object

`P3` `likely-answered` `effort:S` · 👍 1 · 💬 6 · opened 2.4y ago, active 6d ago · labels: `has-workaround` `stale`

Request to add `updatedCommitHash` to the page object alongside `updatedAt` for footer display. Maintainer provided workaround using `transformPageData` and module augmentation.

**Assessment:** Valid feature request but low demand (1 upvote). Maintainer brc-dd suggested solving via `transformPageData` instead of core addition. Issue is 18+ months old with workaround documented.

**Action:** Close: answered—user successfully implemented workaround; feature has minimal demand and maintainer prefers plugin approach.

#### [#3641](https://github.com/vuejs/vitepress/issues/3641) feat: add validation when initializing VitePress configuration

`P2` `actionable` `effort:S` · 👍 0 · 💬 2 · opened 2.3y ago, active 6d ago · labels: `stale`

Add validation when initializing VitePress config to ensure the config directory is within project root; completes existing TODO.

**Assessment:** Valid request to complete incomplete validation (TODO by yyx990803). User has proposed PR #3640. Prevents misconfiguration that breaks projects. Low effort.

**Action:** Implement—review and merge PR #3640; completes unfinished validation logic with clear UX message.

#### [#4183](https://github.com/vuejs/vitepress/issues/4183) Allow accessing Vite mode from `config.ts`

`P2` `likely-answered` `effort:S` · 👍 0 · 💬 5 · opened 1.8y ago, active 6d ago · labels: `stale`

User requests ability to pass custom Vite mode flag (e.g., --mode foo) to vitepress dev/build commands and access it in config.ts.

**Assessment:** Valid feature request but maintainer brc-dd provided workarounds: using environment variables (MODE=foo) or VITE_-prefixed variables. Thread shows the issue author self-resolved by using vite.define in config. The feature has inherent merit but low demand (0 thumbs up).

**Action:** Close: answered—documented workaround in thread uses VITE_MODE env variables.

#### [#4763](https://github.com/vuejs/vitepress/issues/4763) Allow auto-loading additional config files along with custom loader

`P2` `actionable` `effort:M` · 👍 0 · 💬 0 · opened 1.1y ago, active 6d ago · labels: `build` `stale`

Request to allow both auto-loading of config files and custom loader simultaneously, with custom loader taking precedence.

**Assessment:** Feature gap identified by maintainer brc-dd; currently only one method can be used. References issue #4232 discussion. Valid request for config flexibility.

**Action:** Add config option to enable auto-loading even when custom loader exists; document precedence (custom > auto-loaded).

#### [#4803](https://github.com/vuejs/vitepress/issues/4803) Share data between vitepress config file and dynamic route paths loader

`P2` `actionable` `effort:M` · 👍 0 · 💬 11 · opened 1.0y ago, active 6d ago · labels: `stale`

Request for top-down data sharing between VitePress config and dynamic route path loaders, and content loaders. Users currently must fetch data twice or manually cache to files.

**Assessment:** Valid use case with maintainer (brc-dd) engagement. Maintainer provided a workaround using global VITEPRESS_CONFIG and demonstrated feasibility. Suggested passing config as ctx.config in loader context for type-safe access. Ready to implement.

**Action:** Implement: pass config context to dynamic route path loaders (type-safe, documented, modest effort, unblocks user patterns like sitemap generation from loader data).

#### [#5021](https://github.com/vuejs/vitepress/issues/5021) 关于 frontmatter 中的 permalink

`P2` `actionable` `effort:M` · 👍 0 · 💬 2 · opened 8mo ago, active 6d ago · labels: `stale`

Feature request to use frontmatter `permalink` field as the actual access URL instead of file-based routing, to avoid Chinese characters in URLs.

**Assessment:** Valid feature request motivated by avoiding non-ASCII characters in URLs (common for Chinese documentation). User has existing content with permalink already configured. Shows +1 from another user (BEWINDOWEB). Thread references issue #285. Indicates existing demand.

**Action:** Implement: add support for using frontmatter permalink as route URL to replace file-based routing

#### [#5193](https://github.com/vuejs/vitepress/issues/5193) Plugin hook transformPageData result is cached per page, making it impossible to inject shared data that changes independently of page content

`P2` `actionable` `effort:L` · 👍 0 · 💬 0 · opened 2mo ago, active 6d ago · labels: `stale`

Plugin transformPageData hook results are cached per-file, preventing plugins from injecting shared data that changes independently of page content (e.g., site hierarchy, breadcrumbs).

**Assessment:** Valid plugin API limitation. Detailed problem statement with real-world use case (hierarchical data, breadcrumbs). Author currently relying on undocumented internal clearCache() function. Requests either cache opt-out, lifecycle hook, or public clearCache API. Significant limitation for advanced plugins.

**Action:** Implement public cache invalidation API for plugins (e.g., plugin.clearCache() or clearPageDataCache hook).

#### [#4229](https://github.com/vuejs/vitepress/issues/4229) [feature] Request vitepress to automatically generate a sider from the file directory

`P3` `stale-candidate` `effort:L` · 👍 0 · 💬 0 · opened 1.8y ago, active 6d ago · labels: `stale`

Request for automatic sidebar and nav generation from file directory structure to reduce manual configuration burden for documentation authors.

**Assessment:** Valid feature request but no engagement from maintainers. Marked stale. User mentions related #572 and accumulates many notes. The feature would require significant work on the config system to auto-discover and refresh file structures.

**Action:** Close as stale with pointer to workarounds (data loading, buildEnd hook). No maintainer support signal.

#### [#4397](https://github.com/vuejs/vitepress/issues/4397) export/provide a  api/variable  can get all anchor 提供一个api 可以获取所有的 锚点，像是提供给 minisearch 的那种也可以

`P3` `possibly-fixed` `effort:S` · 👍 0 · 💬 1 · opened 1.6y ago, active 6d ago · labels: `stale`

Request to expose API/variable to retrieve all headings with anchors (e.g., for quick link lookup or feeding to minisearch).

**Assessment:** User wants programmatic access to outline data. shellRaining comment points to `outline.ts` function that does this but is not exported. Likely solvable by exporting existing internal function.

**Action:** Export outline/anchor retrieval from client composables; verify API shape matches minisearch needs.

#### [#4471](https://github.com/vuejs/vitepress/issues/4471) allow multiple different color modes in addition to light\|dark for themes

`P3` `stale-candidate` `effort:L` · 👍 0 · 💬 0 · opened 1.5y ago, active 6d ago · labels: `stale`

Request to support multiple color modes (light, dark, custom themes like 'luxury', 'business') in appearance config, not just dark/light toggle.

**Assessment:** Feature request for advanced theming. User has working workaround using @vueuse/core's useColorMode directly with custom `storageKey`. No maintainer feedback. Marked stale. Low priority: only needed for custom theme developers doing advanced color mode work.

**Action:** Close as stale—document workaround; consider v2 theming overhaul may address this.

#### [#4491](https://github.com/vuejs/vitepress/issues/4491) enhance the md file create time config ?

`P3` `stale-candidate` `effort:M` · 👍 0 · 💬 5 · opened 1.5y ago, active 6d ago · labels: `needs more discussion` `stale`

Request to add 'createTime' config option (file creation timestamp) similar to existing 'lastUpdated' to display file creation date in footer/header.

**Assessment:** Feature request with low priority. brc-dd clarified the ask (creation time alongside last update). Challenges: file system birthtimes unreliable across OS/editors, vim changes birthtimes. Maintainer shellRaining noted VitePress uses git timestamps for lastUpdated and suggested git-based solution or script workaround. No clear path forward without Git.

**Action:** Close as stale—document that file system birthtimes are unreliable; suggest git-based solution or frontmatter workaround.

## CLI (3)

#### [#5200](https://github.com/vuejs/vitepress/issues/5200) Support `--config` options in Vitepress cli

`P2` `actionable` `effort:M` · 👍 0 · 💬 4 · opened 38d ago, active 9d ago · unlabeled

Requests --config CLI option to specify custom config file path, enabling multi-environment deployments without CI-level file overwrites.

**Assessment:** Valid feature request with clear use case (4 environment configs, fragile CI workarounds). Author notes related issue #2084. Community suggested env vars + dynamic imports as workaround; author prefers explicit CLI flag for better UX and readability. Recent discussion (Jun 23-25).

**Action:** Implement --config flag for dev/build commands; document env-var workaround in meanwhile.

#### [#4073](https://github.com/vuejs/vitepress/issues/4073) support specifying `outDir` in `vitepress preview`

`P3` `likely-answered` `effort:S` · 👍 0 · 💬 1 · opened 1.9y ago, active 6d ago · labels: `has-workaround` `stale`

Request to support `--outDir` flag in `vitepress preview` command, similar to `vitepress build`.

**Assessment:** Maintainer brc-dd clarified that `vitepress preview` doesn't accept CLI flags besides base/port; users should configure outDir in config.js or use alternative servers like `npx serve`. This is documented behavior with a workaround.

**Action:** Close: answered; document that vitepress preview uses config-based outDir; mention npx serve as alternative.

#### [#4293](https://github.com/vuejs/vitepress/issues/4293) [Feature]: Allow launch code editor for source doc of current page when serving locally

`P3` `likely-answered` `effort:M` · 👍 0 · 💬 7 · opened 1.7y ago, active 6d ago · labels: `has-workaround` `stale`

Request to add 'Open in code editor' link to locally-served pages, similar to MDN's yari implementation, to quickly jump to source files.

**Assessment:** Valid feature. User inspired by MDN's approach. brc-dd provided multiple solutions: (1) Vue DevTools + vite-plugin-vue-devtools, (2) custom middleware plugin using open-editor package. Author confirmed the plugin approach works and solved their need. Thread effectively closes the issue with working workaround.

**Action:** Close as answered; document the open-editor middleware pattern in docs if not already covered.

## Types (1)

#### [#2595](https://github.com/vuejs/vitepress/issues/2595) Include `dts` file for `dist/client/theme-default/composables` and `components`

`P2` `needs-info` `effort:M` · 👍 2 · 💬 7 · opened 3.0y ago, active 2d ago · labels: `stale`

Feature request to include TypeScript definitions (.dts) for internal composables and components (sidebar, data, etc.) so custom theme overrides can be type-safe.

**Assessment:** Valid request with strong use case: official docs encourage overriding internal components, but types are missing causing vue-tsc failures. 2 thumbs up. Extended discussion where users show real examples (VPFeature customization, VPHomeFeatures reuse). Maintainer expressed concern about exposing internals; community argued overriding internals is official pattern so types should be public.

**Action:** Decide on strategy: either export internal component/composable types in DefaultTheme namespace for official support, or provide comprehensive guide for custom layouts using public APIs only.

## CSS / styling (3)

#### [#4425](https://github.com/vuejs/vitepress/issues/4425) Support native CSS layers

`P2` `possibly-fixed` `effort:M` · 👍 8 · 💬 7 · opened 1.6y ago, active 6d ago · labels: `stale`

Request CSS layer support in default theme base.css to prevent unlayered Tailwind CSS 4 styles from being overridden by base resets. Includes workaround patch.

**Assessment:** Valid request for CSS layers in v2. brc-dd indicated it's a v2 candidate during theme overhaul. Recent comment (mistic100, 2026-03) suggests issue may be resolved by #4905 merge. Should verify if Tailwind CSS 4 + VitePress v2 works now without patch.

**Action:** Verify against current main/v2; close if resolved by #4905, otherwise track for v2 theme work.

#### [#4877](https://github.com/vuejs/vitepress/issues/4877) Reduce layout  shifts with classic scrollbars

`P3` `actionable` `effort:S` · 👍 1 · 💬 1 · opened 11mo ago, active -1d ago · labels: `stale`

Layout shifts occur when navigating between pages with different scroll heights on browsers with classic (non-overlay) scrollbars, as the scrollbar appears/disappears.

**Assessment:** Valid CSS enhancement with recent maintainer engagement (2026-07-04). User (Almaz) proposed solution: add scrollbar-gutter: stable to default theme. Modern standard, excellent browser support (Chrome 94+, Firefox 97+, Safari 15.4+), improves Core Web Vitals (CLS). Ready to implement.

**Action:** Implement: add 'html { scrollbar-gutter: stable; }' to default theme CSS. Trivial change, significant UX improvement, no breaking changes.

#### [#5209](https://github.com/vuejs/vitepress/issues/5209) New CSS custom properties

`P2` `actionable` `effort:M` · 👍 0 · 💬 0 · opened 27d ago, active 27d ago · unlabeled

Requests CSS custom properties and rem-based units to enable easier font-size scaling and WCAG 1.4.12 compliance (line-height >= 1.5x).

**Assessment:** Valid accessibility and customization request. User wants: (1) rem units for proportional scaling, (2) custom properties for fine-tuning, (3) WCAG-compliant line-height values. No community pushback. Aligns with design system theming goals.

**Action:** Refactor default theme CSS to use rem units and custom properties; audit line-heights against WCAG 1.4.12.

## Head / meta (7)

#### [#3161](https://github.com/vuejs/vitepress/issues/3161) Accessing Vite asset URLs in head config

`P2` `upstream-blocked` `effort:L` · 👍 3 · 💬 5 · opened 2.7y ago, active 2d ago · labels: `build` `stale`

Request to access Vite asset URLs in head config (favicon, og:image) so assets get cache-busting hashes and missing assets are caught at build time.

**Assessment:** Valid feature blocked by upstream Vite API limitations (https://github.com/vitejs/vite/issues/7162). brc-dd indicated this needs Vite asset API exposure. Workarounds exist using context.assets but are fragile. Later comment hints at `__VITE_ASSET__` API. Recent user workaround via manifest reading.

**Action:** Track upstream Vite issue #7162. Revisit once Vite exposes asset API. Consider support for import() syntax in frontmatter once upstream unblocks.

#### [#2855](https://github.com/vuejs/vitepress/issues/2855) Support OpenSearch

`P2` `upstream-blocked` `effort:M` · 👍 2 · 💬 1 · opened 2.9y ago, active 2d ago · labels: `theme` `stale`

Request to support OpenSearch protocol for adding site to browser search engines and enabling tab-to-search functionality.

**Assessment:** Valid feature request. Maintainer brc-dd noted that OpenSearch suggestions/results require server support but querying can be supported. This is blocked on server-side infrastructure decisions.

**Action:** Document OpenSearch support requirements. Defer full implementation until server architecture is finalized for 2.0.

#### [#4892](https://github.com/vuejs/vitepress/issues/4892) Add `created` time

`P2` `actionable` `effort:M` · 👍 2 · 💬 2 · opened 11mo ago, active 6d ago · labels: `stale`

Add `created` time metadata to pages in addition to `lastUpdated`. User requests creation date for articles, referencing a prior issue #1891 from 2023 that remains unimplemented.

**Assessment:** Valid feature request with clear use case: migration scenarios break lastUpdated, and JSON-LD schema generation needs datePublished. Low traction (2 thumbs up) but foundational for documentation use. No maintainer response yet.

**Action:** Implement — moderate effort, useful for documentation workflows and schema.org compliance. Use git log with --diff-filter=A to extract creation date.

#### [#3758](https://github.com/vuejs/vitepress/issues/3758) Support using dynamic routes params in the frontmatter

`P2` `possibly-fixed` `effort:M` · 👍 1 · 💬 2 · opened 2.2y ago, active 6d ago · labels: `stale`

Dynamic route parameters cannot be used in frontmatter (e.g. description: {{ $params.description }}); interpolation fails and renders [object Object] in HTML meta tags.

**Assessment:** Valid feature request for dynamic routes. User attempted parameter interpolation in frontmatter YAML but it doesn't work. Related to issue #2637. Thread suggests SSR workaround (client-side patching) but indicates this doesn't work well. Legitimate gap for dynamic routes with param-driven metadata.

**Action:** Support YAML interpolation for route params in frontmatter; or document data loaders as recommended approach for param-driven metadata.

#### [#4793](https://github.com/vuejs/vitepress/issues/4793) Allow for raw HTML in extra head.

`P2` `actionable` `effort:M` · 👍 1 · 💬 0 · opened 1.0y ago, active 6d ago · labels: `stale`

Request to allow raw HTML injection in frontmatter `head` property to support Jinja2 template generation workflow for release notes.

**Assessment:** Specific use case: generating Jinja2 templates from VitePress that tools like towncrier can process. User proposes `RAW_VALUE` marker pattern. 1 reaction. Niche but valid need.

**Action:** Evaluate if raw HTML head injection aligns with design; implement marker or alternative syntax to pass through unprocessed content.

#### [#4168](https://github.com/vuejs/vitepress/issues/4168) Support add importmap script into head

`P2` `actionable` `effort:M` · 👍 0 · 💬 5 · opened 1.8y ago, active 6d ago · labels: `stale`

Support adding importmap scripts to head in correct position (before other scripts); current head config adds to end causing importmap to fail.

**Assessment:** Valid technical issue. importmap scripts must appear before module scripts load (browser constraint). brc-dd suggested moving importmap above other scripts by default. Workaround: use Vite plugin transformHtml at config time. Issue includes reproduction and workarounds.

**Action:** Implement special handling for importmap scripts in head: inject at top, before other scripts.

#### [#5190](https://github.com/vuejs/vitepress/issues/5190) Allow arbitrary attributes on inline script injected by site-config.

`P3` `likely-answered` `effort:M` · 👍 0 · 💬 0 · opened 2mo ago, active 6d ago · labels: `stale`

Requests support for arbitrary attributes (e.g., data-cfasync) on the inline theme-flicker script injected by appearance config.

**Assessment:** Valid use case (Cloudflare Rocket Loader compatibility). Author acknowledges workaround exists via transformHtml hook and considers it acceptable. Feature nice-to-have but not critical. Marked stale.

**Action:** Close: use-case addressed via documented transformHtml workaround; limited scope justifies deferral.

## Accessibility (2)

#### [#3804](https://github.com/vuejs/vitepress/issues/3804) sidebar: use native <details> for collapsible groups

`P2` `actionable` `effort:M` · 👍 0 · 💬 1 · opened 2.2y ago, active 6d ago · labels: `theme` `stale`

Sidebar collapsible groups use custom toggle logic missing Space key support; request to use native <details> element for better accessibility and keyboard behavior.

**Assessment:** Valid accessibility improvement. User points out native <details> handles Space, Enter, and click but custom impl only handles click and Enter. Reduces maintainability burden. Recent comment (2025-07-17) points to PR #4847 that addresses this.

**Action:** Verify PR #4847 status; if merged, close as fixed; if pending, prioritize for 2.0.

#### [#5056](https://github.com/vuejs/vitepress/issues/5056) Improve screen reader accessibility

`P2` `actionable` `effort:M` · 👍 0 · 💬 0 · opened 7mo ago, active 6d ago · labels: `stale`

Default theme needs multiple accessibility improvements: aria-current on active nav, alt text on logo, better ARIA for search popup and social links, semantic list structure.

**Assessment:** Valid accessibility feature request with specific, implementable items. No maintainer feedback yet. All suggested improvements align with WCAG best practices.

**Action:** Implement accessibility improvements: add aria-current="page" to active navigation items, alt text to logo, improve search popup ARIA labeling, structure nav/social links as semantic lists.

## Other (8)

#### [#794](https://github.com/vuejs/vitepress/issues/794) Add "Index Page" feature

`P2` `likely-answered` `effort:M` · 👍 13 · 💬 5 · opened 4.1y ago, active 2d ago · labels: `stale`

Request for an "Index Page" feature that lists all subpage headings (e.g., API reference pages with organized sections).

**Assessment:** Valid enhancement (13 thumbs_up). yyx990803 (Evan You) answered in 2022-08 with the static data import (`.data.ts`) feature as the solution. Subsequent comments (2023-05+) indicate users found the pattern but requested clearer documentation on file location and import syntax. Last comment (2024-06) suggests issue can be closed.

**Action:** Close: feature addressed by data-loading API; ensure docs clarify .data.ts location, import syntax (no extension), and glob patterns.

#### [#4590](https://github.com/vuejs/vitepress/issues/4590) Support llms.txt generation

`P2` `actionable` `effort:L` · 👍 12 · 💬 8 · opened 1.3y ago, active 31d ago · labels: `has-workaround`

Feature request for native `llms.txt` generation to optimize documentation for LLM consumption. Plugin `vitepress-plugin-llms` exists as alternative.

**Assessment:** Valid feature request with 12 thumbs up and emerging LLM use case. Maintainer and community discussing core vs. plugin approach. Plugin author argues for plugin independence; maintainer supports core integration for performance/convenience. No decision yet but active discussion suggests interest.

**Action:** Evaluate demand and decide: accept as core feature for v2 (integrate with evaluated markdown for efficiency) or recommend plugin with better documentation.

#### [#4160](https://github.com/vuejs/vitepress/issues/4160) Client-side redirects

`P2` `actionable` `effort:M` · 👍 6 · 💬 12 · opened 1.9y ago, active 6d ago · labels: `stale`

Request client-side redirect support (wildcard and static) to handle versioned docs (/v2/... redirects from /...) for static hosting on GitHub Pages.

**Assessment:** Valid use case with 6 thumbs_up and multiple supportive comments. brc-dd noted wildcard redirects hard in SSG but static redirects feasible. Several workarounds provided in thread (symlinks, 404 redirect via Layout.vue, Cloudflare proxy). Remains open as feature request; some debate on feasibility.

**Action:** Implement static (non-wildcard) redirects config. Wildcard support may require architectural changes.

#### [#1450](https://github.com/vuejs/vitepress/issues/1450) Expose an API to retrieve the frontmatter and outline of any page

`P2` `likely-answered` `effort:S` · 👍 0 · 💬 6 · opened 3.7y ago, active 2d ago · labels: `docs` `stale`

Request API to retrieve frontmatter and outline of any page for generating dynamic index pages like FAQ lists.

**Assessment:** Feature request answered in-thread. Maintainer brc-dd showed undocumented API: importing `__pageData` from markdown files. Later comments show eager glob technique for listing multiple pages. Solution exists but not documented.

**Action:** Close: answered. Consider documenting the `__pageData` import pattern or exposing a stable public API for this use case.

#### [#2794](https://github.com/vuejs/vitepress/issues/2794) Disable automatically set direction in `<html>`

`P3` `actionable` `effort:S` · 👍 0 · 💬 2 · opened 2.9y ago, active 2d ago · labels: `theme` `stale`

Request to allow disabling automatic dir attribute setting on html element so user can control it dynamically at runtime.

**Assessment:** Valid use case for RTL toggle feature in UI library docs. User has working workaround but experiences dir flipping on each route change. Maintainer asked about static config approach; user clarified runtime toggle need.

**Action:** Add config option (e.g., dir: false) to skip auto-setting dir attribute, allowing runtime control via useData or router hooks.

#### [#3037](https://github.com/vuejs/vitepress/issues/3037) A preview can be displayed when the mouse hovers over a link.

`P3` `actionable` `effort:L` · 👍 0 · 💬 0 · opened 2.8y ago, active 2d ago · labels: `stale`

Request for Wikipedia/Obsidian-style link preview popups on hover.

**Assessment:** Valid feature request with visual examples. No discussion in thread. Nice-to-have enhancement but non-essential. Could be implemented as a theme component or plugin. Low priority compared to bugs.

**Action:** Consider for backlog: could be implemented as optional theme component or external plugin; not blocking.

#### [#4059](https://github.com/vuejs/vitepress/issues/4059) Create a "awesome-vitepress" repository

`P3` `stale-candidate` `effort:L` · 👍 0 · 💬 1 · opened 2.0y ago, active 6d ago · labels: `stale`

Request to create an official awesome-vitepress repository to list VitePress plugins and themes, similar to awesome-vite and awesome-vuepress.

**Assessment:** Feature request for community/ecosystem management, not a core product feature. Low priority and significant effort to maintain. Similar awesome-* repos exist but are not official. Reasonable suggestion but not critical for VitePress functionality.

**Action:** Close: out of scope for core VitePress repo; consider linking to community awesome-vitepress resources in docs instead.

#### [#4234](https://github.com/vuejs/vitepress/issues/4234) Add Taxonomies

`P3` `likely-answered` `effort:L` · 👍 0 · 💬 1 · opened 1.8y ago, active 6d ago · labels: `stale`

Request for taxonomy/tagging system similar to Hugo for categorizing and relating content across multiple dimensions (actors, genres, etc).

**Assessment:** Valid feature request from someone migrating from Hugo. Author later (Nov 2024) discovered VitePress data loading API with buildEnd hook provides a workaround, reducing the priority for core support.

**Action:** Close as answered; point to data loading and buildEnd() hooks for achieving taxonomy patterns.

