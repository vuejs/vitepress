# Bugs (127)

> Part of the [open-issue triage](./README.md) of vuejs/vitepress — snapshot 2026-07-04, generated by Claude Code. Grouped by area, sorted by priority then 👍.

## Markdown / content pipeline (13)

#### [#4625](https://github.com/vuejs/vitepress/issues/4625) Including non-existent regions includes entire file content

`P1` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 0 · opened 1.3y ago, active 6d ago · labels: `needs more discussion` `stale`

File inclusion with non-existent regions (`<!--@include: file.md#notexist-->`) includes the entire file instead of including nothing, breaking modular documentation systems that rely on selective region inclusion.

**Assessment:** Valid and critical bug with clear reproduction and stackblitz example. User's use case (modular docs with slot files) is legitimate and currently broken. No comments yet; issue is well-documented and ready for action.

**Action:** Actionable: change include fallback behavior to include nothing (or warn) when region is not found instead of silently including entire file.

#### [#3133](https://github.com/vuejs/vitepress/issues/3133) H2 anchors generated incorrectly if content generated dynamically

`P2` `actionable` `effort:M` `repro:yes` · 👍 1 · 💬 3 · opened 2.7y ago, active 2d ago · labels: `bug: pending triage` `stale`

H2 anchor IDs generated incorrectly when headers are dynamically generated with v-for loops; IDs become 'repo-name' instead of rendered text like 'client', 'server', 'socket'.

**Assessment:** Valid bug with clear repro (Stackblitz). brc-dd indicated this is a limitation of markdown-to-Vue conversion (cannot infer IDs from dynamic content). User requested a build-time warning for invalid anchors, which is a reasonable enhancement.

**Action:** Verify this is documented limitation. Consider if a build warning for dynamic headers would be helpful; otherwise close with documentation link.

#### [#3147](https://github.com/vuejs/vitepress/issues/3147) createContentLoader do not render Vue components in html output

`P2` `likely-answered` `effort:S` `repro:yes` · 👍 1 · 💬 4 · opened 2.7y ago, active 2d ago · labels: `docs` `wont fix` `stale`

createContentLoader with 'render: true' does not render Vue components; they appear as custom tags in the HTML output.

**Assessment:** Marked 'wont fix' by maintainer brc-dd who clarified that createContentLoader is designed to convert markdown to vue-html only, not to process Vue syntax. User seeking guidance on alternatives. Latest comment suggests this limitation reduces usefulness. Valid use case but documented design limitation.

**Action:** Close: Design limitation. Document alternatives or improve API documentation for expected behavior.

#### [#3428](https://github.com/vuejs/vitepress/issues/3428) The anchor point of markdown file is incorrect after using vue component

`P2` `possibly-fixed` `effort:M` `repro:yes` · 👍 1 · 💬 14 · opened 2.5y ago, active 6d ago · labels: `stale`

Anchor links scroll to incorrect position when markdown includes Vue components or images without dimensions, causing layout shift.

**Assessment:** Valid but partially user-error: issue caused by missing image width/height (CLS violation). Maintainer brc-dd correctly diagnosed as layout shift. Long thread (14 comments) discusses mitigations including postponing scrollTo() and image dimension plugins. No definitive fix offered; workaround is add dimensions to images.

**Action:** Document best practice (image dimensions or markdown-it-image-size plugin); consider if VitePress can auto-delay scrollTo() until LCP.

#### [#3007](https://github.com/vuejs/vitepress/issues/3007) md footnote soft links get rendered onto custom containers

`P2` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 2 · opened 2.8y ago, active 2d ago · labels: `stale`

Markdown footnote reference links inside custom container directives render in the wrong location.

**Assessment:** Valid bug with reproduction steps. brc-dd flagged it as hard to fix due to the interaction between markdown-it footnote plugin and custom container parsing. Thread shows limited investigation but confirms the issue.

**Action:** Investigate: markdown-it plugin ordering or container parsing may need refactoring to preserve footnote references in container titles/content.

#### [#3045](https://github.com/vuejs/vitepress/issues/3045) Problems with function result display when using Vue

`P2` `possibly-fixed` `effort:M` `repro:no` · 👍 0 · 💬 4 · opened 2.8y ago, active 2d ago · labels: `build` `stale`

Code blocks render twice (for dark and light modes), causing incorrect display on one theme. The copy button shows the correct value but the displayed output is wrong.

**Assessment:** Valid issue with clear cause identified by brc-dd: code blocks render twice for theme support. Issue is referenced as fixable by #3006. Marked stale but the problem is still relevant for dual-theme setups.

**Action:** Verify against 2.0.0-alpha; check if #3006 was merged and whether dual rendering persists in current version.

#### [#3511](https://github.com/vuejs/vitepress/issues/3511) Bug: cannot properly slugify badge with children

`P2` `likely-answered` `effort:S` `repro:yes` · 👍 0 · 💬 3 · opened 2.4y ago, active 6d ago · labels: `build` `stale`

Bug: Badge elements with children are not properly excluded from header anchor slugs; custom slugify config no longer works.

**Assessment:** Reported as regression. Maintainer provided working workaround using `getTokensText()` in anchor config (Feb 2024). Root cause is markdown-it behavior treating `<badge>bar</badge>` as text + html_inline tokens. User found solution functional.

**Action:** Document the getTokensText workaround; consider adding Badge exclusion as built-in anchor option.

#### [#3602](https://github.com/vuejs/vitepress/issues/3602) bug: GitHub-flavored Alerts label are not translatable

`P2` `actionable` `effort:S` `repro:no` · 👍 0 · 💬 0 · opened 2.4y ago, active 6d ago · labels: `stale`

GitHub-flavored Alerts plugin doesn't accept or apply custom label configuration, unlike the containers plugin.

**Assessment:** Real bug: plugin parameter exists but is not wired to markdown config. No maintainer comments. Affects i18n / multi-language sites.

**Action:** Fix—wire gitHubAlertsPlugin config option to markdown config; similar pattern to container labels.

#### [#4216](https://github.com/vuejs/vitepress/issues/4216) Prohibit parsing<>tags in markdown

`P2` `needs-info` `effort:M` `repro:yes` · 👍 0 · 💬 4 · opened 1.8y ago, active 6d ago · labels: `needs more discussion` `stale`

Markdown tables with unescaped angle brackets (e.g., \| -s <serialNumber>\|) trigger Vue parsing errors ([plugin:vite:vue] Element is missing end tag).

**Assessment:** Valid bug: VitePress/markdown-it treats <> as Vue component syntax in table cells, breaking standard Markdown that works in Typora/other tools. Thread suggests disabling Vue parsing globally or using workarounds (backticks, &lt;). Recent comment (2025-05) confirms ongoing issue; another user added a plugin solution. Marked stale.

**Action:** Verify: document that `<text>` in tables requires escaping or backticks; consider adding markdown config option to disable Vue parsing in tables or globally.

#### [#4300](https://github.com/vuejs/vitepress/issues/4300) Unescaped code blocks syntax highlight doesn't work

`P2` `likely-answered` `effort:S` `repro:yes` · 👍 0 · 💬 3 · opened 1.7y ago, active 6d ago · labels: `needs more discussion` `stale`

Syntax highlighting doesn't work when using unescaped code blocks with dynamic content from string variables or data loaders.

**Assessment:** Maintainer provided workaround: use createMarkdownRenderer to pre-render HTML and inject via v-html, or import code files directly instead of embedding as strings. User confirmed workaround works. Thread shows this is a limitation of the feature, not a bug. May benefit from better docs on createMarkdownRenderer API.

**Action:** Close: answered — maintainer provided working workaround and explained limitations.

#### [#4757](https://github.com/vuejs/vitepress/issues/4757) include different title by one  file trigger error  ，同时包含一个文件中2个不同的段落 报错了 <!--@include: ../basic.md#query_request_suggestion -->

`P2` `needs-repro` `effort:M` `repro:no` · 👍 0 · 💬 0 · opened 1.1y ago, active 6d ago · labels: `bug: pending triage` `stale`

Error 'User defined `id` attribute is not unique' when including multiple sections from same file using <!--@include> syntax with different anchor IDs.

**Assessment:** Clear reproduction steps provided but no code sample or repo. Chinese/English report. No maintainer investigation yet. Appears to be validation error in include processing.

**Action:** Ask for minimal reproduction file or verify if anchor inclusion logic has duplicate ID validation bug.

#### [#5015](https://github.com/vuejs/vitepress/issues/5015) BUG：Tags with side effect (<script> and <style>) are ignored in client component templates.

`P2` `possibly-fixed` `effort:S` `repro:yes` · 👍 0 · 💬 3 · opened 8mo ago, active 6d ago · labels: `stale`

Bug when using markdown-it-mathjax3 v5.2.0 with VitePress: script and style tags with side effects are ignored in client component templates, causing rendering errors for math formulas.

**Assessment:** Legitimate bug with clear reproduction steps. However, maintainer brc-dd quickly identified the root cause as markdown-it-mathjax3 v5.x being incompatible, recommending downgrade to v4.x. User confirmed the workaround fixes the issue. Not a VitePress bug; upstream dependency issue.

**Action:** Document: markdown-it-mathjax3 v5.x incompatibility - recommend v4.x; consider upstream coordination

#### [#5079](https://github.com/vuejs/vitepress/issues/5079) Anchor links are not resolving to the correct scroll position

`P2` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 11 · opened 6mo ago, active 6d ago · labels: `build` `stale`

Images without explicit width/height cause layout shift (CLS), making anchor links scroll to wrong positions. Affects Chromium; Firefox/Safari unaffected.

**Assessment:** Valid bug related to cumulative layout shift. Chromium-specific (linked to https://issues.chromium.org/issues/40753240). Maintainer provided workaround with markdown-it plugin and CSS adjustments.

**Action:** Document workaround solution in guides; consider improving image sizing plugin defaults or adding guidance for aspect-ratio handling.

## Default theme (25)

#### [#3878](https://github.com/vuejs/vitepress/issues/3878) Outline marker does not move to correct item

`P1` `actionable` `effort:S` `repro:yes` · 👍 0 · 💬 1 · opened 2.1y ago, active 6d ago · labels: `theme` `stale`

Outline/TOC marker does not highlight correctly when clicking an item near the end of the page. Outline indicator moves to wrong heading.

**Assessment:** Valid bug with clear repro on vitepress.dev reference page. User notes behavior: clicking "$frontmatter" shows marker at "$params" instead. Likely off-by-one error in scroll-to-active logic. Possible duplicate of #3449 per user.

**Action:** Verify against #3449 (possible duplicate). If distinct, debug scroll-to-active margin calculation for items near page end. (duplicate of #3449)

#### [#4359](https://github.com/vuejs/vitepress/issues/4359) Local navigation dropdown misplaced without sidebar

`P1` `actionable` `effort:S` `repro:yes` · 👍 0 · 💬 5 · opened 1.6y ago, active 6d ago · labels: `theme` `contribution welcome` `stale`

Local navigation outline dropdown is misaligned when sidebar is disabled, appearing offset by sidebar width.

**Assessment:** Valid bug with Stackblitz repro. brc-dd correctly identified root cause: `--vp-sidebar-width` CSS variable should be 0 when no sidebar. Another user (yuxi-ovo) claimed to have fixed it in PR #4393.

**Action:** Verify fix status in PR #4393; if merged, mark as possibly-fixed; otherwise coordinate merge.

#### [#4897](https://github.com/vuejs/vitepress/issues/4897) Reduce layout shifts caused by LocalNav for devices with width 960px - 1280px

`P1` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 5 · opened 11mo ago, active 6d ago · labels: `theme` `stale`

LocalNav component causes layout shifts (CLS) on devices with 960px–1280px viewport width due to dynamic client-side injection. Component renders with `empty` class on SSR but becomes populated during hydration.

**Assessment:** Valid bug with performance impact (layout shift). brc-dd confirmed the issue and suggested defaulting hasLocalNav to true on SSR for most pages. Implementation requires detecting headers at build time and setting an HTML class accordingly.

**Action:** Implement — add build-time detection of header anchors and set class on <html> if headers exist and outline/aside not disabled.

#### [#2535](https://github.com/vuejs/vitepress/issues/2535) Cannot change prev/next labels when disabling them globally

`P2` `actionable` `effort:S` `repro:no` · 👍 2 · 💬 1 · opened 3.0y ago, active 2d ago · labels: `theme` `stale`

Bug: Cannot change prev/next link labels while disabling them globally. Related to #2317, the same frontmatter attributes control both disabling and labeling, creating a conflict.

**Assessment:** Valid bug with clear repro steps. User has legitimate use case: disable globally, enable with custom labels on specific pages. 2 thumbs up, one commenter interested. Relates to recent change (#2317).

**Action:** Separate disabling logic from label customization in theme config; allow per-page override of labels independently of enable/disable.

#### [#2208](https://github.com/vuejs/vitepress/issues/2208) The underline position of 'VPDocAsideOutline' on the right is incorrect

`P2` `possibly-fixed` `effort:S` `repro:yes` · 👍 0 · 💬 1 · opened 3.2y ago, active 2d ago · labels: `theme` `stale`

Underline position of active heading in doc outline (VPDocAsideOutline) is incorrect when html has scroll-behavior: smooth; throttling function causes misalignment.

**Assessment:** Valid but narrow bug. Maintainer (brc-dd) stated smooth scrolling isn't supported. Issue is that throttleAndDebounce doesn't account for smooth scroll duration. May be fixed by not supporting smooth scroll or by improving scroll event handling.

**Action:** Document that smooth scrolling unsupported; or implement scroll detection that works with smooth scroll timing.

#### [#2842](https://github.com/vuejs/vitepress/issues/2842) When browser width is between 840px and 770px a horizontal scrollbar appears when there are too many items

`P2` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 0 · opened 2.9y ago, active 2d ago · labels: `stale`

Horizontal scrollbar appears in navbar when browser width is between 840px and 770px with many nav items. Should collapse to overflow menu instead.

**Assessment:** Valid bug with clear repro steps. Affects responsive layout in a specific breakpoint range. No maintainer response in thread. Issue is old but the underlying layout problem is genuine.

**Action:** Verify in current version; fix responsive navbar breakpoint logic to properly collapse items to overflow menu.

#### [#2885](https://github.com/vuejs/vitepress/issues/2885) VPLocalSearch/VPAlgoliaSearch box `⌘` is glitched on Safari on Mac

`P2` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 6 · opened 2.8y ago, active 2d ago · labels: `help wanted` `theme` `stale`

Search box ⌘ (Command) key character displays with misalignment glitch in Safari on macOS, particularly at lower zoom levels. Related to gradient and blur filter rendering.

**Assessment:** Valid CSS rendering bug specific to Safari. brc-dd reproduced on Safari TP and identified the root cause: blur filter with radius > 68px causes rendering issues on Safari. Partial fix applied (gradient issue resolved). Remains a cosmetic but noticeable issue.

**Action:** Apply CSS fix for keyboard shortcut display: reduce blur radius or use alternative filtering approach compatible with Safari rendering engine.

#### [#2930](https://github.com/vuejs/vitepress/issues/2930) themeConfig.socialLinks.icon

`P2` `actionable` `effort:S` `repro:yes` · 👍 0 · 💬 1 · opened 2.8y ago, active 2d ago · labels: `theme` `a11y` `stale`

Social link icons in theme config lack `width` and `height` attributes on SVG elements, causing Google Search Console mobile suitability verification to fail.

**Assessment:** Valid bug with clear reproduction. User provides exact SVG format needed. Single comment asks clarifying question but no resolution. Quick fix to add width/height attributes to default theme icon SVGs.

**Action:** Add width="24" height="24" attributes to SVG icons in default theme social links.

#### [#3030](https://github.com/vuejs/vitepress/issues/3030) `permalink: undefined` in `mardown` settings disables Outline

`P2` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 2 · opened 2.8y ago, active 2d ago · labels: `theme` `has-workaround` `stale`

Setting markdown.anchor.permalink to undefined hides permalinks but also breaks the outline scroll-tracking feature.

**Assessment:** Valid bug with workaround (CSS visibility:hidden). brc-dd acknowledged the issue and stated the current implementation depends on anchor elements being present. This is a design limitation that should be addressed by decoupling permalink rendering from scroll tracking.

**Action:** Refactor: decouple anchor element presence from permalink visibility to allow hiding symbols without breaking outline tracking.

#### [#3194](https://github.com/vuejs/vitepress/issues/3194) TOC aside height problem

`P2` `actionable` `effort:S` `repro:yes` · 👍 0 · 💬 0 · opened 2.7y ago, active 2d ago · labels: `theme` `stale`

Table of contents (TOC) aside has fixed height causing dead zone in large viewports (>1200px). The aside extends beyond its container, blocking user interaction.

**Assessment:** Valid layout bug with screenshots provided. Affects default theme CSS. The fix is straightforward—change min-height to max-height or adjust layout containment. No blocker comments from maintainers yet.

**Action:** Fix by adjusting CSS min-height to max-height on aside or ensure proper layout containment within document flow.

#### [#3393](https://github.com/vuejs/vitepress/issues/3393) New local navbar design breaks ubiquitous header styling

`P2` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 4 · opened 2.5y ago, active 6d ago · labels: `theme` `stale`

New split header design (VPNav + VPLocalNav) breaks unified header styling (e.g., blur effects) that worked before PR #3359.

**Assessment:** Valid regression from recent theme refactor. User shows clear example of broken blur effect. Maintainer brc-dd pinged kiaking (theme author) but no follow-up. Secondary complaint (giladgd) about curtain removal also noted but addressed by elringus as subjective UX change.

**Action:** Investigate wrapping both VPNav and VPLocalNav in single header element or expose CSS custom property for coordinated styling.

#### [#3449](https://github.com/vuejs/vitepress/issues/3449) Cursor is misaligned in the article directory

`P2` `needs-repro` `effort:S` `repro:yes` · 👍 0 · 💬 0 · opened 2.5y ago, active 6d ago · labels: `theme` `stale`

Bug: cursor misalignment when clicking on sidebar navigation items (Windows 10, VitePress 1.0.0-rc.36).

**Assessment:** Specific bug report with screenshot showing cursor offset. No maintainer response or repro confirmation. Marked stale. May be Windows/browser-specific rendering issue.

**Action:** Request repro on latest 1.x stable; check if fixed in 2.0.0-alpha or was OS/theme-specific.

#### [#3811](https://github.com/vuejs/vitepress/issues/3811) 'Return to top' button is always visible in custom layout page

`P2` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 0 · opened 2.2y ago, active 6d ago · labels: `theme` `stale`

'Return to top' button always visible on custom layout pages in mobile view, even when already at top of page.

**Assessment:** Valid bug with clear repro steps. Similar to #2312 but specific to custom layouts on narrow viewports. No maintainer response. Button visibility state not properly tied to scroll position.

**Action:** Fix return-to-top button visibility logic; ensure scroll position check works correctly in custom layouts on mobile.

#### [#3893](https://github.com/vuejs/vitepress/issues/3893) Inconsistency in Custom Containers use of Color Functions

`P2` `actionable` `effort:S` `repro:no` · 👍 0 · 💬 3 · opened 2.1y ago, active 6d ago · labels: `theme` `stale`

CSS variables `--vp-c-note-*` exist but are not used by custom containers. Setting them has no effect; containers remain hardcoded to default colors.

**Assessment:** Valid bug confirmed by brc-dd. Root cause identified: variables were added (#3482) but not actually used in container styles. brc-dd committed to fixing this. Clear fix scope.

**Action:** brc-dd to implement the fix using the CSS variables that were already defined.

#### [#4108](https://github.com/vuejs/vitepress/issues/4108) Firefox: after clicking sidebar and nav links, cursor sticks on `default`

`P2` `actionable` `effort:S` `repro:yes` · 👍 0 · 💬 0 · opened 1.9y ago, active 6d ago · labels: `theme` `stale`

Firefox browser bug: cursor remains `default` instead of `pointer` on sidebar and nav links after clicking them; affects SPA navigation.

**Assessment:** Valid Firefox-specific bug with clear reproduction steps on vitepress.dev and screen recordings. User identified workaround (add margin between links). Likely a CSS/cursor handling issue with dynamic link updates.

**Action:** Apply margin-based workaround or investigate cursor reflow issue in Firefox after SPA navigation.

#### [#4732](https://github.com/vuejs/vitepress/issues/4732) [Suggestion] Avoid using `social-links-list` class, as it may be blocked by EasyList

`P2` `actionable` `effort:S` `repro:yes` · 👍 0 · 💬 3 · opened 1.2y ago, active 6d ago · labels: `theme` `stale`

CSS class 'social-links-list' is blocked by EasyList ad blocker filter, causing social links to not display at medium viewport width.

**Assessment:** Valid bug: social-links-list class is on EasyList blocklist, causing false-positive filtering in ads blockers (uBO, Adblock Plus). Affects users with Fanboy social filter enabled. Maintainer brc-dd notes breaking change risk (external customizations depend on class names) and suggests deferring to v2 theme rewrite.

**Action:** Rename 'social-links-list' to 'social-links-items' or similar non-blocklisted name; document as breaking change for v2.

#### [#4841](https://github.com/vuejs/vitepress/issues/4841) `getSidebar`'s matching logic is buggy: Does not support non-strictly hierarchical sidebar config objects (with subtree lifting via duplication)

`P2` `actionable` `effort:S` `repro:yes` · 👍 0 · 💬 3 · opened 12mo ago, active 6d ago · labels: `bug: pending triage` `stale`

getSidebar() matching logic is buggy with non-strictly-hierarchical sidebar configs. Path "/api-examples.md" incorrectly matches sidebar key "/a" because it starts with "/a", causing wrong sidebar to render.

**Assessment:** Confirmed bug. User provided detailed reproduction (repo + steps). Maintainer suggested immediate workaround (add trailing slash to keys). Underlying issue: getSidebar() does prefix-only matching without checking for path boundaries. User questioned whether keys should auto-normalize trailing slashes.

**Action:** Fix: normalize sidebar keys to end with "/" on config load, or improve getSidebar matching to require path boundary after prefix. Verify workaround applies to docs.

#### [#4884](https://github.com/vuejs/vitepress/issues/4884) Layout shift when search modal toggle

`P2` `needs-info` `effort:M` `repro:yes` · 👍 0 · 💬 0 · opened 11mo ago, active 6d ago · labels: `theme` `stale`

Layout shift occurs when search modal opens/closes because VitePress locks body scroll (correct) but scrollbar disappears (causing shift). Affects both local search and Algolia search.

**Assessment:** Real, reproducible issue shown in video. No comments or maintainer input. Related to #4877 (scrollbar layout shifts). Same root cause as #4877 but triggered by modal toggle instead of navigation.

**Action:** Combine with #4877: apply scrollbar-gutter: stable fix to resolve both; or add padding-right: calc(var(--scrollbar-width)) to body when modal is open.

#### [#4972](https://github.com/vuejs/vitepress/issues/4972) When the hamburger button is expanded in mobile view, the display is not as expected

`P2` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 1 · opened 9mo ago, active 6d ago · labels: `theme` `stale`

On mobile, if user scrolls slightly before opening hamburger menu, page content below shows through the expanded menu instead of being completely covered.

**Assessment:** Valid mobile UI bug with video repro. User confirmed still exists as of March 2026. Z-index or position/overflow issue with menu overlay. No maintainer response yet.

**Action:** Investigate z-index, position, and overflow properties on mobile sidebar overlay; check recent theme CSS changes.

#### [#5112](https://github.com/vuejs/vitepress/issues/5112) No `target=_self` on the next page button.

`P2` `actionable` `effort:S` `repro:yes` · 👍 0 · 💬 0 · opened 5mo ago, active 6d ago · labels: `theme` `stale`

The next/previous page navigation buttons do not respect the `target: '_self'` option that is available for sidebar links, causing inconsistent behavior between sidebar and pagination controls.

**Assessment:** Valid bug with clear reproduction steps and config example. Touches the pagination component rendering in the default theme. No maintainer response; stale tagged but issue is straightforward.

**Action:** Implement: add `target` attribute support to next/prev page buttons in default theme layout component.

#### [#5120](https://github.com/vuejs/vitepress/issues/5120) The copy button obstructs the text selection and code copying within the code blocks in mobile view.

`P2` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 1 · opened 4mo ago, active 6d ago · labels: `theme` `stale`

The copy button in code blocks obstructs text selection on mobile devices, preventing users from selecting and copying code manually despite having sufficient screen space.

**Assessment:** Valid mobile UX bug with video reproduction. Reporter confirmed it has not been fixed in latest version (thread comment 2026-06-27). Requires CSS/layout adjustment to code block styling on mobile.

**Action:** Implement: reposition or hide copy button on mobile viewports, or use CSS to prevent overlap with selectable text.

#### [#5213](https://github.com/vuejs/vitepress/issues/5213) Inconsistent mobile/desktop breakpoint between NavBar and Search Component at 768px

`P2` `actionable` `effort:S` `repro:yes` · 👍 0 · 💬 1 · opened 25d ago, active 16d ago · labels: `bug: pending triage`

Navigation Bar and Search Component use inconsistent mobile/desktop breakpoints at 768px (NavBar switches at 768px, Search at 769px). This causes mismatched UI state where the header is in desktop mode but search behaves like mobile.

**Assessment:** Valid bug with clear reproduction (screenshots at 767px/768px/769px). Community member (pupuking723) offered to fix by unifying breakpoints. Well-scoped issue suitable for immediate implementation.

**Action:** Assign to contributor or implement fix to align both components to use min-width: 769px / max-width: 768px boundary.

#### [#4579](https://github.com/vuejs/vitepress/issues/4579) 侧边栏不会自动定位到当前页面所对应的链接主题。

`P3` `needs-info` `effort:M` `repro:no` · 👍 2 · 💬 0 · opened 1.4y ago, active 6d ago · labels: `theme` `stale`

Sidebar does not automatically scroll/locate to the current page's link after refresh; users must manually find their position.

**Assessment:** Valid UX issue with no comments. Likely a theme behavior that needs verification. Could be expected design or oversight. Unclear if this is regression or limitation. Needs reproduction steps.

**Action:** Ask for reproduction steps and clarify expected behavior; check if sidebar scroll-to-active exists in current theme.

#### [#2406](https://github.com/vuejs/vitepress/issues/2406) social links in teams should be vertically aligned

`P3` `actionable` `effort:S` `repro:yes` · 👍 1 · 💬 0 · opened 3.1y ago, active 2d ago · labels: `theme` `stale`

In team component, social links are not vertically aligned at bottom of card when team member descriptions have varying line counts.

**Assessment:** Valid CSS layout bug affecting team page display. Screenshot clearly shows misalignment. Low engagement (1 thumbs_up) indicates cosmetic impact. Simple flexbox fix needed.

**Action:** Use flexbox gap and align-items to stick social links to bottom of team cards.

#### [#3048](https://github.com/vuejs/vitepress/issues/3048) Component is missing template or render function.

`P3` `likely-answered` `effort:M` `repro:yes` · 👍 0 · 💬 1 · opened 2.7y ago, active 2d ago · labels: `bug: pending triage` `stale`

Vue component wrapping a mermaid diagram throws 'missing template or render function' warning; custom mermaid integration issue.

**Assessment:** Thread shows detailed workaround: use markdown-it-diagram + mermaid plugin with custom Diagram.vue wrapper. This is a user integration issue, not a VitePress core bug. Reporter never replied to the solution.

**Action:** Close: answered — maintainer provided working solution with complete code example in thread.

## Local search (8)

#### [#2939](https://github.com/vuejs/vitepress/issues/2939) Dynamic routes/pages not visible in `local` search

`P1` `needs-info` `effort:L` `repro:no` · 👍 9 · 💬 5 · opened 2.8y ago, active 2d ago · labels: `stale`

Local search does not index dynamically generated pages created via VitePress dynamic routes feature.

**Assessment:** Valid limitation: search index is generated before dynamic routes are rendered. Maintainer directed users to third-party PageFind plugin. Recent comments (2025-07-10, 2026-04-08) show continued user frustration and suggest this remains unsolved in core. No repro provided but issue is well-understood.

**Action:** Investigate feasibility of post-render search indexing for dynamic routes; or clearly document limitation and recommend PageFind for dynamic content.

#### [#3024](https://github.com/vuejs/vitepress/issues/3024) can not use $frontmatter with Local Search

`P1` `actionable` `effort:M` `repro:yes` · 👍 4 · 💬 2 · opened 2.8y ago, active 2d ago · labels: `bug: pending triage` `stale`

Local search cannot index or find titles rendered from frontmatter variables via $frontmatter.

**Assessment:** Valid bug with reproduction on Stackblitz. brc-dd stated it's not supported and closed quickly. However, shellscape's follow-up comment (3 days later) shows a user opened a PR to add support, indicating real demand (4 thumbs_up). The feature is reasonable and increases search utility.

**Action:** Revisit: reconsider support for frontmatter content in local search — user demand confirmed by follow-up PR attempt.

#### [#3685](https://github.com/vuejs/vitepress/issues/3685) Content Security Policy: unsafe-eval has to be set due to `new Function` call when using customized search

`P1` `actionable` `effort:M` `repro:yes` · 👍 1 · 💬 3 · opened 2.3y ago, active 6d ago · labels: `build` `stale`

Custom tokenize function in local search requires unsafe-eval CSP directive due to deserialization of functions via new Function(). User cannot use customized search with strict CSP policies.

**Assessment:** Valid and well-documented bug with clear repro. Maintainer (brc-dd) acknowledged the issue and suggested precompilation approach as fix. This is a legitimate CSP security concern that affects real-world deployments.

**Action:** Implement precompilation of functions to avoid new Function() deserialization at runtime, enabling strict CSP compliance.

#### [#4246](https://github.com/vuejs/vitepress/issues/4246) Local Search Provider Hash Changes Every Build

`P2` `actionable` `effort:S` `repro:yes` · 👍 1 · 💬 2 · opened 1.8y ago, active 6d ago · labels: `stale`

Local search provider bundles change hash on every build even when documentation content is unchanged.

**Assessment:** Confirmed bug by brc-dd. Simple repro: use local search, build twice, observe hash change on @localSearchIndexroot bundle. Indicates non-deterministic build or missing cache invalidation logic.

**Action:** Investigate local search index generation for non-deterministic content; ensure stable hashing when content unchanged.

#### [#4688](https://github.com/vuejs/vitepress/issues/4688) `MiniSearch: duplicate ID` error when restarting the dev server (Duplicate for #4251)

`P2` `actionable` `effort:S` `repro:yes` · 👍 1 · 💬 4 · opened 1.2y ago, active 6d ago · labels: `stale`

MiniSearch duplicate ID error when restarting dev server after markdown changes; search cache not cleared on incremental updates.

**Assessment:** Known issue (duplicate of #4251). Maintainer brc-dd confirmed minor fix exists (discard before add) and plans to refactor search plugin. Author notes workaround: press 'r' to restart dev server. Has 1 thumbs_up; affects user testing workflow. Actionable fix is small.

**Action:** Implement search index partial rebuild on markdown changes—add discard logic before re-indexing to avoid duplicate ID errors. (duplicate of #4251)

#### [#3873](https://github.com/vuejs/vitepress/issues/3873) After packaging, the local search index js file exceeds 500kb.

`P2` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 5 · opened 2.2y ago, active 6d ago · labels: `perf` `stale`

Local search index chunk exceeds 500 KB after minification, with reported index files over 800 KB. User has large documentation site and seeks to reduce chunk size.

**Assessment:** Valid performance issue on large sites. Maintainer brc-dd (May 2024) confirmed no current way to split chunk; recommends brotli compression, excluding unneeded pages. User has 13M characters (~245k lines) of markdown. Legitimate but site-specific mitigation exists.

**Action:** Explore chunk splitting for search index or investigate if Vite 8 migration improved bundling. Otherwise, document brotli compression and lazy-load benefits.

#### [#4934](https://github.com/vuejs/vitepress/issues/4934) Injected $frontmatter.title is not searchable

`P2` `possibly-fixed` `effort:M` `repro:yes` · 👍 0 · 💬 2 · opened 10mo ago, active 6d ago · labels: `bug: pending triage` `stale`

Custom-injected frontmatter variables in page titles (e.g., `# {{ $frontmatter.title }}`) do not appear in local search results.

**Assessment:** Valid bug. Issue shows reproduction on StackBlitz. User #marcoroth reported similar issue and opened PR #5162 to fix on 2026-03-23. PR likely addresses search indexing of interpolated frontmatter in heading text.

**Action:** Verify against PR #5162 — likely fixed in recent commit. Check if fix is merged to main branch.

#### [#4979](https://github.com/vuejs/vitepress/issues/4979) Build minisearch indexes after `vite.plugins.transform`

`P2` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 4 · opened 9mo ago, active 6d ago · labels: `build` `stale`

Local search (minisearch) indexes are not built correctly when titles are injected via Vite plugin transform, even with enforce: 'pre'.

**Assessment:** Real issue: localSearchPlugin reads markdown directly from disk, bypassing Vite transforms. Maintainer brc-dd provided workaround using `_render` hook in search options. User confirmed workaround works in v1 and v2. Root cause is plugin architecture.

**Action:** Document the _render workaround in search configuration; consider architectural fix to respect Vite transforms in future versions.

## Algolia search (1)

#### [#3604](https://github.com/vuejs/vitepress/issues/3604) algolia搜索框在输入关键字时会中断输入法拼写

`P2` `upstream-blocked` `effort:M` `repro:yes` · 👍 0 · 💬 9 · opened 2.4y ago, active 6d ago · labels: `upstream` `stale`

Algolia search input breaks Chinese IME composition during typing in Firefox; user cannot complete input of multi-character words.

**Assessment:** Real bug in Firefox + Algolia combo with Chinese/CJK input methods. Maintainer brc-dd traced root to Algolia DocsSearch issue #1304 (CompositionEvent handling). UnoCSS local search works fine, confirming Algolia-specific problem.

**Action:** Close: upstream-blocked—root cause in Algolia DocsSearch, not VitePress; user can switch to local search or await Algolia fix.

## Build (31)

#### [#3314](https://github.com/vuejs/vitepress/issues/3314) Vite's "transformIndexHtml" hook is not invoked on build

`P1` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 7 · opened 2.6y ago, active 2d ago · labels: `build` `stale`

Vite's transformIndexHtml hook not invoked during build. Works in dev mode but missing during production build.

**Assessment:** Valid bug with clear repro. Maintainer (brc-dd) identified root cause: no index.html during build, unlike Vite. Suggests passing template to transformIndexHtml hook. Multiple comments requesting this feature. Strong practical need evident from recent follow-ups (Jan-Aug 2025).

**Action:** Implement transformIndexHtml support for build by applying it to the generated HTML template, similar to how transformHtml works.

#### [#3363](https://github.com/vuejs/vitepress/issues/3363) bug: Repeated builds will result in incorrect cache

`P1` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 3 · opened 2.5y ago, active 2d ago · labels: `stale`

Repeated calls to the Node.js build API cache incorrect content: second build returns first build's output despite file changes.

**Assessment:** Valid, reproducible bug with clear test case (external repo). Maintainer brc-dd investigated and found root cause in module caching; noted that a PR was reverted and suggests worker thread isolation is the correct fix. Blocking for programmatic API users.

**Action:** Implement worker thread isolation for build API calls to prevent cross-build module cache pollution.

#### [#3774](https://github.com/vuejs/vitepress/issues/3774) dead link errors should show transformed URL

`P1` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 12 · opened 2.2y ago, active 6d ago · labels: `docs` `stale`

Dead link checker reports wrong filename in error message when non-.html file extensions are transformed during build (e.g., .crt becomes .crt.html).

**Assessment:** Regression introduced between rc.39 and rc.40. User provides clear StackBlitz repro. brc-dd offers workaround (VITE_EXTRA_EXTENSIONS env var) but acknowledges error message could be improved. Affects clarity of build failures.

**Action:** Improve dead link error reporting to show transformed URL, not original; verify fix works with provided StackBlitz repro.

#### [#4227](https://github.com/vuejs/vitepress/issues/4227) Packaging optimization

`P1` `upstream-blocked` `effort:L` `repro:yes` · 👍 0 · 💬 8 · opened 1.8y ago, active 6d ago · labels: `need more info` `stale`

Large sites (1000+ markdown files) hit JavaScript heap out-of-memory errors during build; disabling local search helps dev but build still fails.

**Assessment:** Severe: user with 1000+ MD files (via dynamic routing) exhausts Node heap during build (v1.5.0). Thread reveals: local search is major memory consumer, disabling it helps dev but not build; buildConcurrency=1 doesn't fix it. Root cause appears to be Rollup/Vite bundler at scale. User frozen at v1.4.1. Another project (X-CMD with 3000+ pages) also affected. Upstream issue in Rollup/Vite.

**Action:** Upstream-blocked: investigate if Vite 8 migration (recent in main branch) improves memory behavior; document buildConcurrency tuning; consider recommending Astro/Hugo for 1000+ page sites.

#### [#4331](https://github.com/vuejs/vitepress/issues/4331) createContentLoader doesn't respect cleanUrls

`P1` `actionable` `effort:S` `repro:yes` · 👍 0 · 💬 0 · opened 1.7y ago, active 6d ago · labels: `build` `stale`

createContentLoader doesn't respect cleanUrls config setting, producing links with .html extension when cleanUrls is enabled.

**Assessment:** Valid bug with clear diagnosis: createContentLoader's md.render() call lacks MarkdownEnv context that createMarkdownToVueRenderFn provides. User correctly identified root cause in code. High-value fix: affects anyone using createContentLoader with cleanUrls enabled. No maintainer response.

**Action:** Fix by passing appropriate MarkdownEnv to md.render() in createContentLoader, similar to createMarkdownToVueRenderFn pattern.

#### [#4833](https://github.com/vuejs/vitepress/issues/4833) JavaScript heap out of memory when building large documentation sites with extensive mathematical formulas

`P1` `possibly-fixed` `effort:L` `repro:yes` · 👍 0 · 💬 6 · opened 12mo ago, active 26d ago · labels: `help wanted` `build` `perf`

JavaScript heap out-of-memory error when building large VitePress sites with thousands of markdown files containing extensive mathematical formulas (KaTeX/MathJax), even with --max-old-space-size=8192.

**Assessment:** Real, reproducible issue affecting production builds of math-heavy sites. Maintainer (brc-dd) identified potential root cause (#4415) and noted that Rolldown-Vite resolves the issue with 60% faster builds and lower memory. Recent comment (2026-06-07) suggests @webc.site/math as workaround. Issue may be auto-fixed by Rolldown integration in upcoming versions.

**Action:** Verify against main/2.0.0-alpha with Rolldown; likely fixed by Vite 8 migration and Rolldown bundler integration. Document as workaround: switch to lightweight math compiler.

#### [#4997](https://github.com/vuejs/vitepress/issues/4997) static asset not working for poster attribute of video element in raw html

`P1` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 4 · opened 8mo ago, active 6d ago · labels: `upstream` `client` `stale`

Static asset not working for the `poster` attribute of HTML video elements in raw HTML within markdown files (2.0.0-alpha.12), though it works when manually imported or in custom Vue components.

**Assessment:** Legitimate bug with clear reproduction. Maintainer brc-dd confirmed the issue is browser/Vue quirk related to `createStaticVNode`. Root cause identified in comments: Vue compiler optimization for static vnodes breaks video poster attribute on navigation. Suggested workarounds exist (playsinline, url fragment).

**Action:** Verify fix in latest 2.0.0-alpha; if persists, may need to prevent static vnode optimization for video elements with poster attributes

#### [#5134](https://github.com/vuejs/vitepress/issues/5134) OOM (heap out of memory) during `vitepress build` with large dynamic routes (~26,000 pages)

`P1` `actionable` `effort:L` `repro:yes` · 👍 0 · 💬 5 · opened 4mo ago, active 0d ago · labels: `build` `perf` `stale`

VitePress build crashes with out-of-memory (OOM) error when generating ~26,000 dynamic pages via .paths.ts files, even with 8 GB+ heap allocation; requests chunked/streaming page rendering.

**Assessment:** Valid bug affecting large-scale static generation. Multiple users report OOM with 5,000–26,000+ pages (comments 2026-03-27, 2026-05-06, 2026-07-04). Maintainer engagement limited. Latest comment (2026-07-04) shows build still fails with 18 GB heap on v2.0.0-alpha.17. High impact for enterprise users.

**Action:** Implement incremental/streaming page rendering or add `build.maxPagesPerChunk` config to batch SSR rendering and reduce peak memory usage.

#### [#5143](https://github.com/vuejs/vitepress/issues/5143) Name Collision & Unintentional Link Leak

`P1` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 0 · opened 4mo ago, active 6d ago · labels: `build` `stale`

Two critical build issues: (1) name collisions when file paths normalize to the same chunk name (e.g., `hello/world.md` and `hello_world.md` both become `hello_world`), causing cryptic build errors; (2) global `__VP_HASH_MAP__` exposed on all pages leaks URLs of intentionally unlisted/private pages.

**Assessment:** Two serious issues with reproductions provided (StackBlitz links). Issue 1 causes build failure; Issue 2 is a security/privacy concern. Reporter mentions drafted PR solving both issues. No maintainer response yet. Both are blocking bugs.

**Action:** Review and merge proposed PR; implement unique chunk naming to prevent collisions and restrict __VP_HASH_MAP__ visibility for unlisted pages.

#### [#2179](https://github.com/vuejs/vitepress/issues/2179) bug: Typescript data loaders from a package don't build correctly

`P2` `actionable` `effort:M` `repro:yes` · 👍 2 · 💬 3 · opened 3.3y ago, active 2d ago · labels: `build` `stale`

TypeScript data loaders from a package don't build correctly; transpiled export { data } causes 'data is not declared' error in staticDataPlugin.

**Assessment:** Valid bug with clear root cause found by reporter: TypeScript export re-export becomes invalid JavaScript when transpiled. Workaround is to use .js + .d.ts. Maintainer linked to #3185.

**Action:** Fix staticDataPlugin to handle transpiled data loader exports; or improve TypeScript→JS transpilation for .data.ts files from packages.

#### [#2537](https://github.com/vuejs/vitepress/issues/2537) Build hooks in dev mode

`P2` `needs-info` `effort:M` `repro:yes` · 👍 2 · 💬 5 · opened 3.0y ago, active 2d ago · labels: `has-workaround` `stale`

Build hooks (transformHead, buildEnd) only run during build, not in dev mode, causing dynamic head transformations to be invisible during development.

**Assessment:** Valid issue with stackblitz repro. Maintainer provided workarounds (transformIndexHtml, transformPageData). Commenter argues has-workaround label is incorrect for RSS feed use case (which requires buildEnd semantics). Inherent tension: running heavy transforms on every dev update is slow.

**Action:** Clarify expected behavior for 2.0; consider selective hooks that run in dev (e.g., lighter transformPageData) vs build-only (buildEnd); document current dev limitations.

#### [#3125](https://github.com/vuejs/vitepress/issues/3125) custom resolve alias not available in .paths.js file

`P2` `likely-answered` `effort:M` `repro:yes` · 👍 2 · 💬 5 · opened 2.7y ago, active 2d ago · labels: `build` `stale`

Vite resolve.alias defined in config is not available in [slug].paths.js files; import fails with 'Invalid paths file' error.

**Assessment:** Valid limitation. brc-dd explained dynamic route loaders bypass Vite's normal resolution due to SSR/build constraints. Community workaround documented: use Node subpath imports in package.json. issue was referenced and solved by user babakfp.

**Action:** Close: answered — workaround is documented in thread; limitation is architectural (vite-node not yet in core).

#### [#289](https://github.com/vuejs/vitepress/issues/289) Stripped page assets (.lean.js) are not actually being stripped

`P2` `stale-candidate` `effort:M` `repro:yes` · 👍 1 · 💬 3 · opened 5.2y ago, active 2d ago · labels: `build` `stale`

The .lean.js optimization regex fails to match certain Vue AST patterns (createVNode vs createStaticVNode), resulting in lean.js files as large as their full .js counterparts.

**Assessment:** Valid bug with clear repro (small pages, local image refs, inline SVG). Reported in v0.13.0; no maintainer response. Low impact (only 1 thumbs_up) but real performance issue for content-heavy pages. May be outdated given Vite 8 migration and Vue compiler changes.

**Action:** Verify whether Vite 8/Vue updates have resolved this; if not, audit the regex pattern against current Vue compiler output.

#### [#3244](https://github.com/vuejs/vitepress/issues/3244) Dead links in sidebar not reported

`P2` `actionable` `effort:M` `repro:yes` · 👍 1 · 💬 0 · opened 2.6y ago, active 2d ago · labels: `stale`

Dead links in sidebar are not reported during build. Sidebar with broken link references (e.g., '/Bar' when Bar.md doesn't exist) builds without warning.

**Assessment:** Valid bug with clear repro (vitepress.new). Should warn or error on invalid sidebar links. Helps catch config mistakes early. No maintainer comments. Small user base impact but quality-of-life improvement.

**Action:** Implement build-time validation of sidebar links; warn or error on unresolved paths.

#### [#4165](https://github.com/vuejs/vitepress/issues/4165) Why the css and js files import fail after I build?  In other words, the styles are lost and the web pages are messed up.

`P2` `needs-info` `effort:M` `repro:yes` · 👍 1 · 💬 8 · opened 1.8y ago, active 6d ago · labels: `need more info` `stale`

Built dist files have broken asset imports due to leading slash in href (absolute paths instead of relative), causing missing styles when deployed to web server.

**Assessment:** Valid deployment issue. User confirmed workaround (removing leading slash) fixes it. brc-dd asked for context on deployment method. Last comment (Feb 2025) from Duclearc suggests issue still occurs on nginx with vitepress build. Root cause likely base config or deployment path mismatch.

**Action:** Request full config.ts and deployment context. Verify base setting matches actual deployment path.

#### [#309](https://github.com/vuejs/vitepress/issues/309) Support `@vitejs/plugin-legacy`

`P2` `upstream-blocked` `effort:L` `repro:yes` · 👍 0 · 💬 6 · opened 5.1y ago, active 2d ago · labels: `build` `stale`

Using @vitejs/plugin-legacy with VitePress fails due to VitePress assuming RollupOutput instead of RollupOutput[] when legacy plugin produces multiple build targets.

**Assessment:** Valid bug with clear root cause (brc-dd identified in 2022-07). brc-dd noted (2023-07) that full fix requires refactoring asset emission and module scripts for legacy browser support—non-trivial. Partial workaround: use build.target + polyfill.io.

**Action:** Defer pending broader refactoring of legacy asset handling; document workaround; revisit post-v2 stabilization if legacy support becomes priority.

#### [#2629](https://github.com/vuejs/vitepress/issues/2629) don't process md files in public dir

`P2` `needs-info` `effort:M` `repro:no` · 👍 0 · 💬 3 · opened 3.0y ago, active 2d ago · labels: `build` `stale`

VitePress currently processes markdown files in the public directory, generating HTML files with public/ path prefix, which is unexpected.

**Assessment:** Lead maintainer (brc-dd) reported this; valid issue with expected behavior clarification. User disagreed with proposed fix and wanted special handling. No consensus on whether to process or skip public/*.md files.

**Action:** Clarify desired behavior: should public/*.md be skipped entirely or processed separately? Document decision and implement accordingly.

#### [#3039](https://github.com/vuejs/vitepress/issues/3039) Dead links in frontmatter not reported

`P2` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 1 · opened 2.8y ago, active 2d ago · labels: `build` `stale`

Dead link checker does not report broken links in frontmatter (hero.actions, features.link) even though they reference markdown files.

**Assessment:** Valid bug with clear Stackblitz reproduction. brc-dd identified the root cause: link validation only processes markdown content, not frontmatter YAML. Affects users with layout: home. Reasonable enhancement to extend validation to theme-aware frontmatter fields.

**Action:** Implement: extend link validation to extract and check links in common frontmatter fields (hero.actions[].link, features[].link, etc).

#### [#3081](https://github.com/vuejs/vitepress/issues/3081) Can't build when files are missing (ignoreDeadLinks is not working)

`P2` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 4 · opened 2.7y ago, active 2d ago · labels: `stale`

ignoreDeadLinks option only ignores page links, not missing asset files; build fails when images referenced in markdown are missing.

**Assessment:** Valid limitation. brc-dd clarified that ignoreDeadLinks only applies to page links, not asset resolution (Rollup error). User has legitimate need for migration scenarios. Recent comment (kieronlanning, May 2025) confirms issue still blocks migrations.

**Action:** Implement or document — consider extending ignoreDeadLinks to asset resolution, or add new ignoreAssets option for SSG builds.

#### [#3490](https://github.com/vuejs/vitepress/issues/3490) Build failed for md file name containing `#`

`P2` `stale-candidate` `effort:S` `repro:yes` · 👍 0 · 💬 1 · opened 2.4y ago, active 6d ago · labels: `wont fix` `stale`

Build fails when markdown filenames contain `#` character; also reports issues with `[.*]` patterns in filenames.

**Assessment:** Bug with repro (Stackblitz link provided). Labeled 'wont fix'. User suggests at minimum better error message. Related issue: square bracket patterns cause missing .path.js in 1.1.0.

**Action:** Clarify if this is by design (URL/slug encoding issue) or fixable; at minimum add validation and error message for forbidden filename patterns.

#### [#3712](https://github.com/vuejs/vitepress/issues/3712) vitepress dev: Error: ENOENT: no such file or directory

`P2` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 0 · opened 2.3y ago, active 6d ago · labels: `stale`

VitePress dev server crashes when SVN conflict markers (<<<< .mine) are present in markdown files, causing ENOENT and halting entire dev session.

**Assessment:** Valid issue with clear repro. Error happens because VitePress tries to process merge conflict artifact files as real markdown. Request asks for graceful fallback (warning instead of crash). No maintainer response yet.

**Action:** Improve error handling: skip or warn on files containing merge conflict markers instead of crashing dev server.

#### [#3716](https://github.com/vuejs/vitepress/issues/3716) Cannot build with docker buildkit on arm64

`P2` `possibly-fixed` `effort:M` `repro:yes` · 👍 0 · 💬 9 · opened 2.3y ago, active 6d ago · labels: `need more info` `stale`

Docker build with buildkit on arm64 (Apple Silicon) hangs indefinitely during vitepress build, especially when building amd64 images on arm64 host.

**Assessment:** Issue reports infinite progress during Docker build. Root cause appears to be cross-architecture build (amd64 on arm64) rather than VitePress itself. Author self-diagnosed: missing --platform=linux/amd64 directive. Maintainer (brc-dd) confirmed build works on native arm64. Issue may be user configuration error, not VitePress bug.

**Action:** Close: likely user misconfiguration; add note that cross-platform builds require explicit --platform directives.

#### [#3763](https://github.com/vuejs/vitepress/issues/3763) file name too long

`P2` `actionable` `effort:L` `repro:yes` · 👍 0 · 💬 7 · opened 2.2y ago, active 6d ago · labels: `build` `stale`

Build fails with 'file name too long' error when deeply nested folders with long Chinese character names exceed filesystem path limits.

**Assessment:** Valid issue affecting Windows/CentOS with deep folder structures. brc-dd identified the root cause: chunk resolution logic includes full paths in names, exceeding NAME_MAX limits. Maintainer notes this requires significant refactoring to fix; recommended workaround is using a different tool.

**Action:** Consider for 2.0 refactoring: explore hash-based or shorter chunk naming to mitigate path length issues on Windows.

#### [#3881](https://github.com/vuejs/vitepress/issues/3881) Even after adding a custom 404.md, the standard 404 page is displayed.

`P2` `needs-repro` `effort:M` `repro:yes` · 👍 0 · 💬 1 · opened 2.1y ago, active 6d ago · labels: `build` `stale`

Custom 404.md file is created but standard 404 page displays when accessing non-existent paths. The custom page briefly appears then gets replaced.

**Assessment:** Valid bug with reproduction link provided. Issue marked stale but still relevant. Appears to be an SSR/routing issue where custom 404 page exists but standard page takes precedence.

**Action:** Verify against current version (2.0.0-alpha with Vite 8), check if fixed by recent migrations. May need SSR/routing investigation.

#### [#4219](https://github.com/vuejs/vitepress/issues/4219) Copying generated file to www folder to be served by nginx doesn´t work

`P2` `needs-repro` `effort:M` `repro:yes` · 👍 0 · 💬 0 · opened 1.8y ago, active 6d ago · labels: `bug: pending triage` `stale`

After building VitePress site and copying /dist folder to nginx, Vue components and search function fail to load (missing /vue directory).

**Assessment:** Build artifact issue: user provided Dockerfile multi-stage build and nginx config. In dev mode works; after dist copy to nginx, Vue runtime is missing or not served correctly. May be asset path misconfiguration, missing routing config in nginx, or base URL issue. Marked stale.

**Action:** Ask for nginx config and base URL setting; verify if issue is VitePress build or nginx routing—likely misconfiguration.

#### [#4275](https://github.com/vuejs/vitepress/issues/4275) 打包后没有index.html页面

`P2` `needs-repro` `effort:M` `repro:no` · 👍 0 · 💬 4 · opened 1.7y ago, active 6d ago · labels: `need more info` `stale`

Build produces no index.html file despite using auto-generated file-based navigation config. Segmentation fault reported during npm build.

**Assessment:** User reports dev mode works but production build fails silently or crashes. Minimal issue description (many '1' placeholders). brc-dd requested reproducible example. Later comment mentions segmentation fault in npm on Windows. Possibly Windows-specific or environment issue.

**Action:** Request minimal reproducible repo (vitepress.new) with exact config and file structure; investigate Windows segfault separately.

#### [#4482](https://github.com/vuejs/vitepress/issues/4482) Using babylonjs/loaders, the error "config must export or return an object" occurs

`P2` `upstream-blocked` `effort:M` `repro:yes` · 👍 0 · 💬 1 · opened 1.5y ago, active 6d ago · labels: `build` `needs more discussion` `stale`

Using @babylonjs/loaders causes build error 'config must export or return an object' due to file named KHR_animation_pointer.data.js being mistakenly treated as Vite config file.

**Assessment:** Valid bug. VitePress data loader plugin incorrectly tries to load .data.js files from node_modules as config. brc-dd suggested adding exclude/filter config to the plugin. Workaround exists (disable plugin) but is invasive. Upstream issue in how Vite/VitePress handles .data.js file detection.

**Action:** Add configurable exclude pattern to vitepress:data plugin to skip non-config .data.js files; or fix file detection logic.

#### [#4529](https://github.com/vuejs/vitepress/issues/4529) [ERR_MODULE_NOT_FOUND] when consuming custom theme

`P2` `possibly-fixed` `effort:M` `repro:yes` · 👍 0 · 💬 4 · opened 1.4y ago, active 6d ago · labels: `build` `stale`

Custom theme extending DefaultTheme fails at build with ERR_MODULE_NOT_FOUND for 'without-fonts' module, though works in dev. Has reproduction repo.

**Assessment:** Valid bug with clear reproduction. Maintainer provided workaround (vite.ssr.noExternal config). Root cause likely in how theme is bundled/exported. One follow-up question from user still unanswered, but issue is solvable via workaround.

**Action:** Verify if Vite 8 migration fixed this; if not, clarify the fix and improve custom theme documentation with noExternal requirement.

#### [#4603](https://github.com/vuejs/vitepress/issues/4603) Build writes normal output to stderr

`P2` `needs-info` `effort:M` `repro:no` · 👍 0 · 💬 3 · opened 1.3y ago, active 6d ago · labels: `has-workaround` `needs more discussion` `stale`

VitePress build outputs normal (non-error) messages to stderr instead of stdout, causing downstream tools like Rush to interpret the build as failed.

**Assessment:** Valid issue with partial context. brc-dd explained that running spinners on stderr is a common practice (cited Ora, Vue CLI, Deno) but agreed logger refactor is planned. Workaround exists (configure Rush, pipe stderr→stdout). No clear decision on whether to change behavior.

**Action:** Defer pending logger refactor — check if Vite 8 migration or recent logger changes addressed this; if not, spec expected behavior once logger work is scoped.

#### [#4612](https://github.com/vuejs/vitepress/issues/4612) [Bug] Build fails when srcDir is set to a directory outside project root

`P2` `needs-repro` `effort:M` `repro:yes` · 👍 0 · 💬 3 · opened 1.3y ago, active 6d ago · labels: `has-workaround` `stale`

Build fails with 'Rollup failed to resolve import vue' error when srcDir is set to a directory outside the project root.

**Assessment:** Valid configuration edge case. brc-dd explained root cause: Vue must be resolvable from the markdown file's location (requires installing vitepress in parent package.json or using Vite alias/plugin workaround). Has workarounds; likely not a bug but a configuration constraint.

**Action:** Close: has-workaround documented — is configuration/edge-case not a vitepress bug; user must install deps where srcDir points to.

#### [#5133](https://github.com/vuejs/vitepress/issues/5133) ERR_MODULE_NOT_FOUND

`P2` `needs-repro` `effort:M` `repro:no` · 👍 0 · 💬 1 · opened 4mo ago, active 6d ago · labels: `need more info` `stale`

Build fails with 'Cannot find module' error for app.js when running `vitepress build` in production, while dev environment works normally. No minimal reproduction provided.

**Assessment:** Build error with insufficient context. brc-dd asked for a minimal reproduction or private repo demo (2026-03-05) but no response from reporter. Marked stale. Cannot triage without more information.

**Action:** Request minimal repro or close: stale for 4+ months without response.

## Dev server / HMR (7)

#### [#4415](https://github.com/vuejs/vitepress/issues/4415) Crash with JS heap OOM when editing LaTeX-heavy pages under `vitepress dev`

`P1` `upstream-blocked` `effort:L` `repro:yes` · 👍 2 · 💬 6 · opened 1.6y ago, active 26d ago · labels: `help wanted` `build`

Memory leak and OOM crashes during HMR when editing LaTeX-heavy pages with KaTeX/MathJax in dev mode. Memory usage grows unbounded after multiple edits, eventually exceeding heap limits.

**Assessment:** Valid bug with clear reproduction. Lead maintainer brc-dd investigated and identified multiple causes: Vue compiler-sfc caches not releasing old items, fs-extra/graceful-fs contributing to leaks, and general HMR memory issues in Vite 6. Some issues appear to be upstream (Vite/Vue compiler) but VitePress may need fixes for its own caches. Switching to KaTeX partially mitigates but doesn't fully solve.

**Action:** Verify in v2.0-alpha against Vite 8 migration; investigate Vue compiler cache limits and fs-extra memory footprint; coordinate with Vite/Vue teams on HMR cycle GC.

#### [#4313](https://github.com/vuejs/vitepress/issues/4313) When using bun to start, modifying the content of the config.mts file will get stuck at restarting the service

`P1` `actionable` `effort:M` `repro:yes` · 👍 1 · 💬 17 · opened 1.7y ago, active 2d ago · unlabeled

When using bun to start VitePress, modifying config.mts causes server restart to hang indefinitely instead of completing normally.

**Assessment:** Multiple recent reports (latest 2026-07-02) on v2.0.0-alpha.17+. Initial diagnosis: user error (bun spawning with --bun flag due to missing Node), but recent v2 alpha reports suggest regression. Related upstream bun issue #29309. Needs verification against current 2.0.0-alpha release.

**Action:** Verify against latest 2.0.0-alpha; if reproducible, investigate bun+Vite 8 compatibility or escalate to bun repo.

#### [#3292](https://github.com/vuejs/vitepress/issues/3292) Theme is made into npm package, if use the theme, Symbol is duplicated in dev.

`P2` `possibly-fixed` `effort:M` `repro:no` · 👍 0 · 💬 2 · opened 2.6y ago, active 2d ago · labels: `need more info` `stale`

Symbol duplication issue in dev when custom NPM package theme is used. Provider/inject fails because Symbol from theme package gets duplicated in vite optimizeDeps cache.

**Assessment:** Valid bug report. Maintainer (brc-dd) suggested adding theme to vite.ssr.noExternal. Author confirmed workaround works via optimizeDeps.exclude config. Issue may be Vite behavior or VitePress integration. Likely fixable via docs.

**Action:** Verify if this is still an issue in 2.0.0-alpha. If reproducible, explore fixing in VitePress or document the workaround more prominently.

#### [#4342](https://github.com/vuejs/vitepress/issues/4342) import.meta.glob evelopment pattern is not work

`P2` `needs-repro` `effort:M` `repro:yes` · 👍 0 · 💬 1 · opened 1.7y ago, active 6d ago · labels: `bug: pending triage` `stale`

import.meta.glob() in npm-installed theme packages doesn't resolve correctly in dev mode; glob paths don't auto-adjust to node_modules depth, causing module load failures.

**Assessment:** User trying to build publishable theme with dynamic component loading. In dev (when installed via npm), glob paths remain relative to source but should resolve to node_modules. Works in production build. Root cause: glob path resolution doesn't account for different working directory depths. Needs Vite investigation.

**Action:** Verify with minimal repro; check if upstream Vite glob resolution needs to be configured differently for node_modules themes.

#### [#4642](https://github.com/vuejs/vitepress/issues/4642) 在 Vite 插件的 configureServer 钩子里执行 server.restart() 报错 MiniSearch: duplicate ID xx

`P2` `needs-repro` `effort:M` `repro:yes` · 👍 0 · 💬 0 · opened 1.3y ago, active 6d ago · labels: `stale`

Calling `server.restart()` in a Vite plugin's `configureServer` hook when a .md file is added/deleted throws 'MiniSearch: duplicate ID' error.

**Assessment:** Valid bug with clear reproduction code. User is watching .md files and restarting the server; MiniSearch duplicate ID suggests local search index is not properly cleared/reset on restart. May be race condition or improper cleanup.

**Action:** Ask for full error stack trace and vitepress version confirmation; likely needs fix in server restart cleanup logic for search index.

#### [#4800](https://github.com/vuejs/vitepress/issues/4800) [data loader] load not being triggered by file watcher

`P2` `needs-repro` `effort:M` `repro:no` · 👍 0 · 💬 0 · opened 1.0y ago, active 6d ago · labels: `stale`

Data loader glob pattern watch fails for files outside the VitePress source directory. File watcher detects changes when VitePress files change but not when external watched files (e.g., ../src/**/*.py) change.

**Assessment:** Valid edge case but unclear root cause. Opened by user (ISOR3X) and marked stale by maintainer. Watch patterns for external files may be a known limitation. No maintainer investigation in thread.

**Action:** Request step-by-step repro with vitepress version and check if this is a Vite file-watcher limitation or VitePress-specific issue.

#### [#5180](https://github.com/vuejs/vitepress/issues/5180) [Bug] HMR fails with "inject() can only be used inside setup()" when modifying config title

`P3` `possibly-fixed` `effort:S` `repro:yes` · 👍 0 · 💬 2 · opened 3mo ago, active 6d ago · labels: `need more info` `stale`

HMR crashes with 'inject() can only be used inside setup()' error when modifying config title in dev mode.

**Assessment:** Issue was traced to Bun runtime compatibility by maintainer brc-dd; author confirmed it works fine with Node.js. Root cause is Bun-specific HMR behavior, not VitePress. Resolved via workaround (use Node.js instead).

**Action:** Close: issue resolved via runtime switch; recommend Node.js for VitePress development.

## SSR / hydration (14)

#### [#2984](https://github.com/vuejs/vitepress/issues/2984) :target css only works on reload

`P1` `actionable` `effort:M` `repro:yes` · 👍 2 · 💬 5 · opened 2.8y ago, active 2d ago · labels: `client` `stale`

CSS `:target` pseudo-class selector does not update live when user clicks anchor links; only applies on initial page load or hard refresh.

**Assessment:** Valid bug reproduced and documented with minimal example (https://github.com/nekomeowww/reproduction-vitepress-target-pseudo-class-not-working). Root cause: VitePress client-side router may not trigger CSS recalculation on hash navigation. Workaround provided using hashchange event listener.

**Action:** Verify reproduction; likely fix in router hash handling or client hydration logic to trigger style recalculation.

#### [#4134](https://github.com/vuejs/vitepress/issues/4134) TypeError: Cannot read properties of null (reading 'parentNode')

`P1` `possibly-fixed` `effort:M` `repro:yes` · 👍 0 · 💬 3 · opened 1.9y ago, active 6d ago · labels: `bug: pending triage` `stale`

Production site throws errors when refreshing page and navigating: 'Cannot read properties of null (reading parentNode)' and hydration-related issues.

**Assessment:** Real bug affecting multiple users (mistic100 confirmed same issue). Root cause identified in comment thread: SSR hydration mismatch, often triggered by custom markdown containers or components without ClientOnly wrapper. May be related to Vite 8 migration or component lifecycle issue.

**Action:** Verify against 2.0.0-alpha with Vite 8 migration. Check if ClientOnly wrapping is documented and if SSR hydration handling improved.

#### [#4200](https://github.com/vuejs/vitepress/issues/4200) 动态路由打包后遇到的bug，开发环境正常

`P1` `needs-repro` `effort:M` `repro:no` · 👍 0 · 💬 3 · opened 1.8y ago, active 6d ago · labels: `bug: pending triage` `stale`

Dynamic routes using relative paths (e.g., ../svgui/important/) work in dev but produce 404 content in production build; file generation succeeds but content is missing.

**Assessment:** Legitimate bug affecting dynamic routing with parent-directory traversal. User provided directory structure and code snippets but no minimal reproduction repo. Multiple attempts to configure work in dev, fail in build (dev-vs-build discrepancy is a red flag). Labeled stale.

**Action:** Ask for minimal stackblitz/codesandbox reproduction; verify against 2.0.0-alpha, may be related to SSR/build path resolution.

#### [#4361](https://github.com/vuejs/vitepress/issues/4361) Big BUG: Page flashing issue when scrolling down and navigating back in VitePress

`P1` `needs-repro` `effort:L` `repro:yes` · 👍 0 · 💬 2 · opened 1.6y ago, active 6d ago · labels: `client` `stale`

Page flashing/flickering occurs when scrolling down and navigating back using browser back button.

**Assessment:** Valid user-facing issue with video reproduction. Author notes it affects vuejs.org and vite.dev. Comment suggests possible duplicate of #4351. Likely scroll restoration or state rehydration issue.

**Action:** Verify against #4351; if distinct, request minimal repro without iframe; test against latest 2.0.0-alpha.

#### [#4750](https://github.com/vuejs/vitepress/issues/4750) Some SVG icons go missing on vue components

`P1` `possibly-fixed` `effort:M` `repro:yes` · 👍 0 · 💬 11 · opened 1.1y ago, active 6d ago · labels: `build` `upstream` `stale`

SVG icons disappear after browser refresh in production builds due to SSR static node stripping. Specific SVG content triggers Vue import warnings.

**Assessment:** Maintainer brc-dd identified root cause: VitePress strips static content nodes to reduce payload, but parent SVG is not static so renders blank on toggle. Workarounds exist (v-show, raw import). Marked as 'very tricky to fix' by maintainer.

**Action:** Verify if recently fixed by Vite 8 migration (228eef18); if not, plan fix for static node handling in SSR context.

#### [#4045](https://github.com/vuejs/vitepress/issues/4045) SSR in dev for more consistent output in dev and prod

`P2` `possibly-fixed` `effort:L` `repro:yes` · 👍 1 · 💬 5 · opened 2.0y ago, active 6d ago · labels: `build` `has-workaround` `stale`

SSR mode with v-html rendering produces different output in dev (works) vs prod (errors). Self-closing tags like `<i/>` are invalid HTML in SSR.

**Assessment:** Root cause identified by brc-dd: Vue's SSR strictly validates HTML; dev doesn't. brc-dd suggests enabling SSR in dev for consistency (like Nuxt does). Thread shows this is design limitation, not simple bug. Vite 6 environment API may help.

**Action:** Track as design consideration for Vite 6 integration. Document SSR HTML validation differences. User workaround: use proper closing tags.

#### [#3262](https://github.com/vuejs/vitepress/issues/3262) Why I cannot use html in markdown file to insert an image with caption?

`P2` `possibly-fixed` `effort:M` `repro:yes` · 👍 0 · 💬 13 · opened 2.6y ago, active 2d ago · labels: `need more info` `stale`

HTML figure/image tags in markdown work in dev but fail at build time. Build fails when using relative paths in HTML img src (absolute paths work).

**Assessment:** Valid issue but root cause identified in thread: dev uses root path, production uses a different path context. User resolved by using absolute/correct relative paths. Later commenter noted workaround via markdown-it plugin (custom figure rule), but found edge cases with static assets. Suggests deeper SSG hydration or asset path resolution issue. Recent comment (2025-07-04) confirms it was a dev vs. build path issue.

**Action:** Verify against latest version and Vite 8 migration. Likely fixed by recent changes; if not, document relative path requirements for HTML in markdown.

#### [#3586](https://github.com/vuejs/vitepress/issues/3586) GitHub Pages results in "Hydration completed but contains mismatches"

`P2` `upstream-blocked` `effort:M` `repro:yes` · 👍 0 · 💬 2 · opened 2.4y ago, active 6d ago · labels: `docs` `stale`

GitHub Pages trailing slash URLs cause hydration mismatch errors. User redirected to `/markdown-examples/` but page breaks until refresh.

**Assessment:** Real issue with GitHub Pages specifically. Maintainer brc-dd confirmed it's a GitHub Pages platform problem, not VitePress code. Suggested possible client-side workaround but acknowledged detection is tricky.

**Action:** Close: upstream-blocked—GitHub Pages behavior cannot be fixed in VitePress; consider adding to docs as known limitation.

#### [#3699](https://github.com/vuejs/vitepress/issues/3699) Computed prop is not changed on pageload

`P2` `likely-answered` `effort:S` `repro:yes` · 👍 0 · 💬 1 · opened 2.3y ago, active 6d ago · labels: `upstream` `stale`

Computed properties bound to components are only evaluated at build time, not updated on client hydration. Theme mismatch occurs when dark mode is preferred on client but light mode was SSR-rendered.

**Assessment:** Valid hydration mismatch issue. Maintainer (brc-dd) provided working solution: use ref + onMounted + watchPostEffect or conditionally return undefined during SSR. This is a Vue hydration behavior, not VitePress bug. Issue is answered.

**Action:** Close: answered; point to solution in Vue docs about SSR hydration.

#### [#3780](https://github.com/vuejs/vitepress/issues/3780) hydration mismatch with default build

`P2` `needs-info` `effort:M` `repro:no` · 👍 0 · 💬 11 · opened 2.2y ago, active 6d ago · labels: `need more info` `stale`

Hydration mismatch occurs when VitePress site is deployed to Azure App Service but runs perfectly locally with dev and preview modes.

**Assessment:** brc-dd suspects routing issue (server not sending correct HTML, possibly a fallback). User provided deployed page source showing missing home layout. Likely environment-specific (Azure hosting, ASP.NET Core setup). Community member suggested enabling __VUE_PROD_HYDRATION_MISMATCH_DETAILS__ for diagnostics.

**Action:** Ask user for: (1) local dist/index.html source comparison, (2) Azure hosting config (base path, SPA fallback setup), (3) debug output with mismatch details enabled.

#### [#3867](https://github.com/vuejs/vitepress/issues/3867) defineClientComponent doesn't forward slots

`P2` `likely-answered` `effort:M` `repro:yes` · 👍 0 · 💬 2 · opened 2.2y ago, active 6d ago · labels: `has-workaround` `stale`

Bug: `defineClientComponent` doesn't forward Vue slots when wrapping globally-registered components. Default slot content not rendered in markdown usage.

**Assessment:** Valid bug on VitePress 1.1.4. Maintainer brc-dd provided detailed workaround (May 2024) showing manual slot forwarding via h(). User acknowledged it works but inelegant. Core issue: helper function needs enhancement.

**Action:** Improve defineClientComponent to auto-forward slots and expose proper slot typing. Priority depends on adoption frequency.

#### [#4107](https://github.com/vuejs/vitepress/issues/4107) Content disappeared after navigation (refresh on page or navigation accessing to page)

`P2` `needs-repro` `effort:L` `repro:no` · 👍 0 · 💬 6 · opened 1.9y ago, active 6d ago · labels: `bug: pending triage` `stale`

Content disappears after page navigation or refresh in production build; appears to be GitHub Pages specific. Splash appears briefly then blank page.

**Assessment:** Bug report from production deployment (GitHub Pages). No clear root cause identified. User provided repo link but issue is not reproducible in standard VitePress setup. Similar to #3649 but user confirms no cloudflare proxy. Requires investigation of user's config or potential hydration issue.

**Action:** Ask for minimal repro (create public repo with issue) or check GitHub Pages base URL configuration; this may be a project-specific config issue.

#### [#4544](https://github.com/vuejs/vitepress/issues/4544) Hydration mismatch issue

`P2` `needs-info` `effort:M` `repro:no` · 👍 0 · 💬 10 · opened 1.4y ago, active 6d ago · labels: `need more info` `stale`

Hydration mismatch error when using serve package to statically serve built VitePress site; works with http-server. Only happens when navigating directly to page URLs.

**Assessment:** Maintainer suspects clean URLs config issue or serve package incompatibility. Thread shows similar issue in VueUse repo linked to locale-dependent date formatting causing SSR/client mismatch. No root cause determined. User hasn't provided sample repo.

**Action:** Request minimal reproduction repo; check if related to clean URLs config or locale-dependent content.

#### [#4538](https://github.com/vuejs/vitepress/issues/4538) enhanceApp 异步导入组件库 进行客户端组件渲染 文档打包后第一次进入组件示例 首页视图残留

`P3` `likely-answered` `effort:M` `repro:yes` · 👍 0 · 💬 7 · opened 1.4y ago, active 6d ago · labels: `need more info` `stale`

Hydration mismatch when using async imports of component libraries (Element Plus) in enhanceApp during build preview; page view glitches on first navigation but works after refresh.

**Assessment:** Maintainer diagnosed as timing/usage issue, not a VitePress bug. Caused by interacting with page before JS hydration completes. Provided alternative solutions: use inBrowser guard, avoid dynamic imports, or use SSR-compatible Element Plus setup. User accepted answer.

**Action:** Close as not-a-bug with explanation; enhance docs on SSR compatibility and hydration timing.

## Router / navigation (8)

#### [#3028](https://github.com/vuejs/vitepress/issues/3028) Vitepress' router.js prevents event trackers accurately recording

`P1` `actionable` `effort:L` `repro:yes` · 👍 3 · 💬 9 · opened 2.8y ago, active 2d ago · labels: `need more info` `stale`

VitePress router updates URL before other click handlers (e.g. PostHog, Google Analytics) run, causing analytics to record wrong page context.

**Assessment:** Valid bug with clear reproduction. Author detailed the root cause: router uses capture: true in event listener, executing before document-level handlers. brc-dd suggested removing capture: true, but author's testing showed side effects. No final resolution in thread. Affects any analytics/tracking integration.

**Action:** Verify and test: experiment with event listener timing (capture:true vs false) to balance SPA behavior with analytics accuracy, or provide configuration option.

#### [#4364](https://github.com/vuejs/vitepress/issues/4364) Links in the Nav bar do not support rewrites

`P1` `actionable` `effort:M` `repro:no` · 👍 1 · 💬 0 · opened 1.6y ago, active 6d ago · labels: `bug: pending triage` `stale`

URL rewrites do not apply to navigation bar links. Clicking nav link with rewrite path does not redirect as expected, while sidebar and other links work correctly.

**Assessment:** Valid bug. User provided clear config reproduction showing `rewrites: { '/guide': 'guide/intro' }` doesn't work in nav. Theme component likely bypasses rewrite logic for nav links.

**Action:** Verify nav link rendering logic applies rewrites; trace diff from working sidebar link handling.

#### [#3062](https://github.com/vuejs/vitepress/issues/3062) Enable rewrites function got 404 when running in preview and build mode.

`P1` `actionable` `effort:L` `repro:yes` · 👍 0 · 💬 5 · opened 2.7y ago, active 2d ago · labels: `stale`

Rewrites feature works in dev mode but breaks with 404s in preview/build modes; createContentLoader URLs don't reflect rewritten paths.

**Assessment:** Significant regression affecting URL routing. brc-dd noted the rule pattern was invalid initially but later reports (tracy2zhang, aiktb) confirm inconsistent dev vs build behavior. Multiple users affected. No fix confirmed.

**Action:** Prioritize — dev/build inconsistency is a critical workflow issue; requires investigation of rewrite handling in SSG phase.

#### [#5045](https://github.com/vuejs/vitepress/issues/5045) keycloak sso redirect show 404

`P1` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 6 · opened 7mo ago, active 6d ago · labels: `bug: pending triage` `stale`

After Keycloak SSO login redirect, pages show 404 error in alpha.2 but worked in alpha.1. Issue appears related to router.ts changes between versions.

**Assessment:** Valid regression reported with technical analysis by user. User identified router.ts changes as likely cause between alpha.1 and alpha.2. Maintainer should verify SSO redirect behavior against recent router changes.

**Action:** Verify router changes between alpha.1-alpha.2 against Keycloak SSO redirect behavior; check if URL normalization is breaking query/hash parameters.

#### [#4058](https://github.com/vuejs/vitepress/issues/4058) Redirection for `/index` leads to empty page

`P2` `actionable` `effort:S` `repro:yes` · 👍 1 · 💬 2 · opened 2.0y ago, active 6d ago · labels: `has-workaround` `client` `stale`

Accessing a page with `/index` suffix redirects correctly but the page content fails to render; page reload fixes it. Not reproducible in dev mode.

**Assessment:** Valid bug with clear repro on vitepress.dev. The server responds 404 for the `/index` URL but the client-side redirect happens, causing partial/empty render. Issue is in production builds only, likely a router/hydration edge case.

**Action:** Verify router handling of /index URL redirects; ensure client-side hydration works correctly after redirect in production builds.

#### [#4335](https://github.com/vuejs/vitepress/issues/4335) Can't seem to get index.md to reliably reroute to readme.md

`P2` `needs-info` `effort:M` `repro:no` · 👍 0 · 💬 1 · opened 1.7y ago, active 6d ago · labels: `bug: pending triage` `stale`

Rewrites config doesn't reliably reroute readme.md to index.md across all scenarios (dev vs build, different URL patterns).

**Assessment:** User tested multiple rewrite patterns with inconsistent results. Works in some cases but not others. Testing notes show some patterns fail in built output. Needs clarification: does rewrites support regex? How does it differ in dev vs SSG build? Uses vitepress-sidebar plugin which may interact with rewrites. Needs minimal repro.

**Action:** Ask for minimal reproduction with specific rewrite pattern that fails; clarify rewrites regex support and dev/build differences.

#### [#4667](https://github.com/vuejs/vitepress/issues/4667) Hydration mismatch errors when copying a site from `v0.10.0` to `stable`, but only for two-digit version numbers?

`P2` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 3 · opened 1.3y ago, active 6d ago · labels: `stale`

Hydration mismatch errors with versioned docs (e.g. v0.10.0) when copied to 'stable' symlink, but not with two-digit versions like v0.9.7.

**Assessment:** Valid bug with detailed reproduction. Maintainer brc-dd pinpointed root cause: VitePress uses base.length in hydration checks, causing mismatch when base lengths differ (e.g. 'stable' vs 'v0.10.0'). User acknowledges workarounds; community could benefit from fix or documentation.

**Action:** Implement fix for base.length-dependent hydration logic, or document limitation with workarounds for versioned docs patterns.

#### [#4907](https://github.com/vuejs/vitepress/issues/4907) The variable in the page title is not replaced under dynamic routing

`P2` `likely-answered` `effort:S` `repro:no` · 👍 0 · 💬 1 · opened 11mo ago, active 6d ago · labels: `stale`

Variables in page titles (e.g., `{{ $frontmatter.x }}`) are not replaced in HTML title element during SSR for dynamic routes.

**Assessment:** Valid bug but with documented design restriction. brc-dd clarified that dynamic frontmatter injection in titles is not supported in SSR due to design limitations. User advised to create a heading component or use useTitle from vueuse instead. Issue #4844 references related limitation.

**Action:** Close: answered — this is a design restriction. Document workaround in FAQ or guide.

## i18n (1)

#### [#4347](https://github.com/vuejs/vitepress/issues/4347) Language selector (i18n) points to missing URLs

`P2` `actionable` `effort:M` `repro:no` · 👍 0 · 💬 0 · opened 1.6y ago, active 6d ago · labels: `stale`

Language selector in i18n setups links to non-existent localized pages. Should fall back to parent directory or language root when page doesn't exist in selected language.

**Assessment:** Valid issue with clear expected behavior. No comments in thread. Straightforward fix needed in theme component that handles language switching logic.

**Action:** Implement fallback logic in language selector to check parent paths when target locale page doesn't exist.

## Config (4)

#### [#3246](https://github.com/vuejs/vitepress/issues/3246) Logo not picked up if not in `public` folder

`P2` `actionable` `effort:S` `repro:yes` · 👍 0 · 💬 4 · opened 2.6y ago, active 2d ago · labels: `stale`

Logo not picked up unless placed in `public/` folder. Documentation suggests logos work from root, but they're not included in build output without public/ placement.

**Assessment:** Valid issue with inconsistent docs. Static assets auto-detected, but logo must be in public/. Root cause is asset bundling behavior. Later comments show some users worked around via version updates; one still hit the issue. Vite 8 migration may have changed behavior.

**Action:** Clarify docs (logo must be in public/) or fix asset handling to auto-pick logos from asset directories.

#### [#4173](https://github.com/vuejs/vitepress/issues/4173) Site config and data loaders don't support subpath imports

`P2` `upstream-blocked` `effort:L` `repro:yes` · 👍 0 · 💬 5 · opened 1.8y ago, active 6d ago · labels: `stale`

Subpath imports from package.json (e.g., #*: ./*) don't work in .vitepress/config.ts and *.data.ts files due to Node/esbuild module resolution limitations.

**Assessment:** Valid bug but upstream-blocked on Node/esbuild: esbuild doesn't resolve subpath imports without proper extensions. brc-dd confirmed Node doesn't auto-resolve TS extensions. Workarounds: use .js extension, use tsx/bun. TS support requires Node experimental strip types or manual file watching.

**Action:** Document limitation and workarounds. Consider if VitePress can add esbuild plugin for subpath resolution in config loader.

#### [#4725](https://github.com/vuejs/vitepress/issues/4725) createMarkdownRenderer的参数未生效

`P2` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 1 · opened 1.2y ago, active 6d ago · labels: `stale`

createMarkdownRenderer function parameters not taking effect when called in buildEnd hook with custom markdown config options.

**Assessment:** Valid bug with reproduction steps. User attempts to use createMarkdownRenderer in buildEnd for RSS generation but custom options are ignored. Maintainer brc-dd explains: VitePress supports singleton renderer only; options are inferred from config, not passed per-call. Design limitation documented but not intuitive.

**Action:** Clarify in documentation that createMarkdownRenderer uses singleton config—consider refactoring API for v2 to support per-call options.

#### [#4976](https://github.com/vuejs/vitepress/issues/4976) Symbolic linked `srcDir` makes redirects

`P2` `possibly-fixed` `effort:S` `repro:yes` · 👍 0 · 💬 4 · opened 9mo ago, active 6d ago · labels: `stale`

Using a symlinked srcDir causes unwanted redirects to `/docs/` path instead of serving from root.

**Assessment:** User reported redirect issue with symlinked srcDir. Maintainer brc-dd questioned the redirect but acknowledged issue; provided workaround to use srcDir value instead of symlink. User accepted workaround. May not be a priority for core fix given simple workaround exists.

**Action:** Document workaround in migration guide; defer core fix unless symlink support is planned feature.

## CLI (1)

#### [#2873](https://github.com/vuejs/vitepress/issues/2873) npx vitepress init does not install the vitepress dependency in package.json

`P1` `actionable` `effort:S` `repro:yes` · 👍 3 · 💬 2 · opened 2.8y ago, active 2d ago · labels: `stale`

npx vitepress init creates package.json scripts but does not install vitepress as a dependency, causing 'vitepress: command not found' when running scripts.

**Assessment:** Valid bug with clear repro. brc-dd confirmed it's currently by design (expects prior installation), but acknowledged it's unintuitive compared to other CLI tools (vite, next, etc.). User report shows this breaks workflow expectations.

**Action:** Fix: make 'npx vitepress init' automatically install vitepress as dependency in package.json. Aligns with modern CLI tool conventions.

## Types (1)

#### [#4662](https://github.com/vuejs/vitepress/issues/4662) vue-tsc reports errors on clean VitePress .md files

`P2` `upstream-blocked` `effort:M` `repro:yes` · 👍 0 · 💬 2 · opened 1.3y ago, active 6d ago · labels: `upstream` `stale`

vue-tsc reports 'Unable to resolve embedded' errors on .md files when vitePressExtensions is configured in tsconfig.

**Assessment:** Legitimate bug with clear reproduction steps. Maintainer brc-dd identified root cause as upstream (vue-tsc/language-tools). Author created upstream issue #5299 in vuejs/language-tools. Labeled 'upstream' and 'stale'; blocked until language-tools fix.

**Action:** Verify fix in language-tools#5299; mark fixed if upstream resolves.

## CSS / styling (6)

#### [#570](https://github.com/vuejs/vitepress/issues/570) Font normalization makes user font size override in browser impossible

`P2` `actionable` `effort:S` `repro:yes` · 👍 2 · 💬 0 · opened 4.3y ago, active 2d ago · labels: `theme` `stale`

Default theme uses absolute px units for font-size, preventing user browser font-size overrides—an important accessibility feature.

**Assessment:** Valid accessibility issue (Wikimedia Foundation reported). No comments/pushback. Simple fix: replace px with rem/em in theme CSS on root and derived elements. Straightforward accessibility win.

**Action:** Audit default theme CSS; migrate all font-size px → rem (use 16px as base); test browser font-size overrides.

#### [#3154](https://github.com/vuejs/vitepress/issues/3154) code not is black

`P2` `possibly-fixed` `effort:S` `repro:no` · 👍 0 · 💬 7 · opened 2.7y ago, active 2d ago · labels: `theme` `has-workaround` `stale`

Code block background color changed from dark to light in RC.24 when using custom themes like 'material-theme-palenight'.

**Assessment:** Regression in RC.24 related to adaptive theming. brc-dd provided workaround using CSS variables. Issue has 'has-workaround' label. Related to markdown theme handling. Recent comments show workarounds exist and users found solutions.

**Action:** Verify if fixed in 1.x or 2.0.0-alpha. If still present, clarify interaction between markdown.theme config and adaptive styling in docs.

#### [#4971](https://github.com/vuejs/vitepress/issues/4971) Font weights are not rendered correctly on Android.

`P2` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 22 · opened 9mo ago, active 6d ago · labels: `bug: pending triage` `stale`

On some Android devices, font weights for non-English headings are not rendered correctly and appear the same as body text.

**Assessment:** Real bug with detailed investigation in thread. Root cause identified: Chrome refuses to synthesize weights for local fonts at weight 600. brc-dd proposed removing `font-synthesis` CSS restriction as fix; noted loading Noto Sans CJK would ensure consistent weights. Device/OS-specific repro.

**Action:** Investigate font-synthesis removal and test with Noto Sans CJK; may be fixable by adjusting CSS or web font loading.

#### [#5187](https://github.com/vuejs/vitepress/issues/5187) Headings h3/h4 has no bottom margin

`P2` `actionable` `effort:S` `repro:no` · 👍 0 · 💬 0 · opened 2mo ago, active 6d ago · labels: `bug: pending triage` `stale`

h3 and h4 headings lack bottom margin in VitePress default theme, unlike Typora rendering.

**Assessment:** Valid CSS consistency issue. User provides clear expected fix (add margin-bottom: 16px). Author is uncertain if intentional. No maintainer response. Marked stale but appears to be a genuine styling oversight.

**Action:** Verify if margin was intentionally removed; if not, apply suggested CSS fix to default theme.

#### [#3086](https://github.com/vuejs/vitepress/issues/3086) Navbar visible on print when navbar param equals false

`P3` `actionable` `effort:S` `repro:yes` · 👍 0 · 💬 0 · opened 2.7y ago, active 2d ago · labels: `stale`

navbar: false frontmatter hides navbar in browser view but navbar still appears in printed pages/PDFs.

**Assessment:** Reproducible CSS issue with clear steps. Print styles not respecting frontmatter navbar setting. No comments in thread yet. Valid but low-priority cosmetic bug.

**Action:** Fix — add print media query to CSS to honor navbar: false in @media print rules.

#### [#3313](https://github.com/vuejs/vitepress/issues/3313) Consider not using opacity for any text content

`P3` `actionable` `effort:S` `repro:yes` · 👍 0 · 💬 1 · opened 2.6y ago, active 2d ago · labels: `theme` `a11y` `stale`

Text opacity in dark mode causes font rendering artifacts on some scripts (Farsi/Persian fonts). Overlaying glyph paths become visible when zoom is applied.

**Assessment:** Valid a11y/rendering bug affecting non-Latin scripts. One comment from designer suggests intent of opacity is intentional (reduce harshness). No consensus on fix. Conservative approach needed to avoid breaking other use cases.

**Action:** Investigate CSS approach that avoids opacity for text while preserving dark mode design goals. Consider offering CSS variable override for affected languages.

## Head / meta (3)

#### [#1298](https://github.com/vuejs/vitepress/issues/1298) Content Security Policy: Ensure innerHTML assignments are TrustedHTML and publish security policy name, hashes

`P1` `actionable` `effort:L` `repro:yes` · 👍 2 · 💬 1 · opened 3.8y ago, active 2d ago · labels: `stale`

Strict Content Security Policy (CSP) with trusted-types breaks VitePress sites due to innerHTML assignments being strings instead of TrustedHTML.

**Assessment:** Security-critical issue. Users cannot use strict CSP with VitePress. Requires either TrustedHTML API usage or exposing policy names/hashes. Recent follow-up (Jun 2025) shows this is still unresolved. Important for security-conscious deployments.

**Action:** Audit innerHTML usage in build output; implement TrustedHTML assignments or provide CSP configuration option with hashes for inlined scripts/styles.

#### [#2419](https://github.com/vuejs/vitepress/issues/2419) Frontmatter: images in meta tags aren't resolved/bundled

`P2` `actionable` `effort:M` `repro:no` · 👍 2 · 💬 0 · opened 3.1y ago, active 2d ago · labels: `bug: pending triage` `stale`

Images referenced in frontmatter for meta tags (e.g., og:image) are not resolved or bundled, while the same image paths work in markdown. Assets are not copied and paths are not transformed.

**Assessment:** Valid bug affecting meta tag configuration. User attempted relative paths and url() syntax without success. No reproduction provided but issue is clear and reproducible. Affects SEO/meta functionality. Low engagement (2 thumbs up) but valid concern.

**Action:** Verify reproducer can be created; likely needs asset resolution hook in frontmatter processing pipeline or better docs on workaround (transformHead).

#### [#4640](https://github.com/vuejs/vitepress/issues/4640) Page title is not interpolated inside <title> element

`P2` `possible-duplicate` `effort:M` `repro:yes` · 👍 1 · 💬 2 · opened 1.3y ago, active 6d ago · labels: `bug: pending triage` `stale`

Page title with Vue interpolation (e.g., `# {{ title }}`) renders correctly as an h1 but the interpolated value does not appear in the `<title>` HTML element; raw template syntax shows instead.

**Assessment:** Valid bug with StackBlitz reproduction. Related to #4637 (Tynopia mentioned both issues and offered workaround). Root cause: title extraction happens at build time before Vue compilation. Needs triage: is this expected or should build-time title extraction support Vue interpolation?

**Action:** Link to #4637, await PR #4630 review — likely needs custom head extraction or defer Vue compilation until runtime for title extraction.

## Accessibility (3)

#### [#2980](https://github.com/vuejs/vitepress/issues/2980) a11y: Headings are read verbosely by VoiceOver

`P1` `actionable` `effort:S` `repro:yes` · 👍 0 · 💬 0 · opened 2.8y ago, active 2d ago · labels: `a11y` `stale`

Heading elements lack `aria-label` attribute, causing VoiceOver to read heading text verbosely with extra description. Should render as simple `aria-label="Heading Text"`.

**Assessment:** Valid accessibility bug with clear repro on official VitePress docs. User demonstrates exact HTML output needed and offers to contribute. No maintainer response yet. Straightforward fix in markdown-it heading rendering rules.

**Action:** Add aria-label to heading elements in default markdown-it renderer; accept community contribution if offered.

#### [#3517](https://github.com/vuejs/vitepress/issues/3517) accessibility: interactive controls should not be nested

`P1` `actionable` `effort:M` `repro:yes` · 👍 0 · 💬 0 · opened 2.4y ago, active 6d ago · labels: `theme` `a11y` `stale`

Accessibility bug: nested interactive controls in collapsible sidebar (header with tabindex=0 contains a button), violating WCAG nesting rules.

**Assessment:** Valid a11y issue with clear description and screenshot. Correctly identified structural problem (nested interactive elements). No maintainer response. Blocks accessibility compliance.

**Action:** Fix sidebar group header markup to use single interactive control or remove tabindex from group container; test with ARIA tools.

#### [#2085](https://github.com/vuejs/vitepress/issues/2085) navbar inconsistent behaviour

`P2` `actionable` `effort:S` `repro:yes` · 👍 0 · 💬 6 · opened 3.3y ago, active 2d ago · labels: `a11y` `stale`

Navbar hover states are inconsistent and not fully WCAG compliant: title fades (low contrast), dropdown items reduce contrast, social links increase contrast differently.

**Assessment:** Valid accessibility concern. Maintainer (Evan You) clarified that logo is exempt from contrast as a logotype per WCAG, and that hover underline not required. Issue reporter agrees on some points but maintainer closed discussion noting these are mostly preferences.

**Action:** Standardize hover/focus states across nav components; ensure all interactive elements meet WCAG AA minimum contrast in both hover and default states.

## Other (1)

#### [#4242](https://github.com/vuejs/vitepress/issues/4242) [Memory Leak]: Builds incorporating Shiki Twoslash crash with JavaScript heap out of memory errors

`P1` `upstream-blocked` `effort:M` `repro:yes` · 👍 4 · 💬 7 · opened 1.8y ago, active 6d ago · labels: `upstream` `perf` `stale`

Memory leak causing JavaScript heap out-of-memory errors when building with Shiki Twoslash transformers on large documentation.

**Assessment:** Confirmed reproducible bug with detailed repro repo. brc-dd suggested moving to Shiki upstream. Issue opened there (#796). Multiple affected users (Effect-TS, Graffle, Nolebase). Later investigation by nekomeowww pinpointed Vue 3.5.6+ or Vite 5.4.8+/VitePress 1.4.0+ as potential causes.

**Action:** Track upstream Shiki issue #796; may also relate to Vite/Vue dependency changes per comments.

